# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*.local

# Turbo
.turbo/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test
coverage/
.nyc_output/

# Supabase CLI cache (local)
supabase/.temp/

# Claude Code local settings (keep .claude/agents/ tracked, only settings.json is local)
.claude/settings.json
.claude/settings.local.json
.env.production

.kite-passport/

# Local DB backups — never commit prod data
backups/

# Vercel CLI auth (any subdir)
.vercel/

# Stale legacy duplicate of .nexus/project-context.md (CLAUDE.md → use .nexus/ as canonical)
/project-context.md

# Local ops/smoke helpers — machine-specific (abs paths + dev-tokens.env), NOT for CI.
# El settlement downstream x402 ya se validó on-chain (Base tx 0x9fb7…, Avax tx 0xb9cf…).
/scripts/diag-base-settle.mjs
/scripts/diag-base-settle-run.sh
/scripts/smoke-base-downstream.mjs
/scripts/smoke-base-downstream-run.sh
/scripts/enable-base-downstream.sh
/scripts/set-avax-facilitator-key.sh
