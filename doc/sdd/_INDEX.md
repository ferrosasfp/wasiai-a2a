# SDD Index — WasiAI A2A Protocol

| # | Fecha | HU | Tipo | Mode | Status | Branch |
|---|-------|----|------|------|--------|--------|
| 001 | 2026-04-01 | Kite Chain — Conexion Ozone Testnet | feature | QUALITY | DONE | feat/wkh-5-kite-chain |
| 002 | 2026-04-02 | x402 Payment Middleware — Kite Service Provider | feature | QUALITY | DONE | feat/wkh-6-kite-payment-clean |
| 003 | 2026-04-02 | Supabase — Migrar registries de in-memory a PostgreSQL | evolutivo | QUALITY | DONE | feat/wkh-7-supabase-registries |
| 004 | 2026-04-02 | Migrar framework de Hono a Fastify | refactor | QUALITY | DONE | feat/wkh-20-fastify-migration |
| 006 | 2026-04-03 | Agent Cards — Google A2A spec (WKH-15) | feature | QUALITY | DONE | feat/wkh-15-agent-cards |
| 007 | 2026-04-03 | Tasks DB — A2A Task CRUD (WKH-23) | feature | QUALITY | DONE (fila duplicada: la de abajo, misma HU y misma rama, es la vigente). Corregido 2026-07-29: decía "WIP"; el trabajo está en `main` (`src/services/task.ts`, `src/routes/tasks.ts`) y la rama `feat/wkh-23-tasks-db` ya no existe ni local ni remota. | feat/wkh-23-tasks-db |
| 007 | 2026-04-03 | Tasks DB — A2A task lifecycle (WKH-23) | feature | QUALITY | DONE | feat/wkh-23-tasks-db |
| 008 | 2026-04-03 | x402 compose — client-side payment (WKH-9) | feature | QUALITY | DONE | feat/wkh-9-x402-compose |
| 009 | 2026-04-04 | Mock Community Hub registry (WKH-32) | feature | SPEED | DONE | feat/wkh-32-mock-registry |
| 011 | 2026-04-04 | LLM Planning — Claude selects agents by goal (WKH-10) | feature | QUALITY | DONE | feat/wkh-10-llm-planner |
| 012 | 2026-04-04 | POST /orchestrate — Flujo completo goal→discover→compose→pay (WKH-13) | feature | QUALITY | DONE | feat/wkh-13-orchestrate-full |
| 013 | 2026-04-04 | Schema Transform — LLM adapts output/input between agents (WKH-14) | feature | QUALITY | DONE | feat/wkh-14-schema-transform |
| 014 | 2026-04-05 | Dashboard Analytics — KPIs + events + UI (WKH-27) | feature | QUALITY | DONE | feat/wkh-27-dashboard |
| 015 | 2026-04-05 | POST /orchestrate — LLM Planning + Fallback (WKH-13) | evolutivo | QUALITY | DONE | feat/015-orchestrate-llm-planning |
| 016 | 2026-04-06 | Build — copy src/static/* into dist/static (fixes /dashboard ENOENT) | patch | FAST | DONE | main (5a14ab8) |
| 018 | 2026-04-06 | Gasless Integration EIP-3009 — testnet PYUSD (WKH-29) | feature | QUALITY | DONE | feat/018-gasless-aa |
| 020 | 2026-04-09 | Kite contracts source-of-truth doc + DEPRECATED banner in spike (WKH-36) | patch | FAST | DONE | main |
| 021 | 2026-04-09 | Pitch Fase 0 — Passport drift fix + chain-adaptive framing + types cleanup (WKH-37) | patch | FAST | DONE | main + wasiai-landing |
| 022 | 2026-04-06 | Gasless graceful degradation (WKH-38) | feature | FAST+AR | DONE | feat/022-gasless-degradation |
| 025 | 2026-04-06 | A2A Key Middleware — requirePaymentOrA2AKey (WKH-34-W4) | feature | LAUNCH | DONE | feat/025-a2a-key-middleware |
| 026 | 2026-04-06 | Hardening — Rate Limiting, Error Limiting, Circuit Breaker, Backpressure (WKH-18) | feature | QUALITY | DONE | feat/026-hardening |
| 027 | 2026-04-06 | Demo Script E2E — smoke test automatizado (WKH-30) | feature | FAST | DONE | feat/027-demo-script-e2e |
| 028 | 2026-04-06 | README.md rewrite — reflect production architecture (WKH-17) | doc | FAST | DONE | feat/028-readme-rewrite |
| 029 | 2026-04-06 | E2E Test Suite — full middleware chain + inject (WKH-E2E) | test | QUALITY | DONE | feat/029-e2e-tests |
| 030 | 2026-04-06 | GET /health endpoint (WKH-HEALTH) | patch | FAST | DONE | main |
| 031 | 2026-04-06 | POST /discover alias — fix 404 for POST callers (WKH-DISCOVER-POST) | bugfix | FAST | DONE | feat/031-discover-post |
| 032 | 2026-04-06 | Bearer Auth — Authorization: Bearer as alternative to x-a2a-key (WKH-BEARER-AUTH) | feature | FAST+AR | DONE | feat/032-bearer-auth |
| 033 | 2026-04-06 | Invoke docs — clarify proxy invocation in Agent Card + /discover (WKH-INVOKE-DOCS) | doc | FAST | DONE | feat/033-invoke-docs |
| 034 | 2026-04-06 | Global Event Tracking — onResponse hook for all endpoints (WKH-EVENT-TRACKING) | feature | FAST+AR | DONE | feat/034-event-tracking |
| 035 | 2026-04-06 | Bearer Auth Fix on /auth/* + Test Hardening (WKH-BEARER-FIX) | bugfix | FAST+AR | DONE | feat/035-bearer-fix |
| 036 | 2026-04-06 | Sync .env.example + README.md with current features (WKH-DOCS-SYNC) | doc | FAST | DONE | feat/036-docs-sync |
| 037 | 2026-04-06 | Migrate x402 from v1 to v2 — Pieverse format (WKH-X402-V2) | bugfix | QUALITY | DONE | feat/037-x402-v2 |
| 038 | 2026-04-11 | Biome Linter + Formatter (WKH-QG-LINT) | tooling | FAST | DONE | feat/038-biome-linter |
| 039 | 2026-04-11 | Security Headers — X-Content-Type-Options + X-Frame-Options (WKH-QG-HEADERS) | patch | FAST | DONE | feat/039-security-headers |
| 040 | 2026-04-12 | Discover verified+status — filtro activos por defecto (WKH-DISCOVER-VERIFIED) | feature | FAST+AR | DONE | main (779f93a) |
| 041 | 2026-04-13 | Migrate x402 payment token from PYUSD to KXUSD (WKH-KXUSD) | config | FAST+AR | DONE | main |
| 042 | 2026-04-13 | MCP Server x402 — Tools para Claude Managed Agent (WKH-MCP-X402) | feature | QUALITY | DONE | feat/042-mcp-server-x402 |
| 043 | 2026-04-20 | Security Hardening — HSTS + CORS restrictivo + requireAuth en /registries (WKH-SEC-01) | security | QUALITY | DONE | feat/043-wkh-sec-01-hardening |
| 044 | 2026-04-21 | 1% Protocol Fee Real Charge — deduct from budget + transfer to Kite wallet (WKH-44) | feature | QUALITY | DONE | feat/044-wkh-44-protocol-fee-real-charge |
| 046 | 2026-04-20 | Marketplace Integration Guide — Auth + CORS policy for 3rd-party consumers (WKH-46) | doc | FAST | DONE | feat/046-wkh-46-integration-guide |
| 052 | 2026-04-21 | Migrate x402 payment token from KXUSD → PYUSD (WKH-52) | config | QUALITY | DONE | feat/052-wkh-52-pyusd-migration |
| 053 | 2026-04-22 | Supabase RLS + ownership checks en queries (WKH-53) | security | QUALITY | DONE | feat/wkh-53-rls-ownership ([DONE.md](053-wkh-53-rls-ownership/DONE.md)) |
| 054 | 2026-04-24 | Downstream x402 Payment — wasiai-a2a → wasiai-v2 Agents (Avalanche Fuji) (WKH-55) | feature | QUALITY | DONE | feat/wkh-55-downstream-x402-fuji |
| 055 | 2026-04-26 | A2A Fast-Path en compose — passthrough estructurado entre agentes A2A-compliant (WKH-56) | feature | QUALITY | DONE | feat/055-wkh-56-a2a-fast-path ([done-report.md](055-wkh-56-a2a-fast-path/done-report.md)) |
| 056 | 2026-04-26 | LLM Bridge Pro — model selector + verification + cache fingerprint + telemetry (WKH-57) | feature | QUALITY | DONE | feat/056-wkh-57-llm-bridge-pro ([done-report.md](056-wkh-57-llm-bridge-pro/done-report.md)) |
| 057 | 2026-04-27 | Defensive fallback en discovery para v2 schema drift (WAS-V2-3-CLIENT / WKH-57) | bugfix | QUALITY | DONE | feat/057-wkh-57-was-v2-3-client ([done-report.md](057-wkh-57-was-v2-3-client/done-report.md)) |
| 058 | 2026-04-27 | SSRF Protection — validateRegistryUrl en discoveryEndpoint (WKH-62 / SEC-SSRF-1) | security | QUALITY | DONE | feat/058-wkh-62-sec-ssrf-1 ([done-report.md](058-wkh-62-sec-ssrf-1/done-report.md)) |
| 059 | 2026-04-27 | SEC-SCOPE-1: requirePaymentOrA2AKey — checkScoping con target vacío (WKH-61) | security | QUALITY | DONE | feat/059-wkh-61-sec-scope-1 ([done-report.md](059-wkh-61-sec-scope-1/done-report.md)) |
| 060 | 2026-04-27 | registries CRUD sin ownership — cross-tenant takeover del discovery surface (WKH-63 / SEC-REG-1) | security | QUALITY | DONE | feat/060-wkh-63-sec-reg-1 ([done-report.md](060-wkh-63-sec-reg-1/done-report.md)) |
| 061 | 2026-04-27 | SEC-DRAIN-1: /gasless/transfer drain del operator wallet con budget mínimo (WKH-59) | security | QUALITY | DONE | feat/061-wkh-59-sec-drain-1 ([done-report.md](061-wkh-59-sec-drain-1/done-report.md)) |
| 062 | 2026-04-27 | SEC-RCE-1: L2 transform cache poisoning + new Function() = RCE multi-tenant (WKH-60) | security | QUALITY | DONE | feat/062-wkh-60-sec-rce-1 ([done-report.md](062-wkh-60-sec-rce-1/done-report.md)) |
| 063 | 2026-04-28 | Cross-Chain E2E Retro — PRs #48-#52 consolidated post-mortem (Kite PYUSD inbound + Fuji USDC outbound) | retro | QUALITY | DONE | main (#48 edde596 → #52 e4d217d) ([done-report.md](063-cross-chain-e2e-retro/done-report.md)) |
| 064 | 2026-04-28 | Forward-key middleware — llamadas internas v2 → wasiai-a2a (WKH-65) | security | FAST+AR | DONE | feat/064-wkh-65-a2a-forward-key ([done-report.md](064-wkh-65-a2a-forward-key/done-report.md)) |
| 069 | 2026-04-30 | [MCP-X402] Build wasiai-x402 MCP server for Claude Console managed agents (WKH-64) | feature | QUALITY | DONE | feat/069-wkh-64-mcp-x402 ([done-report.md](069-wkh-64-mcp-x402/done-report.md)) |
| 070 | 2026-04-30 | [MCP-VERCEL] HTTP transport for wasiai-x402 + Vercel remote deploy (WKH-65) | feature | FAST+AR | DONE | feat/070-wkh-65-mcp-vercel-deploy ([done-report.md](070-wkh-65-mcp-vercel-deploy/done-report.md)) |
| 071 | 2026-04-29 | [PROD-HARDENING] wasiai-x402 MCP — concurrency + monitoring + chaos tests (WKH-66) | feature | QUALITY | DONE | feat/071-wkh-66-prod-hardening ([done-report.md](071-wkh-66-prod-hardening/done-report.md)) |
| 072 | 2026-04-30 | [BUG-WKH66] Balance-gate decimals mismatch — PYUSD inbound vs USDC outbound (WKH-67) | bugfix | QUALITY | DONE | fix/072-wkh-67-balance-gate-decimals ([done-report.md](072-wkh-67-balance-gate-decimals/done-report.md)) |
| 073 | 2026-05-01 | README Refresh — Production Architecture + Cross-Chain Narrative (WKH-79) | doc | FAST | DONE | feat/073-wkh-79-readme-refresh ([done-report.md](073-wkh-79-readme-refresh/done-report.md)) |
| 074 | 2026-05-01 | Operator Identities Runbook — centralize secrets, recovery flows, bus factor (WKH-80) | doc | FAST | DONE | docs/074-wkh-80-operator-identities-runbook ([done-report.md](074-wkh-80-operator-identities-runbook/done-report.md)) |
| spike | 2026-05-01 | [KITE-PASSPORT] Integration spike — Model B Hybrid approved (WKH-68) | spike | FAST | DONE | main `f896fa0` ([decision-doc.md](spike-kite-passport/decision-doc.md) · [poc-results.md](spike-kite-passport/poc-results.md) · [discovery-notes.md](spike-kite-passport/discovery-notes.md)) |
| 075 | 2026-05-01 | DB Migration Pre-flight Checks Runbook (WKH-78) | tooling | FAST+AR | DONE | feat/075-wkh-78-migration-preflight ([done-report.md](075-wkh-78-migration-preflight/done-report.md)) |
| 076 | 2026-05-01 | [BEARER-ROTATION] Headless rotation cron + dual-bearer overlap + 24h invalidation (WKH-75) | feature | QUALITY | DONE | feat/076-wkh-75-bearer-rotation-cron ([done-report.md](076-wkh-75-bearer-rotation-cron/done-report.md)) |
| 077 | 2026-05-01 | Public Docs & Onboarding — getting-started + API reference + MCP integration + networks (WKH-82) | doc | QUALITY | DONE | feat/077-wkh-82-public-docs-onboarding ([done-report.md](077-wkh-82-public-docs-onboarding/done-report.md)) |
| 078 | 2026-05-03 | [WKH-90] Discord-aware payload formatting in alerts.mjs | patch | FAST+AR | DONE | feat/078-wkh-90-discord-alert-formatter ([done-report.md](078-wkh-90-discord-alert-formatter/done-report.md)) |
| 079 | 2026-05-03 | [BUG-WKH75] setup-cronjob.mjs sends crontab strings to cron-job.org API (WKH-89) | bugfix | FAST+AR | DONE | feat/079-wkh-89-cronjob-schedule-fix ([done-report.md](079-wkh-89-cronjob-schedule-fix/done-report.md)) |
| 080 | 2026-05-04 | [WKH-88] Bearer rotation refinements — HTTP method gate + JSDoc + KV mutex + tests | hardening | FAST+AR | DONE | feat/080-wkh-88-bearer-rotation-refinements ([done-report.md](080-wkh-88-bearer-rotation-refinements/done-report.md)) |
| 081 | 2026-05-03 | [WKH-91] Discord formatter polish — truncate, hostname, test hardening | polish | FAST | DONE | feat/081-wkh-91-discord-formatter-polish ([done-report.md](081-wkh-91-discord-formatter-polish/done-report.md)) |
| 082 | 2026-05-04 | [WKH-86] Migration pre-flight refinements — manifest + DROP IF EXISTS + security hardening | tooling | FAST | DONE | feat/082-wkh-86-migration-preflight-refinements ([done-report.md](082-wkh-86-migration-preflight-refinements/done-report.md)) |
| 083 | 2026-05-04 | [WKH-87] Public docs refinements — Node version + chain.ts inline + decimal drift carry-forward | doc | FAST | DONE | docs/083-wkh-87-public-docs-refinements ([done-report.md](083-wkh-87-public-docs-refinements/done-report.md)) |
| 084 | 2026-05-04 | [KITE-PASSPORT] Model B Hybrid — Passport inbound + operator outbound cross-chain (WKH-69) | feature | QUALITY | DONE | feat/084-wkh-69-passport-hybrid-inbound ([done-report.md](084-wkh-69-passport-hybrid-inbound/done-report.md)) |
| 085 | 2026-05-03 | [WKH-92] Autonomous Passport x402 smoke runner | tooling | FAST | DONE | feat/085-wkh-92-passport-smoke-autonomous ([done-report.md](085-wkh-92-passport-smoke-autonomous/done-report.md)) |
| 086 | 2026-05-13 | [WKH-MULTICHAIN] Multi-chain support — Avalanche Fuji/mainnet + Kite testnet/mainnet | feature | QUALITY | DONE | feat/086-wkh-multichain-a2a ([done-report.md](086-wkh-multichain-a2a/done-report.md)) |
| 087 | 2026-05-14 | [WKH-59] Middleware /compose debit reads real agent price from registry (real-price-debit) | feature | QUALITY | DONE | feat/087-wkh-59-real-agent-price-debit ([done-report.md](087-wkh-59-real-agent-price-debit/done-report.md)) |
| 088 | 2026-05-19 | [BASE-01] Base chain adapter (sepolia + mainnet) — USDC EIP-3009 path (WKH-104) | feature | QUALITY | DONE | feat/wkh-base-port-v1 ([done-report.md](088-wkh-104-base-adapter/done-report.md)) |
| 090 | 2026-05-19 | [BASE-03] Bazaar Discovery Extension + Agent-Card Schemas (WKH-106) | feature | FAST+AR | DONE | feat/wkh-base-port-v1 ([done-report.md](090-wkh-106-bazaar-extension/done-report.md)) |
| 091 | 2026-05-19 | [BASE-04] Smoke E2E Base Sepolia con tx hash real en Basescan (WKH-107) | test | FAST | DONE | feat/wkh-base-port-v1 ([done-report.md](091-wkh-107-smoke-base-sepolia/done-report.md)) |
| 092 | 2026-05-19 | [BASE-05] README "Base Support" + integration guide Base (WKH-108) | doc | FAST | DONE | feat/wkh-base-port-v1 ([done-report.md](092-wkh-108-base-docs/done-report.md)) |
| 093 | 2026-05-27 | [BASE-06] x402 payment path chain-aware — rutear verify/settle a x-payment-chain (WKH-111) | feature | QUALITY | DONE | feat/093-wkh-111-x402-chain-aware ([done-report.md](093-wkh-111-x402-chain-aware/done-report.md)) |
| 094 | 2026-05-27 | [BASE-07] outbound downstream payment chain-aware — settle a sub-agentes en Base/Kite (no solo Avalanche) (WKH-112) | feature | QUALITY | DONE | feat/094-wkh-112-downstream-chain-aware ([done-report.md](094-wkh-112-downstream-chain-aware/done-report.md)) |
| 095 | 2026-05-27 | [BASE-08] discovery chain validation dinámica (sin hardcode) + compose chain-flow — outbound settle real en las 3 chains (WKH-113) | feature | QUALITY | DONE | feat/095-wkh-113-discovery-chain-dynamic ([done-report.md](095-wkh-113-discovery-chain-dynamic/done-report.md)) |
| 096 | 2026-05-29 | [WKH-35] Fondeo verificado on-chain de agent keys (budget prepago) — re-habilitar POST /deposit multi-chain (Kite/Avalanche/Base) | feature | QUALITY | DONE (implementado, no desplegado — pendiente aplicar 2 migraciones + env vars) | feat/wkh-base-port-v1 ([DONE.md](096-wkh-35-deposit-onchain/DONE.md)) |
| 097 | 2026-05-29 | [WKH-AUDIT-A2A] Remediación Auditoría Profesional — Hardening + Hygiene | security | QUALITY | DONE | feat/097-wkh-audit-a2a-hardening ([done-report.md](097-remediacion-auditoria-a2a/done-report.md)) |
| 098 | 2026-05-29 | [WKH-AUDIT-A2A-CLEANUP] Limpieza final — repo A+ (tsc + lint + isProduction) | hygiene | FAST+AR | DONE | feat/098-a2a-cleanup-aplus ([done-report.md](098-a2a-cleanup-aplus/done-report.md)) |
| 099 | 2026-05-30 | [WKH-DEPOSIT-INFO] GET /auth/deposit-info — public endpoint with per-chain funding info | feature | FAST+AR | DONE | feat/099-deposit-info ([done-report.md](099-deposit-info/done-report.md)) |
| 100 | 2026-05-31 | [WKH-100] wasiai-agentkey: ERC-8004 Identity Binding Fase 1 — register/resolve/bind AgentID en Base | feature | QUALITY | DONE | feat/100-wasiai-agentkey-erc8004 ([done-report.md](100-wasiai-agentkey/done-report.md)) |
| 101 | 2026-05-31 | [WKH-101] wasiai-agentkey Fase 2: EIP-712 Delegation + Session Key + Server-Side Enforcement | feature | QUALITY | DONE | feat/101-wasiai-agentkey-delegation ([done-report.md](101-wasiai-agentkey-delegation/done-report.md)) |
| 102 | 2026-05-31 | [WKH-102] Fix billing /orchestrate — debita steps 1..N de master keys (TD-WKH-101-ORCH) | bugfix | FAST+AR | DONE | feat/102-orchestrate-billing-fix ([done-report.md](102-orchestrate-billing-fix/done-report.md)) |
| 103 | 2026-05-31 | [WKH-103] wasiai-agentkey Fase 3: Reputación ERC-8004 (off-chain + on-chain read opcional) | feature | QUALITY | DONE | feat/103-wasiai-agentkey-reputation ([done-report.md](103-wasiai-agentkey-reputation/done-report.md)) |
| 104 | 2026-05-31 | [WKH-104] Tech Debt Closure — TDs WKH-101/102/103 (comment/drift/race-test/sybil) | tech-debt | QUALITY | DONE | feat/104-tech-debt-closure ([done-report.md](104-tech-debt-closure/done-report.md)) |
| 105 | 2026-05-31 | [WKH-105] Autonomía agéntica: SDK + agente de referencia (TypeScript) | feature | QUALITY | DONE | feat/105-autonomous-agent-sdk ([done-report.md](105-autonomous-agent-sdk/done-report.md)); fix-pack robustez (4 commits 306c2b6..e0e210f, E2E en vivo VALIDADO 2026-05-31) |
| 106 | 2026-05-31 | [BASE-02] a2a manda `Authorization: Bearer` al facilitator en verify/settle Base + borra caveat stale (WKH-106) — WIRING de auth, no build; facilitator ya settleaba Base Sepolia real (tx 0xb9b156e…ca35521d). TD-1 (Avalanche bearer gap): **CERRADO por WKH-107 (AVAX-BEARER, #107 DONE)**. | feature | QUALITY | DONE | feat/106-base-facilitator-settlement ([done-report.md](106-base-facilitator-settlement/done-report.md)) |
| 107 | 2026-06-01 | [AVAX-BEARER] a2a manda `Authorization: Bearer` al facilitator en verify/settle Avalanche (WKH-107) — ESPEJO de WKH-106; resuelve TD-1 de WKH-106. WIRING de auth, no build; facilitator ya settlea Avalanche (eip155:43113/43114). Strictly testnet (Fuji 43113). Pendiente ops: setear `AVALANCHE_FACILITATOR_API_KEY` en Railway + smoke E2E Fuji. | feature | QUALITY | DONE | feat/107-avalanche-facilitator-bearer ([done-report.md](107-avalanche-facilitator-bearer/done-report.md)) |
| 108 | 2026-06-01 | [WKH-108] Smoke de regresión commiteable (sin secretos) para el payout downstream x402, apto para CI — capa liviana network-only (facilitator health/supported + chains + breaker) que corre siempre + capa E2E opt-in (`RUN_DOWNSTREAM_E2E=1`) que skippea limpio sin secrets | test | FAST+AR | DONE | feat/108-wkh-108-smoke-downstream-x402-ci ([done-report.md](108-smoke-downstream-x402-ci/done-report.md)) |
| 109 | 2026-06-10 | [WKH-117] Kite Agent Passport como payer dual — alias X-PAYMENT + paymentOrigin telemetry + bind-passport + e2e dual-auth | feature | QUALITY | DONE | feat/WKH-117-kite-passport-dual-auth ([final-report.md](109-kite-passport-dual-auth/final-report.md)) |
| 110 | 2026-06-19 | [WKH-121] Session Keys sin EIP-712 — derivación server-side (user → agent → session) sin wallet requerida | security | QUALITY | DONE | feat/110-wkh-121-key-sessions ([report.md](110-wkh-121-key-sessions/report.md)) |
| 111 | 2026-06-19 | [WKH-122] Revocación granular e instantánea de session keys — DELETE /auth/key-session/:id con ownership guard owner_ref | security | QUALITY | DONE | feat/111-wkh-122-session-revoke ([report.md](111-wkh-122-session-revoke/report.md)) |
| 112 | 2026-06-19 | [WKH-123] KEY-SIGNED-AUTH: Auth por firma EIP-712 (master keys) + HMAC-SHA256 (session keys) — token filtrado no usable sin firma | security | QUALITY | DONE | feat/112-wkh-123-signed-auth ([report.md](112-wkh-123-signed-auth/report.md)) |
| 113 | 2026-06-19 | [WKH-124] KEY-RECEIPTS: Recibos inmutables + proof-chain (session → agent → user) — HMAC-SHA256 firmado + append-only + verify endpoint | feature | QUALITY | DONE | feat/113-wkh-124-receipts ([report.md](113-wkh-124-receipts/report.md)) |
| 114 | 2026-06-19 | [WKH-125] KEY-CONSTRAINTS: Constraints programables por destino (cap por vendor) + ventana de tiempo configurable (rolling/total) | feature | QUALITY | DONE | feat/114-wkh-125-constraints ([report.md](114-wkh-125-constraints/report.md)) |
| 115 | 2026-06-20 | [WKH-118] FEE-COMPOSE: Cobrar 1% protocol fee en /compose — best-effort, idempotente por request.id, mismo patron que orchestrate | feature | QUALITY | DONE | feat/115-wkh-118-fee-compose ([report.md](115-wkh-118-fee-compose/report.md)) |
| 116 | 2026-06-20 | [WKH-SEC-02] RLS Postgres-level — ENABLE ROW LEVEL SECURITY en 7 tablas con owner_ref + down script reversible | security | QUALITY | DONE | feat/116-wkh-sec-02-rls ([report.md](116-wkh-sec-02-rls/report.md)) |
| 117 | 2026-06-22 | [WKH-126b] Integración TS del Escrow No-Custodial — escrow-verifier + routing condicional /deposit + ABI EIP-712 + tests (contrato Solidity = WKH-126a, HU hermana/prerequisito) | feature | QUALITY | DONE | feat/117-wkh-126-escrow-noncustodial ([report.md](117-wkh-126-escrow-noncustodial/report.md)) |
| 118 | 2026-06-22 | [WKH-SEC-02c] RLS Postgres-level en `registries` y `kite_schema_transforms` — ENABLE ROW LEVEL SECURITY en 2 tablas con owner_ref (spinoff de WKH-SEC-02) | security | QUALITY | DONE | feat/118-wkh-sec-02c-rls-registries ([report.md](118-wkh-sec-02c-rls-registries/report.md)) |
| 119 | 2026-06-22 | [WKH-SEC-02b] Validar `p_owner_ref` dentro de `increment_a2a_key_spend` — Ownership Guard DB-level en RPC fundacional de débito | security | QUALITY | DONE | feat/119-wkh-sec-02b-owner-ref-rpc ([report.md](119-wkh-sec-02b-owner-ref-rpc/report.md)) |
| 120 | 2026-06-22 | [WKH-125b] Cap de gasto por destino en delegaciones EIP-712 — espejo del fix de session keys (d076ea8) para el camino debit_delegation_and_parent | security | QUALITY | DONE | fix/120-wkh-125b-delegation-dest-cap ([report.md](120-wkh-125b-delegation-dest-cap/report.md)) |
| 121 | 2026-06-22 | [WKH-126a] Contrato Solidity del Escrow No-Custodial — WasiAIEscrow.sol (Foundry): deploy/debit/debitBatch/withdraw + EIP-712 + UUPS upgradeable + deploy Base Sepolia testnet | feature/smart-contract | QUALITY | DONE | feat/121-wkh-126a-escrow-contract ([report.md](121-wkh-126a-escrow-contract/report.md)) |
| 122 | 2026-06-22 | [WKH-126c] Routing de escrow POR-CADENA — fallback a treasury en cadenas sin contrato (`escrowEnabledForChain` helper) | bugfix | FAST+AR | DONE | fix/122-wkh-126c-escrow-per-chain-routing ([report.md](122-wkh-126c-escrow-per-chain-routing/report.md)) |
| 123 | 2026-06-23 | [WKH-SEC-03] x402 inbound: ligar recipient + amount — binding `authorization.to`/`value` en middleware + adapters (bypass total del cobro x402 inbound, CRIT-1 auditoría) | security | QUALITY | DONE | fix/123-wkh-sec-03-x402-binding ([report.md](123-wkh-sec-03-x402-binding/report.md)) |
| 124 | 2026-06-23 | [WKH-SEC-04] SSRF Hardening: URL final en MCP tools + revalidación invokeUrl en compose — userinfo bypass fix + endpoint path restriction + compose invokeUrl revalidation | security | QUALITY | DONE | feat/124-wkh-sec-04-ssrf-hardening ([report.md](124-wkh-sec-04-ssrf-hardening/report.md)) |
| 125 | 2026-06-23 | [WKH-127] Orchestrate Billing: precio real + reembolso en fallo — débito del costo planificado real (no $1 placeholder) + credit-back cuando pipeline.success===false | bugfix/billing | QUALITY | DONE | fix/125-wkh-127-orchestrate-billing ([done-report.md](125-wkh-127-orchestrate-billing/done-report.md)) |
| 126 | 2026-06-24 | [WKH-129] Dest-cap refund completo — `refund_with_dest_policy`: revertir fila del ledger `a2a_key_dest_spend_ledger` en el refund per-step de /compose (residual de WKH-127/128) | bugfix/billing | QUALITY | DONE | fix/126-wkh-129-dest-cap-refund ([done-report.md](126-wkh-129-dest-cap-refund/done-report.md)) |
| 127 | 2026-06-24 | [WKH-130] Adaptive Input-Retry — orquestador/compose aprende el input correcto del error del agente y reintenta una vez (robustez para agentes externos sin input_schema) | feature/robustness | QUALITY | DONE | feat/127-wkh-130-adaptive-input-retry ([done-report.md](127-wkh-130-adaptive-input-retry/done-report.md)) |
| 128 | 2026-06-30 | [WKH-131] /orchestrate/plan + /orchestrate/execute — flujo consumer quote-antes-de-aprobar para Yarvis PWA | feature | QUALITY | DONE | feat/128-orchestrate-plan-execute ([done-report.md](128-orchestrate-plan-execute/done-report.md)) |
| 129 | 2026-07-01 | [WKH-132] Protocol fee de /orchestrate proporcional al costo REAL del pipeline (no al budget declarado) + consistencia del quote (`maxQuotedCostUsdc == totalCostUsdc + protocolFeeUsdc`) | bugfix/billing | QUALITY | DONE | fix/129-wkh-132-orchestrate-fee-on-cost ([done-report.md](129-wkh-132-orchestrate-fee-on-cost/done-report.md)) |
| 132 | 2026-07-01 | [WKH-135] Centralizar config LLM/modelo en un único punto env-driven | refactor | QUALITY | DONE | refactor/135-centralize-llm-config ([done-report.md](132-wkh-135-centralize-llm-config/done-report.md)) |
| 133 | 2026-07-03 | [WKH-132] *(okx-roadmap 2026-07-03; NO confundir con el WKH-132 histórico fila 129, otra feature DONE)* Transparencia de fee: publicar el fee en el quote de /orchestrate/plan + docs | docs/api | FAST+AR | DONE | feat/133-wkh-132-fee-transparency ([done-report.md](133-wkh-132-fee-transparency/done-report.md)) |
| 134 | 2026-07-03 | [WKH-133] Reputation write-back on-chain a ERC-8004 (cerrar el loop; hoy read-only). Forward-only, Base-only v1, feature-flag default OFF | feature/reputation | QUALITY | DONE | feat/134-wkh-133-reputation-writeback ([done-report.md](134-wkh-133-reputation-writeback/done-report.md)) |
| 135 | 2026-07-03 | [WKH-134] *(nuevo; NO confundir con WKH-134 si aparece histórico)* SDK + publish self-serve de 1 agente: endpoint en a2a (SDK-package en repo aparte), gratis con Agent Key | feature/dx | QUALITY | DONE | feat/135-wkh-134-agent-selfserve-publish ([done-report.md](135-wkh-134-agent-selfserve-publish/done-report.md)) |
| 136 | 2026-07-03 | [WKH-142] *(SEC follow-up de WKH-134)* Defensa en profundidad: guard de importe negativo en el money-path (RPC `increment_a2a_key_spend` + `compose.isInvalid` + `CHECK price_usdc≥0`) | security | QUALITY | BACKLOG (ver ejecución en fila 143) | — (Jira WKH-142) |
| 137 | 2026-07-03 | [WKH-135] *(okx-roadmap Wave 1)* Intents de pago `session` (metered, deposit+vouchers+settle único+refund residual) + `upto` (Buyer cap, Seller reporta uso, settle min(cap,uso)), naming APP-compatible (habilita bridge WKH-141) | feature/billing | QUALITY | DONE | feat/137-wkh-135-payment-intents-session-upto ([done-report.md](137-wkh-135-payment-intents-session-upto/done-report.md)) |
| 138 | 2026-07-03 | [WKH-136] *(okx-roadmap Wave 1, P1)* Splits atómicos al settlement (bps: plataforma/creador/referral) — generaliza el protocol fee único en splits configurables ruteados a N recipients. Config default (10000/0/0) byte-idéntico a hoy. Engine seam-compatible, testeado, listo. Decisión pendiente: activar creador/referral (requiere ampliar firma) vs v1 solo-plataforma. Money-path QUALITY completo; 2342 tests passed; migración en Postgres efímero verificada. | feature/billing | QUALITY | DONE | feat/138-wkh-136-atomic-splits-bps ([done-report.md](138-wkh-136-atomic-splits-bps/done-report.md)) |
| 139 | 2026-07-04 | [WKH-137] *(okx-roadmap Wave 2, P1)* Pagos IM-native + QR sin hostear HTTP — v1 acotado por el Analyst a SOLO el lado consumidor: primitiva "invocation link" (mint+redeem opaco, single-use, price-capped) reusando `/orchestrate/plan`+`/execute` (WKH-131) y session keys (WKH-121). El lado "seller sin HTTP" y el bot Telegram/WhatsApp quedan explícitamente fuera, marcados `[NEEDS CLARIFICATION]` para el humano. | feature/billing | QUALITY | DONE | feat/139-wkh-137-invocation-links ([done-report.md](139-wkh-137-im-qr-payments/done-report.md)) |
| 140 | 2026-07-04 | [WKH-138] *(okx-roadmap Wave 2, P1)* Embedded agent-wallet + gasless beyond Kite — HU original (L, security-crítica) recortada a un **v1 acotado**: gasless en Avalanche/Base reutilizando el patrón de Kite (`GaslessAdapter`, hoy stubs en `avalanche/gasless.ts` y `base/gasless.ts`), SIN construir la wallet embebida/custodia/EIP-7702. Wallet embebida diferida a HU separada (sugerida WKH-138b), bloqueada hasta ratificación humana del modelo de custodia — ver Missing Inputs. | feature/billing | QUALITY | DONE | feat/140-wkh-138-gasless-avalanche-base ([done-report.md](140-wkh-138-embedded-wallet-gasless/done-report.md)) |
| 141 | 2026-07-04 | [WKH-139 v1, SUPERADA] *(okx-roadmap Wave 3, P2)* Dispute resolution real: escrow + dispute window (release/dispute/timeout) con resolver HUMANO. F0 encontró que el escrow "staged" de WKH-126 (contrato Solidity, NNN 121/117/122) NO es reutilizable acá — es un primitivo distinto (funding agregado por Agent Key, no hold por-tarea, sin concepto de resolver). Bloqueada para pasar a F2 hasta ratificación humana de 4 `[NEEDS CLARIFICATION]` (quién puede disputar, identidad/proceso del resolver, duración de la ventana, y aceptar el enfoque off-chain vs esperar deploy+auditoría del contrato). **Reemplazada por la fila 145 (WKH-139 v2, agente-árbitro autónomo)** — esta branch queda obsoleta. | feature/billing | QUALITY | DEFERRED (superada por 145) | feat/141-wkh-139-dispute-window |
| 142 | 2026-07-04 | [WKH-141] *(okx-roadmap Wave 3, Strategic)* Bridge APP-compatible (Agent Payments Protocol de OKX) — HU original (L, "Broker/bridge completo con XMTP") recortada a un **v1 acotado**: declaración aditiva de capacidades APP-compatibles en el Agent Card (outbound) + adaptador interno de mapeo puro (charge/session/upto → vocabulario APP), feature-flag default OFF, SIN aceptar/parsear payloads reales de terceros (bloqueado por falta de spec confirmado). El bridge inbound real, XMTP y MPP real quedan `[NEEDS CLARIFICATION]` — bloqueante: acceso al spec/wire-format exacto de APP. | feature/interop | QUALITY | DONE | feat/142-wkh-141-app-bridge ([done-report.md](142-wkh-141-app-bridge/done-report.md)) |
| 143 | 2026-07-04 | [WKH-142] *(SEC follow-up de WKH-134)* Defensa en profundidad: guard de importe negativo en el money-path — choke-point único en `increment_a2a_key_spend` (RAISE EXCEPTION `p_amount_usd < 0`, hereda a los 4 RPCs de débito vía `PERFORM`) + `compose.isInvalid` rechaza `priceUsdc < 0` + `CHECK (price_usdc >= 0)` en `a2a_agents` (con clamp de datos existentes en la misma migración) | security | QUALITY | DONE | fix/143-wkh-142-negative-amount-guard ([done-report.md](143-wkh-142-negative-amount-guard/done-report.md)) |
| 144 | 2026-07-04 | [WKH-143] Activar splits de creador/referral — cablear el seam | feature/billing | QUALITY | DONE | feat/144-wkh-143-activate-creator-referral-splits ([done-report.md](144-wkh-143-activate-creator-referral-splits/done-report.md)) |
| 145 | 2026-07-04 | [WKH-139 v2] Agente-árbitro autónomo de disputas (redefinición de la fila 141) — rules-first, ancla decisiones en la proof-chain de recibos (WKH-124) + el intent `session` de WKH-135 (NO el escrow on-chain WKH-126, que es un fondo agregado sin concepto de hold por-tarea/resolver). LLM SOLO para casos genuinamente ambiguos, nunca como ejecutor de fondos. Testnet únicamente. Bloqueada para pasar a F2 hasta ratificación humana de 3 `[NEEDS CLARIFICATION]` (modelo de autoridad/tope de monto, evidencia admisible, path de apelación). | feature/billing | QUALITY | DONE | feat/145-wkh-139-agent-arbiter ([work-item.md](145-wkh-139-agent-arbiter/work-item.md) · [done-report.md](145-wkh-139-agent-arbiter/done-report.md)) |
| 146 | 2026-07-04 | [WKH-090 / HU-090] Segundo rail de pago — adapter Tempo/MPP (Machine Payments Protocol, Stripe + Paradigm), sobre el testnet de Tempo (stablecoin `pathUSD`, faucet desde dic-2025 — blocker de F0 levantado por research web del orquestador). MPP-EVM tiene "x402 exact compatibility": el adapter reusa la maquinaria x402/EIP-3009 existente (`SettleRequest`/`X402Proof`), sin contrato de tipos paralelo. v1 detrás de feature-flag default OFF, mismo patrón que WKH-141 (APP bridge) y WKH-133 (reputation write-back). Política de selección de rail (HU-091) marcada `[NEEDS CLARIFICATION]` — no bloquea: v1 usa el mecanismo explícito ya existente (header > manifest > default), sin auto-routing. | feature/billing | QUALITY | DONE | feat/146-hu-090-tempo-mpp-rail ([work-item.md](146-hu-090-tempo-mpp-rail/work-item.md) · [done-report.md](146-hu-090-tempo-mpp-rail/done-report.md)) |
| 147 | 2026-07-04 | [WKH-143b] Write-path de creator/referral — cerrar el seam dormido de WKH-143: capturar `payoutWallet`/`referrerRef` en `POST`/`PATCH /agents` (WKH-134) para que `payout_wallet` deje de ser SIEMPRE `NULL` en self-published. Activa pagos REALES de creator (`SPLIT_BPS_CREATOR>0` + wallet válida). `referrer_ref` queda persistido pero funcionalmente inerte — el read-side (`resolveAgentSplitContext`) sigue hardcodeando `referral: null` (DT-6 de WKH-143); activar referral de verdad requiere una HU separada (sugerida WKH-143c) que resuelva la semántica de `referrer_ref` — `[NEEDS CLARIFICATION]` heredado, no bloqueante. | feature/billing | QUALITY | DONE | feat/147-wkh-143b-split-writepath ([work-item.md](147-wkh-143b-split-writepath/work-item.md) · [done-report.md](147-wkh-143b-split-writepath/done-report.md)) |
| 148 | 2026-07-04 | [WKH-143c] Activar referral real en los splits (Opción B) — cerrar DT-6 de WKH-143: `resolveAgentSplitContext` resuelve `referrer_ref` = slug de otro agente self-published a su `payout_wallet` vía `getSplitContextRow`. Cero lookup nuevo, dedup case-insensitive, gate fino `referralActive()`. Con default `10000/0/0` byte-idéntico. **Roadmap de splits 100% cerrado** (plataforma + creator + referral). | feature/billing | QUALITY | DONE | feat/148-wkh-143c-activate-referral ([done-report.md](148-wkh-143c-activate-referral/done-report.md)) |
| 149 | 2026-07-05 | [WKH-71] Operator wallet — auto-alert below threshold + funding runbook. Ticket original (mayo 2026, USDC mainnet) actualizado a la realidad testnet/multi-chain: monitor nuevo de balance de **gas nativo** multi-wallet (settle wallet del facilitator `0x9c0638...`, operador del gateway `0xf432...`) × multi-chain (Fuji, Kite testnet, Base Sepolia), reusando `alerts.mjs`/cron existentes de WKH-66/75/89/90/91; AC-3 agrega error explícito "operator funding low" en `fee-charge.ts`/`gasless-signer.ts` cuando el operador de `wasiai-a2a` no tiene gas. Cambios en `wasiai-facilitator` (dueño real del wallet que se secó hoy) quedan fuera — repo distinto, no en este workspace. | feature/ops | FAST+AR | DONE | feat/149-wkh-71-operator-wallet-alert ([report.md](149-wkh-71-operator-wallet-alert/report.md)) |
| 150 | 2026-07-05 | [WKH-77] On-Call Alerting — Health Monitor + Escalation Runbook. Ticket original (mayo 2026) pedía webhook por-servicio; reformulado a un **monitor central de health** (HTTP down/5xx/unreachable/timeout + degradado-lógico vía JSON-path configurable) sobre 4 servicios propuestos (wasiai-a2a gateway P0, wasiai-facilitator P0, wasiai-x402-mcp P1, app.wasiai.io P1), reusando `alerts.mjs`/cron de WKH-66/71/75/89/90/91 — mismo patrón que WKH-71 (fila 149) pero concern distinto (uptime/health, no balance). Escalation runbook P0/P1/P2 + test-alert end-to-end. NO duplica WKH-74 (synthetic/functional probes). | feature/ops | FAST+AR | DONE | feat/150-wkh-77-oncall-alerting ([work-item.md](150-wkh-77-oncall-alerting/work-item.md) · [done-report.md](150-wkh-77-oncall-alerting/done-report.md)) |
| 151 | 2026-07-06 | [WKH-74] Synthetic monitoring del payment path — capas A (expect-402 sin auth, $0), C (doc-only: toggles error-rate Vercel/Railway) y D (real-tx gated por deploy SHA, ~$0.02/mes). Capa B (balance operator wallet) YA HECHA por WKH-71 (fila 149); canal on-call/runbook YA HECHO por WKH-77 (fila 150) y reusado. Cierra el hueco visto en el incidente 2026-07-05 (settle-timeout con servicio 200-ok que WKH-77 no cazó). | feature/ops | FAST+AR | DONE | feat/151-wkh-74-synthetic-monitoring ([work-item.md](151-wkh-74-synthetic-monitoring/work-item.md) · [done-report.md](151-wkh-74-synthetic-monitoring/done-report.md)) |
| 152 | 2026-07-06 | [WKH-114] AC verificables + verificación de completitud por step en /orchestrate y /compose — adjunta acceptance criteria explícitos a cada step en plan-time (LLM planner o caller-provided) y evalúa el output del agente contra ellos en execute-time (rules-first, LLM-judge solo si ambiguo, mismo patrón que el árbitro WKH-139 v2), exponiendo veredicto pass/fail/unverified por step de forma additive-only. Versión a-nivel-de-código del gap visto en el incidente 2026-07-05 (Chaski 200-ok sin trabajo real); WKH-71/74/77 lo cazan desde afuera, esta HU verifica desde dentro. Billing/débito explícitamente OUT (CD-1). | feature | QUALITY | DONE | feat/152-wkh-114-verifiable-step-ac ([report.md](152-wkh-114-verifiable-step-ac/report.md)) |
| 153 | 2026-07-06 | [WKH-93] Opt-in refunds para fallos del agente en /compose (fee-on-attempt mitigation) — flag `refundOnFailure` por step: si `true` y el agente downstream falla con 5xx/error de red (NO 4xx), acredita de vuelta el monto debitado + header `x-refund-applied` + log estructurado + audit row en tabla nueva `a2a_compose_refunds`. **F0 encontró un hallazgo crítico**: el modelo "fee-on-attempt puro" que el ticket describe ya NO existe — WKH-128/129/130 (2026-06-24→06-30) YA reembolsan automática e incondicionalmente CUALQUIER fallo de step (5xx o 4xx). Para que AC-3/AC-4 del ticket tengan sentido, esta HU debe REVERTIR ese auto-refund incondicional detrás del nuevo flag opt-in — cambio de comportamiento de billing en producción. Bloqueada para pasar a F2 hasta ratificación humana explícita de DT-1 (ver Missing Inputs #1 del work-item). | feature/billing | QUALITY | ❌ OBSOLETA — cerrada en el gate HU_APPROVED (2026-07-06). El auto-refund de CUALQUIER fallo ya existe (WKH-128/129/130) + audit en `a2a_refund_outbox`; implementar el opt-in del ticket sería una regresión. Decisión del owner: no revertir. Jira WKH-93 Finalizada. | — (no F2, cerrada en gate) ([work-item.md](153-wkh-93-compose-refund-on-failure/work-item.md)) |
| 154 | 2026-07-06 | [WKH-144] [SEC] x402/settle re-verify fail-CLOSED en mainnet — el re-verify on-chain del settle (`verifySettledTx`/`verifyDefaultChainSettle` en `settle-verifier.ts`) trata `RPC_UNAVAILABLE` como fail-OPEN sin importar la chain; en mainnet eso anularía la única verificación independiente sobre el facilitator. Fix: gate mainnet-only (chainKey termina en `-mainnet`) implementado en UN choke-point (`verifySettledTx`) — `fee-split.ts`/`compose.ts` heredan el fix sin cambios de código (ambos consumen `verifyDefaultChainSettle`). Testnet 100% sin cambios; contradicción definitiva sigue fail-closed sin cambios. Nonce anti-replay x402 (`x402-nonce.ts`) marcado OUT (subsistema/riesgo distinto — dedup DB sobre un nonce ya single-use on-chain). | security | FAST+AR | DONE | fix/154-wkh-144-settle-reverify-fail-closed ([work-item.md](154-wkh-144-settle-reverify-fail-closed/work-item.md) · [done-report.md](154-wkh-144-settle-reverify-fail-closed/done-report.md)) |
| 155 | 2026-07-06 | [WKH-115] Inbound adapter de tareas/bounties externos → /orchestrate (demanda push/pull) — POST /inbound/:source/tasks, HMAC-SHA256, lifecycle `ingested→routed→settled|rejected|failed`, tabla `a2a_inbound_tasks` con owner_ref+RLS, adapter genérico webhook HTTP, presupuesto capado, rechazo de escrow, SSRF, ruteo in-process. Pendiente: migración bdwv + env vars por fuente + redeploy. | feature | QUALITY | DONE | feat/155-wkh-115-inbound-adapter ([work-item.md](155-wkh-115-inbound-adapter/work-item.md) · [done-report.md](155-wkh-115-inbound-adapter/done-report.md)) |
| 156 | 2026-07-06 | [WKH-150] [WKH-144b] Polish del fail-closed mainnet — 2 refinamientos no-bloqueantes dejados por AR/CR de WKH-144 (fila 154): (1) el log `"trusting facilitator"` en `fee-split.ts:455-459`/`compose.ts:966-969` se emite también en el path fail-closed mainnet (`ok:false, warn:true`), contradiciendo el bloqueo que sigue — se corrige SOLO el texto, cero cambio de lógica; (2) guard del invariante `-mainnet` del que depende `isMainnetChainKey()` — JSDoc en `ChainKey` (`types.ts`) + test no-tautológico en `settle-verifier.test.ts` que cruza `SUPPORTED_CHAINS` contra el booleano `.testnet` de los objetos viem `Chain` (fuente independiente del sufijo del propio `ChainKey`). | security | FAST+AR | DONE | fix/156-wkh-150-failclosed-polish ([done-report.md](156-wkh-150-failclosed-polish/done-report.md)) |
| 157 | 2026-07-06 | [WKH-151] Orchestrate discovery broaden-retry — fix del plan vacío intermitente en /orchestrate ($0, `planStatus:'no_agents'`) cuando `preferCapabilities` del caller (Chaski) no matchea el nombre exacto de las capabilities publicadas por los agentes de remesa en bdwv (`remit.corridor-discovery`/`cashout-match`/`kyc-check`). Fix: UN retry de `discoveryService.discover` sin el filtro de capabilities (mismo `maxPrice`/`limit`) cuando el primer intento da 0 agentes; si el retry también da 0, `no_agents` genuino sin cambios. Riesgo money-path analizado: el guard `allStepsAreDemos` (`orchestrate.ts:713-726`) y el chequeo de budget por step son post-plan e independientes de cómo se llamó a discovery — un candidate-set ampliado no puede saltarse el bloqueo de planes 100% echo/demo ni exceder budget. Scope acotado a `orchestrate.ts` (NO toca `discovery.ts` ni el planner). F3/AR/CR/F4 completados; tsc 0, biome 0, 2772 tests. MNR-1 extraído como candidato a follow-up. | bugfix | FAST+AR | DONE | fix/157-wkh-151-orchestrate-discovery-broaden ([work-item.md](157-wkh-151-orchestrate-discovery-broaden/work-item.md) · [done-report.md](157-wkh-151-orchestrate-discovery-broaden/done-report.md)) |
| 158 | 2026-07-06 | [WKH-153] Planner LLM ignora `input_schema` por-agente — el ejemplo de output `"input": { "query": "specific input" }` en el prompt de `llmPlan` (`orchestrate.ts:207`) sesga al LLM a copiar ese shape genérico para TODOS los agentes seleccionados, incluso cuando publican `input_schema` estructurado. Sub-caso `agentCount=3` de "Chaski \$0": el orquestador elige bien los 3 agentes de remesa (`agentshop-corridor-discoverer`/`cashout-matcher`/`kyc-validator`) pero los invoca con `{query}` → agentes devuelven HTTP 400 (`amountUSD and receiverCountry required`, etc.) → step-0 falla → fee \$0. Fix: reforzar el ejemplo del prompt para que derive el input del `input_schema`/`example_input` de CADA agente, sin fijar un shape único (preserva agentes `{query}` genuinos). Validación: test determinístico sobre el string del `userPrompt` (mock de `client.messages.create`) — el smoke con LLM real queda fuera de CI (no determinístico). | bugfix | FAST+AR | DONE | fix/158-wkh-153-planner-input-schema ([done-report.md](158-wkh-153-planner-input-schema/done-report.md)) |
| 159 | 2026-07-07 | [WKH-157] Discover free-text (`q`) recall fix — auditoría e2e 2026-07-07 (E2E-1, severidad BAJA): `GET/POST /discover` con búsqueda libre devolvía 0/pocos resultados (`q=sentiment`→0, `q="send money"`→0) pese a existir agentes relevantes, porque `discoveryService.discover()` reenvía `query.query` upstream (`discovery.ts:450-451`) y el filtro local (`:319-326`) solo puede estrechar lo que el upstream ya devolvió, nunca recuperarlo. Fix propuesto: broaden-retry (espejo de WKH-151, fila 157) — si el resultado post-filtro es 0 con `query.query` seteado, reintentar UNA vez sin reenviar `q` upstream y aplicar el filtro local sobre ese set. Money-path safe confirmado en F0: `orchestrate.ts:520-524` (el planner) solo pasa `capabilities`/`maxPrice`/`limit`, nunca `query.query` — el retry de esta HU no se dispara en ese path. | bugfix | FAST+AR | DONE (mergeada a `main`, merge `da1c45e`, rama sin commits fuera de `main`). Corregido 2026-07-29: decía "in progress". | fix/159-wkh-157-discover-freetext-filter ([work-item.md](159-wkh-157-discover-freetext-filter/work-item.md)) |
| 160 | 2026-07-07 | [WKH-152] Planner LLM sin guard de relevancia — un agente real pero IRRELEVANTE al goal se ejecuta y se cobra porque `fallbackNoRelevance`/`allStepsAreDemos` (`orchestrate.ts:793-861`) solo cubren el fallback greedy y el caso 100%-demos; el path del LLM (`:650-719`) construye `steps` sin validar relevancia (por diseño explícito, comentario `:791-792`). Un plan MIXTO (real-relevante + real-irrelevante) pasa a `ready` y cobra el irrelevante. Exposición ampliada por WKH-151 (fila 157, pool de candidatos más grande). Fix propuesto: guard post-plan determinístico que reusa `tokenizeForRelevance` (`:342-350`) per-step sobre el plan LLM (filtra el/los step(s) irrelevante(s) antes del débito; si queda vacío → `no_relevant_agent` sin cobrar, espejo del guard existente); recalcular step-0/`plannedCostUsd` si el step filtrado era el primero. QUALITY por el riesgo de recomputar el billing del step-0 (WKH-127/132) sin introducir un mismatch billing↔ejecución. | feature/billing | QUALITY | DONE (mergeada a `main`, merge `d5d6ebd`, rama sin commits fuera de `main`). Corregido 2026-07-29: decía "in progress", y las filas 162/164/165 ya la daban por mergeada (el backstop de WKH-152 fue después neutralizado por WKH-166, fila 165). | fix/160-wkh-152-llm-relevance-guard ([work-item.md](160-wkh-152-llm-planner-relevance-guard/work-item.md)) |
| 161 | 2026-07-07 | [WKH-158] LLM planner retry-on-transient-failure (RE-SCOPE 2026-07-07: reemplaza el fix original de este WKH) — repro en vivo: goal ES `"cotiza el precio del dolar"` cayó a `greedyPlan` porque `llmPlan()` falló transitoriamente (timeout/red/rate-limit/JSON inválido, `orchestrate.ts:152-283`) y en un retry manual (nueva request) el LLM funcionó y eligió `wasi-chainlink-price` (juicio semántico real). El humano descartó tocar el greedy guard (`fallbackNoRelevance`, ciego al goal, conservar ahí seguiría siendo money-unsafe — análisis F0 original preservado en el work-item) y en su lugar ataca la raíz: reintentar `llmPlan()` EXACTAMENTE 1 vez ante un fallo transitorio/retryable ANTES de caer a `greedyPlan()` (punto exacto: `orchestrate.ts:656-669` try/catch de `CircuitOpenError` + `:741-748` el `else` "AC7: LLM failed — fallback to greedy"). Si el retry produce plan, se usa (sujeto al backstop mixed-plan-only de WKH-152, sin cambios); si el retry también falla, cae a greedy — comportamiento actual, sin cambios. Bounded (máx 1 retry), money-safe (mismo `llmPlan`/mismo prompt, sin path de cobro nuevo). Greedy, `fallbackNoRelevance`, `allStepsAreDemos` y el backstop de WKH-152 quedan byte-idénticos (Scope OUT). | bugfix | QUALITY | DONE (mergeada a `main`, merge `40c9c8d`, rama sin commits fuera de `main`). Corregido 2026-07-29: decía "in progress". | fix/161-wkh-158-llm-planner-retry ([work-item.md](161-wkh-158-greedy-relevance-guard/work-item.md)) |
| 162 | 2026-07-07 | [WKH-159] Greedy fallback multilingual false-negative (money-safe hardening) — el guard `fallbackNoRelevance` (`orchestrate.ts:851-867`, no `:824-840` — corrido por el backstop de WKH-152 ya mergeado) hard-rechaza (`no_relevant_agent`, sin cobrar) cuando un goal en español no comparte tokens con el corpus en inglés de los agentes, aunque exista un agente relevante — se llega a esta zona SOLO tras 2× fallo del LLM (retry WKH-158) o fallo permanente `!client`/`CircuitOpenError`. F0 confirma: hoy es 100% conservador (rechaza de más, NUNCA cobra de más) porque el hard-reject corre ANTES de cualquier débito (`orchestrate.ts:908-958`, previo a `executeApprovedPlan`). Confirmado también que `/orchestrate/plan` (quote-only) NUNCA debita y `/orchestrate` atómico SÍ debita el step-0 inmediatamente tras `planStatus:'ready'` (`orchestrate.ts:1181-1189`) sin paso de confirmación — por eso relajar el guard es inocuo en `/plan` pero introduce riesgo directo de over-charge en el atómico. Opción (a) conservar top-1 DESCARTADA (mismo razonamiento ya usado por el humano para rechazarla en WKH-158, fila 161). Opción (c) distinguir vocabulario-mismatch de irrelevancia real sin juez semántico: NO factible de forma confiable. Recomendación del Analyst: fix cosmético (enriquecer el `reasoning` del `no_relevant_agent` para señalar posible mismatch de idioma, guiando al caller hacia `/plan`+`/execute` manual con `consideredAgents`, ya disponible hoy) — cero riesgo, cero cambio de `planStatus`/billing. Alternativa funcional (relajar el guard SOLO en `/orchestrate/plan`, dejando el atómico intacto) marcada `[NEEDS CLARIFICATION]` — Chaski (WKH-131) es un caller automatizado que podría auto-aprobar `/execute` sin revisar, reintroduciendo el riesgo de la opción (a) descartada por la puerta de atrás. Greedy, `llmPlan()`, el retry de WKH-158 y el backstop MIXED-PLAN-ONLY de WKH-152 quedan Scope OUT, byte-idénticos. | bugfix | QUALITY | DONE (mergeada a `main`, merge `634fb4b`, rama sin commits fuera de `main`). Corregido 2026-07-29: decía "in progress". | fix/162-wkh-159-greedy-multilingual-guard ([work-item.md](162-wkh-159-greedy-multilingual-guard/work-item.md)) |
| 163 | 2026-07-07 | [WKH-160] Relevancia semántica por embeddings (reemplazo/aumento del matching léxico) — la cadena WKH-152→158→159 destapó que el matching de relevancia hoy es 100% léxico (`tokenizeForRelevance`/`textOverlapsGoal`, overlap de tokens ≥3 chars), incompatible con un producto multilingüe (goal ES vs agente EN = 0 overlap → false-negative). F0 confirma 3 superficies: (a) greedy `fallbackNoRelevance` (`orchestrate.ts:834-867`, backstop raro, post-fallo-LLM), (b) backstop MIXED-PLAN-ONLY sobre el plan del LLM (`orchestrate.ts:869-906`, corre en el HAPPY PATH, mayor impacto), (c) `/discover` free-text (`discovery.ts:358-374`, substring simple, confirmado fuera del money-path). Sin pgvector/embeddings hoy (grep exhaustivo: 0 resultados; migración `a2a_agents` sin columna vector). 5 `[NEEDS CLARIFICATION]` escaladas al humano con recomendación (proveedor Voyage AI vs OpenAI vs bge-m3 self-host; precompute+cache pgvector vs online; umbral de cosine similarity vía calibración; money-safety en greedy — NO relajar sin ratificación, mismo patrón WKH-158/159; prioridad de superficie (c)). Recomendación de fase: acotar ESTA HU a Fase 1 (infra + fallback léxico obligatorio + shadow-mode/telemetría, CERO cambio de `planStatus`/billing); Fase 2 (enforcement real del umbral) como HU de seguimiento post-calibración. Conflicto de merge potencial con filas 161/162 (mismo bloque de `orchestrate.ts`, ambas `in progress`) — recomendado esperar su merge antes de F2. | feature/billing | QUALITY | F1 (sólo `work-item.md`) · PARKEADA, sin implementación. Corregido 2026-07-29: decía "in progress" y no lo está. La rama `feat/163-wkh-160-semantic-embeddings-relevance` **no existe ni local ni remota** (verificado con `git branch -a` + `git ls-remote --heads origin`), el directorio tiene sólo el work-item, y `main` sigue diciendo `// embeddings vuelve con WKH-160, re-enganchando en el mismo hook point.` (`src/services/orchestrate.ts:359`) ⟹ el smart-drop semántico sigue pendiente. | feat/163-wkh-160-semantic-embeddings-relevance ([work-item.md](163-wkh-160-semantic-embeddings-relevance/work-item.md)) |
| 164 | 2026-07-09 | [WKH-163] La remesa insignia se trunca por el backstop léxico de WKH-152 — remesa insignia "Send $400 to my mom in Peru" restaurada. Fix: exclusión de tokens puramente numéricos (`/^\d+$/`) del set de relevancia LLM (goal-side, scoped) + terminal delivery guard (rescata el step terminal si-y-solo-si `llmDropped≥2`). Result: 8/8 ACs PASS, cero regressions (9 T-152-* intactas, 35 money-path tests idem), débito==ejecución airtight, 1 MNR documentado (trade-off terminal-guard). AR APROBADO (0 BLQ, 1 MNR), CR APROBADO (2 NITs foldeados), F4 QA APROBADO (8/8 ACs + zero drift, 2805 tests verdes). Merged: 17f0ae1. | feature/billing | QUALITY | DONE | main (17f0ae1) ([done-report.md](164-wkh-163-cashout-matcher-relevance-fix/done-report.md)) |
| 165 | 2026-07-09 | [WKH-166] Plan del LLM autoritativo — neutralizar el drop léxico del backstop (WKH-152/163) sobre el plan del LLM. Repro en vivo: `send money to Peru via the best option` dropea `agentshop-kyc-validator` porque "best" del goal matchea léxicamente la descripción del corridor ("Found the best route") → backstop ve corridor+cashout relevantes y a KYC off-topic. El LLM planeó los 3 agentes bien; el backstop POST-LLM rompe el plan. Decisión del founder: el plan del LLM es AUTORITATIVO — se neutraliza `llmFilterApplies`/`relevantSteps`/terminal-guard/`applyDrop` (`orchestrate.ts:877-935`, `:1007-1024`) sobre el plan del LLM; el smart-drop semántico vuelve con WKH-160 (fila 163). 3 tests reescritos a conducta nueva; 9+ preservados verdes. AR APROBADO (0 BLQ, 1 nota: expandir over-charge, mitigación=WKH-160). CR APROBADO (1 NIT foldeado). F4 QA 8/8 ACs PASS sin drift. tsc 0, biome 0, 2805 tests. Merged: 8493386. | feature/billing | QUALITY | DONE | fix/165-wkh-166-llm-plan-authoritative ([done-report.md](165-wkh-166-llm-plan-authoritative/done-report.md)) |
| 166 | 2026-07-09 | [WKH-167] `a2a_protocol_fees.fee_total_usdc` — columna aditiva del fee TOTAL (resolve ambigüedad auditoría) + backfill de 158 filas (bdwv/caldz) con reconciliación verificada (0-mismatches) | feature/billing | FAST+AR | DONE | main (2b13d04) ([done-report.md](166-wkh-167-fee-total-usdc/done-report.md)) |
| 167 | 2026-07-09 | [WKH-171] Publicar `remit-corridor-fx` como agente standalone del marketplace A2A (FX/quote-as-a-service) — etapa 1: Next.js endpoint HTTP nuevo en `wasiai-remittance-agents` (fork cobraya sin EIP-712 receipt) + scaffolding Next 14 + 6 tests (5 originales + 1 cobertura 502). Modo pago ratificado = a2a-key prepago (cero código nuevo en `wasiai-a2a`). F3 DONE: tsc 0, build 0 errores, 39 tests verdes, CR APPROVED (3 MENORes → fix-pack), QA APROBADO (5 ACs PASS, 3 PENDING-DEPLOY por diseño, 0 drift). Pendiente: W3 deploy Vercel nuevo, W4 registro `POST /agents` (payload documentado), W5 smoke E2E (AC-1/2/5 con evidencia DB). Runbook completo en done-report. | feature/marketplace | QUALITY | DONE (código) · PENDING-DEPLOY (W3/W4/W5) | feat/167-wkh-171-remit-corridor-fx ([done-report.md](167-wkh-171-remit-corridor-fx/done-report.md)) |
| 168 | 2026-07-10 | [WKH-173] Self-publish (`POST/PATCH/DELETE/GET /agents`) cobra fee placeholder ($1) pese a documentarse "gratis" (`agents.ts:19-20`) — usa `requirePaymentOrA2AKey` sin inyectar costo real → cae al `PLACEHOLDER_FEE_USD` y debita de verdad. Fix de fondo (ratificado por el humano, "sin atajos"): middleware nuevo `requireA2AKey()` que desacopla auth (resolver `owner_ref`) de pago — sin débito, sin chain-resolution, sin fallback a x402 (cierra también un hallazgo nuevo: un caller x402-anónimo que SÍ paga sería debitado on-chain y luego igual rechazado con `A2A_KEY_REQUIRED`). `registries.ts` tiene el MISMO patrón de código pero SIN promesa documentada de "gratis" — queda explícitamente fuera de esta HU (Missing Inputs #3, decisión de producto separada). Fix 2 (default-chain UX en ops pagas compose/orchestrate) recomendado como ticket nuevo, Scope OUT. Relevante para WKH-171 (fila 167, W4 pendiente registrar `POST /agents` — secuenciar deploy de este fix antes si es posible). | bugfix/billing | QUALITY | DONE (código) · PENDING-DEPLOY (`!` Railway) | fix/168-wkh-173-publish-free-auth ([done-report.md](168-wkh-173-publish-free-auth-fix/done-report.md)) |
| 169 | 2026-07-10 | [WKH-170] Publicar `remit-kyc-validator` como agente standalone del marketplace A2A (KYC-as-a-service) — etapa 1: workflow Free KYC ($0) / fallback simulado (Didit real + AML queda en etapa 2, Fase A). Mismo patrón que WKH-171 (fila 167): endpoint HTTP nuevo (fork de `remit-corridor-fx/invoke/route.ts`) envolviendo `runKycValidator` (`kyc-validator.ts`, lógica y fail-safe YA implementados/testeados — PII (`legalId`/`travelRuleData`) NUNCA sale del core, verificado por test 2/5/7 que inyectan DNI real). Modo de pago (a2a-key prepago) y stack (Next.js, mismo deploy Vercel de WKH-171) heredados sin reabrir. Eje crítico: garantía dura NO-PII en 200/400/502 (CD-6, tests defensivos). F3 DONE: tsc 0, build 0 errores, 47 tests verdes (8 nuevos + 39 previos). AR APROBADO (2 MENOR: 1 pre-existente fuera de scope → WKH-177, 1 test agregado post-AR). CR APPROVED (0 BLQ, 0 MENOR). F4 QA APROBADO (5/8 ACs PASS código, 3 PENDING-DEPLOY por diseño: registro+smoke gateados `!`). Pendiente: W3 redeploy Vercel (mismo proyecto), W4 registro `POST /agents` (payload documentado), W5 smoke E2E. Runbook completo en done-report. | feature/marketplace | QUALITY | DONE (código) · PENDING-DEPLOY (W3/W4/W5) | feat/169-wkh-170-remit-kyc-validator ([done-report.md](169-wkh-170-remit-kyc-validator/done-report.md)) |
| 170 | 2026-07-10 | [WKH-172] Publicar `remit-cashout-payout` como agente standalone del marketplace A2A (payout/disbursement-as-a-service) — etapa 1: payout MOCK (`FallbackPayoutProvider`, NO mueve plata real) para validar el riel `discover→pagar→invocar→fee-split` con el hard-gate de KYC + idempotencia + reembolso. Hallazgo F0 resuelto: opt-in `PAYOUT_ALLOW_MOCK` acotado (proof-of-impossibility: NO puede habilitar real). Endpoint HTTP + tests + flag + README. F3 DONE: tsc 0, vitest 59/59 PASS (17 nuevos). AR APROBADO (0 BLQ, proof-of-impossibility del flag). CR APPROVED (0 BLQ). F4 QA APROBADO (6/9 ACs PASS código, 3 PENDING-DEPLOY `!`). Pendiente: W3 redeploy Vercel (`PAYOUT_ALLOW_MOCK=true`), W4 registro `POST /agents`, W5 smoke E2E (AC-1/2 discovery, AC-7 refund, AC-9 fee-split). | feature/marketplace | QUALITY | DONE (código) · PENDING-DEPLOY (!) | feat/170-wkh-172-remit-cashout-payout ([done-report.md](170-wkh-172-remit-cashout-payout/done-report.md)) |
| 171 | 2026-07-12 | [WKH-189] Panel + endpoint de revisión/override de `arb_hold` (agente-árbitro autónomo, WKH-139 v2 fila 145) — cierra el punto de disputa al 100%. Ensancha `close_payment_intent_for_arbitration` (predicado `'disputed'` → `IN ('disputed','arb_hold')`, patrón "Option B" idéntico a WKH-139 v2) sin tocar el dinero-path de settle/refund. Migración additive: CHECK `admin_override`, +3 columnas auditoría nullable. Backend: `GET/POST /dashboard/api/arbitrations` admin-gated reusa seam existente. Frontend: panel nuevo en `dashboard.html`. Bloque B (ops, fuera de scope): aplicar migración `caldz` + flip `ARBITER_ENABLED=true` Railway. F3/AR/CR/F4 DONE: 0 BLQ, 2 MENORs cerrados fix-pack (botones anti-doble-submit, validación `splitPct` rango). 8/8 ACs PASS, 2851 tests verdes, tsc 0, biome 0. Runbook activación en done-report. | feature/billing | QUALITY | DONE (código) · PENDING-DEPLOY | feat/171-wkh-189-arb-hold-override ([work-item.md](171-wkh-189-arb-hold-override/work-item.md) · [done-report.md](171-wkh-189-arb-hold-override/done-report.md)) |
| 172 | 2026-07-13 | [WKH-191] EPIC — Settlement genuinamente non-custodial vía `WasiAIEscrow`. Decompuesto en 8 HUs (2 waves): Wave 0 = flujo normal session/upto (191a-e, dos-saltos buyer→operador→seller, SIN cambios Solidity) DONE; Wave 1 = árbitro (191f-h, upgrade UUPS). PROGRESO: 191a/191b/191c DONE (código) PENDING-DEPLOY; 191d (activación + WKH-192 decimals-aware) READY; 191f DONE (código) UPGRADE-PENDING (gated multisig+timelock); 191g DONE (código) ACTIVATION-PENDING (gated 191h+consent-capture). | epic/billing/security | QUALITY | DONE (Wave 0 código completo, activación pendiente; Wave 1 código 191f+191g DONE, upgrade+wire gated en 191h) | — (branch por sub-HU, ver work-item.md) ([work-item.md](172-wkh-191-escrow-noncustodial-settlement/work-item.md)) |
| 173 | 2026-07-13 | [WKH-191a] Primera HU de la Wave 0 del epic WKH-191 (fila 172) — captura + persistencia + validación (SIN side-effects on-chain) de la firma EIP-712 `DebitAuthorization` del depositante en el flujo normal (session/upto). Nuance de diseño resuelta: firmar en CLOSE/SETTLE (no en OPEN) — el monto final (`min(Σvouchers,deposit)` para session / `min(cap,reportedUsage)` para upto) recién es conocido/derivable por el cliente en ese momento; el contrato exige el monto EXACTO firmado y no soporta semántica "hasta X", así que firmar al abrir requeriría cambio de Solidity (Wave 1) — descartado para 191a. Zero cambios de contrato (confirma DT-1 del epic). Server sigue validando el monto firmado contra su propio cómputo (`close_payment_intent_for_settle`) — un mismatch NO bloquea el settle actual, solo marca la firma capturada como inválida (telemetría para 191b). El settlement real sigue 100% en el path operator-custodial existente; la firma persistida es inerte hasta que 191b la consuma. | feature/billing | QUALITY | DONE (código) · PENDING-DEPLOY | feat/191a-debit-authorization-capture ([work-item.md](173-wkh-191a-debit-authorization-capture/work-item.md) · [done-report.md](173-wkh-191a-debit-authorization-capture/done-report.md)) |
| 174 | 2026-07-13 | [WKH-191b] Segunda HU de la Wave 0 del epic WKH-191 (fila 172) — rewire escrow-aware del settle real en el flujo normal (session/upto), flag-gated (`ESCROW_SETTLE_ENABLED`, AND con `ESCROW_DEBIT_CAPTURE_ENABLED` de 191a). Consume la firma `DebitAuthorization` `valid` que 191a captura para ejecutar un **two-hop on-chain**: hop 1 `escrow.debit(keyId,…)` (buyer→operador) confirmado on-chain + hop 2 forward operador→seller EIP-3009 (sin cambios). Fallback byte-idéntico si flag OFF / sin firma / sin escrow (kite siempre acá). Diseño money-safe: hop1 falla→fallback silencioso; hop1 OK + hop2 falla→reconciliation-pending (NUNCA refund/double-pay, tx hash persistido, 191c lo resuelve). Idempotencia app-side (tx hash persistido ANTES hop2). F3/AR/CR/F4 DONE: 0 BLQ, 7/7 ACs PASS, 2899 tests verdes, tsc 0, biome 0. Bloqueante activación (no código): R-1/MI-1 seam decimals-aware (191d). | feature/billing | QUALITY | DONE (código) · PENDING-DEPLOY (!) | feat/191b-escrow-settle-rewire ([work-item.md](174-wkh-191b-escrow-settle-rewire/work-item.md) · [done-report.md](174-wkh-191b-escrow-settle-rewire/done-report.md)) |
| 175 | 2026-07-13 | [WKH-191c] Tercera HU de la Wave 0 del epic WKH-191 (fila 172) — motor de reconciliación: resuelve `reconciliation_pending` (y el huérfano `hop1_confirmed`) que 191b deja cuando el hop 1 (`escrow.debit`, buyer→operador) se confirmó on-chain pero el hop 2 (operador→seller) falló o quedó ambiguo. Endpoint admin-gated (mismo patrón `DASHBOARD_ADMIN_TOKEN` de WKH-189) que re-verifica on-chain el evento `Debited` ANTES de decidir, y resuelve EXACTAMENTE UN LADO por intent: completar hop2 (retry reusando `settleEscrowAware`, guardia exactly-once de 191b) o devolver al buyer. Hallazgo clave de F0: `WasiAIEscrow.withdraw()` exige `msg.sender==depositor` — el operador NO puede invocarlo por el buyer; el refund es un transfer directo operador→buyer-wallet (mismo primitivo que hop 2, `payTo` = firma recuperada) + `refund_a2a_key_spend` off-chain, NUNCA vía `withdraw()`. Agrega detección (NO corrección automática global) de drift entre el `budget` off-chain y `escrowBalance(keyId)` on-chain (libro autoritativo, decisión del founder). Cero cambios a `contracts/`, cero `arbiter.ts` (Wave 1 sigue bloqueada). Pipeline QUALITY: F0→F1→F2(SPEC_APPROVED)→F2.5→F3→AR(1 BLQ doble-hop2)→fix-pack→re-AR(0 BLQ)→CR(0 BLQ)→F4(9/9 ACs PASS). 0 tsc, 2927 vitest, 0 biome. Migración PENDING-DEPLOY (activación 191d). | feature/billing | QUALITY | DONE (código) · PENDING-DEPLOY | feat/191c-reconciliation-engine ([work-item.md](175-wkh-191c-reconciliation-engine/work-item.md) · [done-report.md](175-wkh-191c-reconciliation-engine/done-report.md)) |
| 176 | 2026-07-13 | [WKH-192] Seam `settlePaymentIntentOnChain` decimals-aware — Fix del hardcode 18d en `usdToWei` para soportar chains con ≠decimals (Base USDC 6d). `usdToAtomic(usd, decimals)` BigInt puro, byte-idéntico Kite 18d, wire única resolución de `getPaymentAdapter()`. Pre-requisito de código de WKH-191d. | bugfix/billing | QUALITY | DONE | fix/192-wkh-192-settle-decimals-aware ([done-report.md](176-wkh-192-settle-decimals-aware/done-report.md)) |
| 177 | 2026-07-13 | [WKH-191f] Wave 1 (HU 6/8) del epic WKH-191 (fila 172) — primer y único cambio de Solidity de la Wave 1: rol `_arbiter` dedicado (separado de `_operator`) + mecanismo de consentimiento explícito on-chain (`setArbitrationConsent`, monotónico/irrevocable, DT-2) + `resolveDispute(keyId, seller, sellerAmount, nonce)` que paga al seller SIN firma del depositante, restringido a `msg.sender==_arbiter` AND consentimiento previo del `keyId`, con anti-replay reusando `_usedNonces` ya existente (DT-3, cero storage nuevo para exactly-once). Storage nuevo consume 2 slots del `__gap[43]→[41]` (CD-1, UUPS-safe). Extiende (no reemplaza) ambas suites de invariantes Foundry con un ghost term `ghost_totalArbiterResolved` + hostile-witnesses nuevos para `resolveDispute` sin arbiter/sin consentimiento. Gap DT-5 RESUELTO: el depositante YA NO puede `withdraw()` fondos durante una disputa abierta app-side — el lock on-chain congela el monto disputado vía `lockForDispute`; `withdraw` respeta el lock, permitiendo solo `amount <= balance - locked`; `resolveDispute` puede pagar al seller solo de lo lockeado. La garantía non-custodial se preserva: el árbitro solo mueve fondos lockeados de keyIds consentidos. Scope: SOLO `contracts/` (contrato + interfaz + tests unitarios + invariantes) — el wire de `arbiter.ts` es WKH-191g, el deploy/upgrade on-chain con timelock es WKH-191h (gated), ambos Scope OUT. Testnet-only (CD-5 heredado del epic). | feature/security/smart-contract | QUALITY | DONE (código) · UPGRADE-PENDING (gated) | feat/191f-escrow-arbiter-contract ([work-item.md](177-wkh-191f-escrow-arbiter-contract/work-item.md) · [done-report.md](177-wkh-191f-escrow-arbiter-contract/done-report.md)) |
| 178 | 2026-07-13 | [WKH-191g] Wave 1 (HU 7/8) del epic WKH-191 (fila 172) — wire de `src/services/arbiter.ts` al contrato `WasiAIEscrow` upgradeado por 191f: al transicionar a `'disputed'` → `lockForDispute(keyId, authorized_usd_atomic)` best-effort (cubre auto-resolve rules/llm bajo cap Y el camino a `arb_hold`); al resolver (`executeArbitration`, tanto el auto-path como el override admin WKH-189 vía `resolveHold`, que hereda el wire sin código nuevo) → `resolveDispute(keyId, seller, sellerAmount, nonce)` si `settleUsd>0` o `releaseDispute(keyId)` si es refund total. Triple gate flag+escrow-configurado+`arbitrationConsent(keyId)` (view) antes de cualquier llamada `onlyArbiter`; fallback byte-idéntico al path operator-custodial actual si falta cualquiera. Key dedicada `ARBITER_PRIVATE_KEY` (nunca `OPERATOR_PRIVATE_KEY`, DT-1). Hallazgo crítico de F0: el flujo de captura de `setArbitrationConsent` NO EXISTE hoy — exige tx directa del depositante (no delegable por firma/relay, a diferencia de `DebitAuthorization`), gap de producto/frontend fuera de alcance de un wire backend-only → marcado `[NEEDS CLARIFICATION]`, HU separada sugerida; hasta entonces el sistema cae SIEMPRE al fallback (comportamiento correcto, CD-7). Segundo hallazgo: `resolveDispute` reusa el MISMO namespace de nonces (`_usedNonces[keyId][nonce]`) que `debit()`/`debitBatch()` — el nonce del árbitro debe derivarse en un esquema disjunto (DT-5, a confirmar en F2). Code-complete/testeable con mocks; ejecución real depende de 191h (deploy/upgrade + `setArbiter()`). Testnet-only (CD-6 heredado del epic). F3/AR/CR/F4 DONE: 0 BLQ, 1 MNR-1 (refina R-3), 8/8 ACs PASS, 2963 tests verdes, tsc 0, biome 0. | feature/security/billing | QUALITY | DONE (código) · ACTIVATION-PENDING (gated 191h+consent) | feat/191g-arbiter-onchain-wire ([work-item.md](178-wkh-191g-arbiter-onchain-wire/work-item.md) · [done-report.md](178-wkh-191g-arbiter-onchain-wire/done-report.md)) |
| 179 | 2026-07-13 | [WKH-195] Fee-charge + compose decimals-aware — cierre del hardcode `×1e12` gemelo de WKH-192 (fila 176) en los 2 seams que WKH-192 dejó como Scope OUT explícito: `feeUsdcToWei()` (`fee-charge.ts:164-166`, leg de plataforma + legs creator/referral vía `fee-split.ts:32`) y el `valueWei` inline del pago inbound x402 (`compose.ts:819-821`). Ambos reusan `usdToAtomic` EXPORTADO de `payment-intent.ts` (DRY, CD-1), byte-idéntico Kite 18d por construcción (CD-2), fallback `?? 18` sin throw (CD-4). Decisión F2: `feeUsdcToWei` MANTIENE su firma `(feeUsdc:number):string` y deriva los decimales ADENTRO del default-chain adapter → `fee-split.ts` intacto, hereda el fix transitivamente (AC-6, CD-3). `compose.ts` hoistea una sola resolución del adapter para decimals+sign (settle de `:928` = mismo singleton determinístico). Latente hoy (default chain Kite 18d correcto); se activa al poner la default chain en Base (USDC 6d → monto 10¹²× mayor). Pipeline QUALITY DONE: tsc 0, 2972 tests verdes, biome limpio, 6/6 ACs PASS, 0 BLQ, 0 MENOR. Merged: main. | bugfix/billing | QUALITY | DONE | fix/179-wkh-195-fee-compose-decimals-aware ([work-item.md](179-wkh-195-fee-compose-decimals-aware/work-item.md) · [sdd.md](179-wkh-195-fee-compose-decimals-aware/sdd.md) · [done-report.md](179-wkh-195-fee-compose-decimals-aware/done-report.md)) |
| 180 | 2026-07-13 | [WKH-194] Contra-medida del nonce del árbitro (anti-griefing R-3/MNR-1) | feature/security | QUALITY | DONE | fix/194-arbiter-nonce-anti-griefing ([done-report.md](180-wkh-194-arbiter-nonce-anti-griefing/done-report.md)) |
| 181 | 2026-07-14 | [WKH-196] Fix uint256 NUMERIC(78,0) precision read — castear `::text` en 5 `.select()` (debit-capture/arbiter/reconciliation ×3), desbloquea activación escrow-settle WKH-191 | bugfix/security | QUALITY | DONE | fix/181-wkh-196-uint256-numeric-precision-read ([done-report.md](181-wkh-196-uint256-numeric-precision-read/done-report.md)) ([work-item.md](181-wkh-196-uint256-numeric-precision-read/work-item.md)) |
| 182 | 2026-07-24 | [WKH-234] PaymentAdapter Solana en el gateway (fee del agente Solana-native) — descubrimiento chain-agnóstico ya existe (WKH-113); esta HU construye el rail de dinero: `ChainKey` += `'solana-devnet'`, `PaymentAdapter` unión discriminada por `vmFamily`, validador base58 namespace-aware, resolver aliases, AdaptersBundle Solana (settle SPL real on-chain + verify), downstream/compose wiring per-leg chain-aware, ledger CAIP-2 aditivo con ownership guard intacto. 10/10 ACs PASS, 0 BLQ (AR/CR), 2 deferrals diferidos como follow-ups (idempotencia cross-proceso, balance pre-check Solana). 6 waves + fix-pack(1) (AR-1 AC-8 falso-verde, CR-2 lint), tsc 0 / 3025 tests / biome 0. Código DONE + REVIEWED. Pendiente: merge a main + aplicar migración SQL + flip flag (founder-gated). | feature/billing/security | QUALITY | DONE (merged 8da3560 + bdwv migrado) | feat/182-wkh-234-solana-payment-adapter ([work-item.md](182-wkh-234-solana-payment-adapter/work-item.md) · [done-report.md](182-wkh-234-solana-payment-adapter/done-report.md)) |
| 183 | 2026-07-24 | [WKH-237] remit-kyc-validator: identidad ERC-8004 anclada en Avalanche (HU-SOL-31, Solana LATAM Labs) — extiende `ERC8004_ALLOWED_CHAINS` en discovery.ts para aceptar C-Chain 43114 / Fuji 43113 junto a Base 8453/84532 (sin regresión), alineando el código al claim del deck (DT-1a). Accept-set (declare + reverse-lookup JSONB) = 100% código+DB testeable sin RPC; el VERIFY/BIND on-chain real sigue hardcodeado a Base y NO hay IdentityRegistry ERC-8004 deployado en Avalanche (Scope OUT: WKH-237b + deploy founder-gated). | feature/security | FAST+AR | DONE | feat/183-wkh-237-erc8004-avalanche-allowset ([work-item.md](183-wkh-237-erc8004-avalanche-anchor/work-item.md) · [done-report.md](183-wkh-237-erc8004-avalanche-anchor/done-report.md)) |
| 184 | 2026-07-25 | [WKH-241] Exponer el payment spec declarado de los agentes self-published (`metadata.payment`) en /discover — completa el rail de fee Solana-native para `remit-corridor-fx-solana`/`remit-cashout-payout-solana` (WKH-235/236, Solana LATAM Labs). Causa raíz confirmada: `mapRowToAgent` (`agent.ts:112-131`) nunca setea `Agent.payment`; solo el mapper de registries externos (`discovery.ts` `mapAgent:611`, vía `readPayment`) lo hace. Fix: extraer `readPayment` a un módulo leaf compartido (`src/lib/payment-spec-reader.ts`, evita el ciclo agent.ts↔discovery.ts) reusado por AMBOS mappers — misma defensa de chain WKH-113 (SEC-AR BLQ-MED-1), sin validador paralelo. `payment` se deriva SOLO de `metadata.payment` explícito (NUNCA de `payout_wallet`, campo distinto — creator-split del 1%, WKH-143b/234). Sin write-path nuevo en esta HU (los 2 agentes target ya tienen `metadata.payment` seedeado por otra vía; exponer un `POST`/`PATCH /agents` que acepte `payment` queda Scope OUT, follow-up sugerido). Confirmado en F0: `signAndSettleDownstream`/`settleSolanaLeg` (`downstream-payment.ts`) YA rutean correctamente con `agent.payment.chain='solana-devnet'` — esta HU es la ÚNICA pieza de código faltante. CÓDIGO habilita el rail; falta CONFIG/OPS (`SOLANA_ADAPTER_ENABLED=true` + `WASIAI_DOWNSTREAM_X402=true` en Railway, founder-gated) para que WKH-235/236 cierre end-to-end. | feature/billing | FAST+AR | DONE | feat/184-wkh-241-expose-self-published-payment-spec ([work-item.md](184-wkh-241-expose-self-published-payment-spec/work-item.md) · [done-report.md](184-wkh-241-expose-self-published-payment-spec/done-report.md)) |
| 185 | 2026-07-25 | [WKH-235a] Idempotencia durable del settle Solana (scope reducido: recuperación de firma tras timeout) — cierra la RAÍZ del follow-up AR-1 de WKH-234 (fila 182): cuando `sendAndConfirmTransaction` lanza timeout con la tx igual confirmada on-chain, recuperar la firma-candidata (de error.signature o Transaction.signature Buffer), re-verificar on-chain REUSANDO el `verify()` existente (monto/mint/destino/meta.err) y si es válida, declarar éxito SIN re-broadcastear. RE-SCOPE 2026-07-25 (ver work-item "RE-SCOPE del orquestador"): durable dedup en `a2a_receipts.settle_intent_id` + `x-idempotency-key` + migración SQL DIFERIDOS porque hoy no existe mecanismo que reintente settle Solana con mismo intentId (hallazgo F0 #3). Reactivación: **antes de mainnet / dinero real, o en cuanto exista reintentador de settles**. Arch: dato plano inyectado a SolanaSettleRequest — CD-1 preservada (adapter sin DB/services). 100% testeable con mocks; aplicar migración es paso ops post-merge. F3/AR/CR/F4 DONE: 4 commits (feat+fix-pack AR+fix-pack CR), 3089 tests PASS (3055 baseline +34 nuevos), 0 BLQ (AR/CR), 3 MNR (AR) / 4 MNR (CR) resueltos en fix-packs, tsc 0, biome 0. | bugfix/security/billing | QUALITY | DONE | fix/185-wkh-235a-solana-settle-idempotency-durable ([work-item.md](185-wkh-235a-solana-settle-idempotency-durable/work-item.md) · [done-report.md](185-wkh-235a-solana-settle-idempotency-durable/done-report.md)) |
| 186 | 2026-07-25 | [WKH-175] Default-chain UX en ops pagas (subset DX): el gateway resolvía la chain por default en silencio cuando el caller no manda `x-payment-chain`, y el 403 de presupuesto decía solo `chain 2368 balance is 0`. Ahora: (1) warn dedupeado **por caller** (`keyId`) con ventana de 15 min + cap con desalojo del menos-recientemente-warneado, más `debug` per-request (el dedup previo por `chainKey` daba ~1 warn por boot: inútil para detectar que un caller dejó de mandar el header); (2) header de respuesta `x-a2a-payment-chain` con el slug resuelto, seteado en el punto de resolución para que viaje también en el 403; (3) 403 enriquecido con slug + causa + chains con saldo **excluyendo la chain que acaba de fallar** (antes podía sugerir la misma red que rechazó el pago). Status y `error_code` idénticos: cero cambios de contrato. **Invariante de seguridad:** el enriquecimiento vive SOLO en el branch master (el `keyRow` es la credencial presentada); delegación y key-session NO lo usan porque ahí el parent key puede ser de otro owner (fuga cross-owner) — documentado en el JSDoc de ambos helpers. Sin `doc/sdd/` propio: subset acotado de un ticket ya analizado, el registro vive en Jira. Scope OUT founder-gated: cambiar el default, header obligatorio, `default_chain` per-key, `exposedHeaders` del CORS. AR 0 BLQ (verificó ausencia de fuga cross-owner) · CR 0 BLQ (reprodujo la política de desalojo y midió el trade-off del dedup) · F4 APROBADO · 2 fix-packs cerraron 10 MENORes. tsc 0 / 3121 tests / biome 0. | feature/dx/security | FAST+AR | DONE | feat/wkh-175-default-chain-dx (merge `5a8fafa`) |
| 189 | 2026-07-26 | [P1-FIX-PACK] Cinco hallazgos P1 de pruebas profundas en 4 commits revertibles por separado (los 2 de `/discover` van juntos: comparten `discovery.ts`, `discover.ts` y la misma sección de INTEGRATION.md, así que separados no revierten sin conflicto — verificado con `git revert --no-commit` de cada commit en aislamiento). **(1)** `/discover` usaba el `limit` del caller como límite de fetch upstream (`schema.discovery.limitParam`), truncando el candidate-set ANTES de los filtros locales → medido con el catálogo de prod: `limit=5` devolvía **2** agentes y `total` subestimaba los matches (2 en vez de 7). Fix: over-fetch independiente (`DISCOVERY_UPSTREAM_FETCH_LIMIT`, default 200). Corrección al diagnóstico reportado: que `total !== agents.length` NO era el bug — es el contrato correcto de paginación; "arreglarlo" habría roto la paginación del cliente. Contrato de `total` fijado y documentado (no existía). **(2)** `minReputation` se aceptaba y se ignoraba en silencio (0 hits en `discovery.ts`). Implementado sobre la fuente real ya cableada (`computeReputationBatch`, 0-100, pre-limit) y filtrando SÓLO `computedReputation.score`, NO el `reputation` auto-reportado por el registry (un filtro de calidad cuyo input controla el filtrado no filtra nada). Modo NaN extra no reportado (`parseFloat('abc')`) cerrado con 400 `INVALID_MIN_REPUTATION`; el JSDoc decía escala 0-1 y era falso. `/compose`+`/orchestrate` NO lo aceptan (sospecha del reporte: negativa). **(3)** Artefacto de float en el monto atómico del challenge 402: NO estaba en `x402.ts`/`augmentX402ChallengeAmount` ni había ningún `Number(...)` — era `parseUnits(amountUsd.toFixed(DECIMALS))` en los 5 adapters, y el drift no es de 1 wei sino de −107 a +89 **cambiando de signo**. Helper `usdToAtomicUnits`. Invariante del AR de la HU 188 probado: 6-dec (USDC, el money-path) **idéntico en 200.000 floats**. `quote()` de Solana estaba en **0% de cobertura** → 3 tests nuevos. **(4)** `steps[].downstreamSettle="skipped:<code>"` en `/compose` (aditivo): el motivo vivía sólo en logs. Revisión de fuga: 6 de 16 skip-codes filtraban config del gateway / fondos del operador / estado de claves → genericizados a `NOT_CONFIGURED`/`UNAVAILABLE` con un `Record` exhaustivo por tipo (agregar un código sin decidir visibilidad no compila). `FLAG_OFF` es warn-once por proceso → hubiera perdido la señal del 2º request. **(5)** `_intentSignatures` sin cota: cap + TTL fail-safe (TTL default = 10× `TIMEOUT_COMPOSE_MS`, override con piso ×2, cap SOFT con ventana protegida que NUNCA desaloja algo joven, porque desalojar una entrada viva = doble pago). Tests con dientes probados por mutación (3 mutaciones → 1/3/8 fallos). Gates: tsc 0, biome 0, **3335 tests** (baseline 3205 +130), cobertura verificada línea-por-línea en cada fix. **Reincidencia documentada**: agregar un export a un módulo que las suites mockean completo (sin `importOriginal`) rompió 12 tests y después 84 — resuelto con módulos leaf (`lib/discovery-query.ts`, `lib/downstream-skip-code.ts`). 3 TDs abiertos (`total` con catálogos >200/registry; NaN de `maxPrice`; `maxAmountRequired` negativo hace el guard de binding siempre falso). **AR it2 → RECHAZADO (1 BLQ-BAJO + 6 MENOR), fix-pack aplicado**: **(BLQ-BAJO-1)** el over-fetch de (1) cambió la MEMBRESÍA del pool que `/compose` usa para hidratar `payment.chain` — `resolveAgent` pedía `discover({limit:50})`, o sea el **top-50 RANKEADO**, y al pasar el ranking a 4× candidatos con los mismos 50 slots un agente non-EVM podía caerse del pool, perder la hidratación (WKH-113: `getAgent` hardcodea `chain=avalanche`) y terminar con el leg downstream salteado o al rail equivocado — **sin cobrarse, en silencio**. Fix: `resolveComposeAgentPoolLimit()` en un leaf compartido, con el page size del pool IGUAL al fetch upstream (`resolveUpstreamFetchLimit` es idempotente ⇒ el `slice` no puede descartar un candidato traído, y el pool es superconjunto del de `main`). Se descartó `discover({})`: sin `limitParam` el tamaño del pool lo decidiría el default de paginación del registry. 7 tests nuevos sin mockear `discoveryService` (el bug vive en la interacción page-size↔over-fetch), incluyendo el borde del settle por el path real de `/compose`; mutación volviendo a 50 mata 4/7 y desalinear cache↔fallback mata 1/7. Revisados los otros 3 consumidores de `discover()` con `limit` implícito: `orchestrate` (50) y el tool MCP (20) NO son bugs (ranking POR DISEÑO, no hidratan pagos), `capabilities` no manda `limit`. **(MENOR-1)** el knob `SOLANA_INTENT_DEDUP_TTL_MS` prometía «un run no puede sobrevivir a su propio timeout» y es FALSO: `middleware/timeout.ts` sólo manda el 504, no cancela nada (sin AbortController/signal) y `ssrf-dispatcher.ts` no fija headersTimeout/bodyTimeout ⇒ no hay cota dura, el margen real del default era ~1.2× y no 10×. Se eligió NO cancelar (abortar en medio de un settle = el estado indeterminado del que el Map protege) y derivar el piso de una cota ESTIMADA y declarada como tal: 5 steps × 300 s de undici = 25 min (piso 6→25 min, TTL default 30→50 min). **(MENOR-2)** el warn del cap saturado era once-per-**proceso** (el mismo anti-patrón que yo había documentado 2 entradas antes para `FLAG_OFF`) → re-armado al bajar del cap, 2 tests con dos episodios. **(MENOR-3)** el invariante de 6 decimales queda acotado a `[0,100)` (el rango barrido) con los 2 contraejemplos declarados (`5e-7`: 0→1; `>=1e21`: lanzaba→devuelve). **(MENOR-4)** `limit` sin validar contra un doc NUEVO que prometía `min(limit,total)`: `limit=0` devolvía todo el catálogo y `limit=-3` devolvía `total-3` → 400 `INVALID_LIMIT` (entero ≥1, sin techo a propósito). **(MENOR-5)** 2 sitios más de conversión USD→atómico con `parseUnits(String(...))` en la pata OUTBOUND (el EVM que marcó el AR **y** el leg Solana, que no marcó) → `usdToAtomicUnits` + guard fail-closed nuevo: un precio que redondea a 0 unidades atómicas NO se firma. **(MENOR-6)** 3 punteros stale al `Record` de skip-codes + `StepResult.downstreamSettle` tipado `` `skipped:${PublicDownstreamSkipCode}` `` en vez de `string`. Gates it2: tsc 0, biome 0, **3362 tests** (+27), cobertura sin líneas nuevas en 0. Anotada en auto-blindaje una advertencia de PRODUCTO para el founder (no de código): con `REPUTATION_SCALE_FACTOR=50` un `minReputation=50` deja el marketplace casi vacío, y un cliente que usaba la escala 0-1 mal documentada pasa de "todo" a "sólo agentes con ≥1 task liquidada" sin release note. | bugfix/security/billing | FAST+AR | DONE (mergeada a `main`, merge `6373dd8`; la rama `fix/p1-discover-reputation-402-cap` no tiene ningún commit fuera de `main` y el fix-pack **AR it3** `9f17f16` también entró). ⚠️ **Corregido 2026-07-29**: esta celda decía "F3 DONE + fix-pack AR it2 (pendiente re-AR/CR/F4)" y un F1 planificó sobre ese estado viejo, recomendando esperar un merge que ya había ocurrido (ver la nota "Un estado desactualizado acá hace planificar mal" al final de este índice). Lo verificable en git es que el código está en `main` (`usdToAtomicUnits` en `src/adapters/base/payment.ts:471` y `src/adapters/solana/payment.ts:260`); **si el re-AR / CR / F4 se corrieron formalmente no queda registro en el repo**: el directorio de la HU sólo tiene `work-item.md` y `auto-blindaje.md`, sin `validation.md` ni `done-report.md`. | fix/p1-discover-reputation-402-cap ([work-item.md](189-p1-discover-reputation-402-cap/work-item.md) · [auto-blindaje.md](189-p1-discover-reputation-402-cap/auto-blindaje.md)) |
| 190 | 2026-07-29 | [WKH-303] Congelar el precio de la cotización entre `/orchestrate/plan` y `/orchestrate/execute` — **el precio que una persona acepta es el que paga, aunque el mercado se mueva en el medio**. Antes, `/execute` re-resolvía el precio EN VIVO y solo rechazaba si superaba el techo `maxQuotedCostUsdc`: si el precio se movía pero quedaba bajo el techo, se cobraba el monto nuevo **en silencio**, uno que el caller nunca aprobó. Ahora `/plan` emite una cotización firmada (HMAC-SHA256, **stateless**: sin tabla ni Redis, AC-7/CD-1) que congela **precio E identidad del agente** por step durante 10 min (`QUOTE_TTL_SECONDS=600`), y `/execute` la redime. Beneficia sobre todo el camino agent-key prepago (el que usa Chaski); x402 ya estaba cubierto porque el pagador firma el monto exacto. **Los tres límites de la garantía, los tres decisiones y no olvidos:** (1) **se pierde si el cliente no reenvía la cotización** — omitirla es el camino de back-compat (AC-6/CD-2) y vuelve al precio vivo bajo techo, así que un SDK que se olvide degrada a "te cobro el precio nuevo que no aprobaste"; el fix-pack agregó el rastro `[orchestrate.quote.absent]` (`orchestrate.ts:527`, solo si el caller era bindeable) para poder **medir** cuántas ejecuciones corren sin garantía, número que antes no existía; (2) **con cotización válida `maxQuotedCostUsdc` queda inerte** — el congelado ES el techo (`doc/INTEGRATION.md:333-342`, dentro del propio ejemplo); (3) **una cotización se puede redimir más de una vez** en sus 10 min y cada redención ejecuta y cobra — *"a quote is not an idempotency key"* (`INTEGRATION.md:387-400`): el modo de falla real es un reintento por timeout que dispara una segunda ejecución y un segundo débito. **Exposición comercial escrita a pedido del AR**: dentro de la ventana, si el precio vivo sube el operador absorbe la diferencia **por cada redención**, no una sola vez; no es una máquina de sacar plata (quien llama paga el congelado cada vez y gasta su presupuesto), y es observable en producción vía `[orchestrate.quote.price-delta]` (`orchestrate.ts:604`, con `frozenUsd`/`liveUsd`/`deltaUsd`). **Mutación**: 22 mutantes + 1 de control; **M18 sobrevive y es EQUIVALENTE**, verificado por separado por AR y CR (probaron `0`, `-0`, `0.0`, ceros derivados de aritmética y `"0"` string: en todos `signQuote` devuelve `null`, porque el guard de la ruta `:345-347` es redundante frente a `isFreezablePrice` en `orchestrate-quote.ts:210` + el corte en `:241`); la propiedad sí está candada donde decide, por **M9**, que muere con `T-Q-U6`/`T-Q-B7`. **Mutante de control** (borrar el guard `i > 0` anti-double-charge del step-0 en `compose.ts`): **45 tests rojos en los dos archivos que nombra el §11.1** del Story File (`orchestrate.billing.test.ts` + `compose.test.ts`) y **60 en la suite completa** — dos alcances de la misma medición, se anota así para que nadie lea una discrepancia donde no la hay. AR APROBADO con MENORes · CR APROBADO CON MENORES · 0 bloqueantes · fix-pack de 5 commits · F4 APROBADO 7/7 ACs con evidencia archivo:línea. Gates: **4100 passed · 19 skipped · 0 failed** (conteo corrido en el cierre, no leído del reporte). ⚠️ **Colisión de numeración `190` (NO resolver ahora)**: cuatro directorios comparten el número (`190-p1-guards-sin-proteccion`, `190-wkh-303-quote-freeze`, `190-wkh-305-compose-field-mapping`, `190-wkh-306-prepago-agentes-propios`) y **21 directorios no tienen fila** en este índice (18 números sin fila, de los cuales `190` aporta 4 ⇒ 17+4=21; con esta fila quedan 20). Renumerar con cinco árboles de trabajo activos es pedir un choque de merges: queda anotado para ordenar con el árbol quieto. 📄 **Este reporte necesita sincronizarse a `wasiai-ecosystem-docs`** (migración de documentación interna en curso). | feature/billing/security | QUALITY | DONE | feat/303-quote-freeze ([work-item.md](190-wkh-303-quote-freeze/work-item.md) · [sdd.md](190-wkh-303-quote-freeze/sdd.md) · [story-HU-303.md](190-wkh-303-quote-freeze/story-HU-303.md) · [done-report.md](190-wkh-303-quote-freeze/done-report.md)) |
| 190 | 2026-07-29 | [WKH-306] Acotar y hacer visible el residuo de pagos varados — cuando un step de `/compose` **paga al agente on-chain** y después el pipeline falla, esa plata salió y no vuelve; antes no quedaba registro. ⚠️ **ENTREGADA EN DOS MITADES CON ESTADOS DISTINTOS, y decirlo importa**: (a) **observabilidad ACTIVA desde el merge** — cada run que falla tras un pago deja fila durable, visible en el panel de reconciliación (`GET /dashboard/api/reconciliation` → `ambiguous.strandedRuns`); (b) **control IMPLEMENTADO, PROBADO y DIFERIDO** — `PIPELINE_EXPOSURE_CEILING_USD` y `STRANDED_EXPOSURE_ALERT_THRESHOLD_USD` se entregan **SIN SETEAR a propósito** (verificado en `main`: `.env.example:1038` y `:1090`, vacías), así que **el día del merge el techo NO acota nada y la alerta NO suena**; sin techo, el guard de presupuesto se comporta exactamente como antes de la HU. Se difirió porque elegir el número hoy sería fijarlo sin un dato de exposición real —medirlo es el motivo de existir de la HU— y un techo bajo **rechaza tráfico legítimo y ya pagado**, daño mayor y de signo opuesto. **EL BLOQUEANTE**: `compose()` llamaba a `executePipeline()` **sin try/catch** y `getStepGasOverheadUsd()` está **diseñada para LANZAR en producción** (`lib/gas-overhead.ts:419-420`); paso 0 paga on-chain → paso 1 pide el gas → lanza → el registro del residuo nunca corría. **Era invisible y el escenario INESCRIBIBLE**: bajo test `isProductionEnv()` es `false`, esa función devuelve 0 y nunca lanza. Arreglo (`compose.ts:226-241`): la envoltura **presta** el array de resultados al pipeline (conserva los steps ya completados aunque se vaya por excepción) y **re-lanza el error tal cual** — observa sin tocar el contrato (convertir el throw en `{success:false}` cambiaría la decisión de débito del step 0 en `orchestrate.ts`). **Números medidos**: cota real **1.9 USD** (peor caso `/orchestrate`, **19 pasos varables**; `/compose` corta en 4), umbral sugerido **19 USD** (10× la cota). Gatillo para configurar: **2 semanas de tráfico real** + `node scripts/report-stranded-exposure.mjs`. **Distinción que corrige una afirmación anterior del propio dev**: el techo es un tope **EN DÓLARES** evaluado contra el **acumulado**, así que un agente más caro publicado mañana **no lo sube: lo CHOCA antes**; lo que caduca no es el techo sino la **sugerencia** de con qué número configurarlo, y esa caducidad **falla del lado benigno** (techo apretado ⟹ pierde disponibilidad, no plata, y se nota solo). **NO ACOTADO**: el camino x402 anónimo (sin agent key no hay débito per-step que corte a mitad). F3 (4107) → AR BLOQUEANTE + CR aprobado → fix-pack → re-AR con 1 reserva → fix → F4 APROBADO. **4118 tests, 21 mutantes / 21 muertos**. Intocables verificadas byte-idénticas: el guard `i > 0` y la posición de `const startTime = Date.now()`. ⚠️ Reporte escrito **después** del merge (desvío de proceso registrado en el propio reporte). 📄 Sincronizar a `wasiai-ecosystem-docs`. | feature/billing/observability | QUALITY | DONE (merged `d0cd3df`) · CONTROL DIFERIDO (envs sin setear) | feat/306-stranded-surface ([work-item.md](190-wkh-306-prepago-agentes-propios/work-item.md) · [sdd.md](190-wkh-306-prepago-agentes-propios/sdd.md) · [done-report.md](190-wkh-306-prepago-agentes-propios/done-report.md)) |
| 209 | 2026-07-29 | [WKH-307] Idempotencia durable del settle en Solana — reemplaza la deduplicación de pagos **EN MEMORIA**, que **se perdía en cada reinicio**, por un ledger durable en Postgres. Lo que lo hace crítico: **en Solana NO hay respaldo on-chain**, así que este seam es la **ÚNICA defensa contra el doble pago**. **EL BLOQUEANTE**: se le preguntaba a la cadena si una tx existe y ante un `null` se concluía "no aterrizó" y se **RE-TRANSMITÍA un pago real** — pero `null` puede ser "no existe", "ese nodo no tiene ese pedazo de historia" o "el nodo va atrasado", y solo la primera autoriza pagar. **El arreglo vive EN EL TIPO**: `SettlementPresence` (`adapters/types.ts:158-175`), unión de **cinco estados que el compilador obliga a agotar**, con "no está" (`absent`) y "no pude preguntar" (`unknown`) por fin separados; `landed_mismatch` es fail-closed con mirada humana y `landed_failed` sí permite re-pagar (una tx fallida ya está grabada y nunca puede re-ejecutarse). La determinación negativa usa ahora `getSignatureStatuses` con `searchTransactionHistory: true` (`solana/payment.ts:548-549`), que obliga al nodo a buscar en el histórico largo. **FAIL-CLOSED de la retención del RPC** — el arranque **mide** cuánta historia retiene el nodo, porque de eso depende poder afirmar "esta tx no existe": medida y suficiente ⟹ arranca; **medida e insuficiente ⟹ corta SIEMPRE**; no medible sin declaración ⟹ corta; no medible **con** declaración explícita del operador ⟹ arranca con warn. **El matiz que la hace correcta: una medición negativa es evidencia positiva de falla y la declaración del operador NO la anula** — la salida cubre únicamente el caso de *no poder medir*, y eso está en la estructura del código (el chequeo de la env vive **dentro del `catch`**, `schema-preflight.ts:179`, y no aparece en la rama de medición insuficiente). Sin default permisivo: solo `'true'` exacto abre. **VERIFICADO CONTRA LA BASE REAL, no contra el archivo**: migración aplicada a desarrollo con **8 chequeos leyendo del catálogo**; **SQL ejercitado de verdad, 27/27 casos** (las 4 funciones, dos reclamos en paralelo con un solo ganador, índice único parcial, archivado de firmas); **encontró una garantía escrita que la base NO cumple** (`T-LDG-10` usaba el máximo de un `uint64` sobre una columna `BIGINT` **con signo** ⟹ corregido a **2^53+1**, `settle-ledger.test.ts:314`, que sigue matando una coerción con `Number()` y además entra en la columna); y el **probe verificado POR EL CANAL QUE USA EL CÓDIGO** (no basta que Postgres levante la señal: el cliente tiene que depositarla donde el código la lee). F3 (4122) → AR BLOQUEANTE + CR aprobado → fix-pack → re-AR con 3 reservas → fix + decisión de fail-closed → F4 APROBADO. **4142 tests, 21 mutantes / 21 muertos, 23 tests viejos retirados y 2 invertidos**. **PENDIENTE**: (1) el **gate de re-hidratación de la migración NUNCA corrió contra Postgres** — es el único código ejecutable de la migración sin ejercitar; el guion existe (`gate-rehydration-test.sql`) pero **necesita entorno descartable**, porque probarlo contra desarrollo **rompería el camino que protege** (crea la tabla de backup que el gate detecta para abortar todo apply futuro); (2) **WKH-307b**, aplicar la migración a producción (caldz), founder-gated — existe por diseño una base sin la tabla, con impacto operativo **nulo** hoy (`SOLANA_ADAPTER_ENABLED` default `false`, cadena devnet) y fail-closed recuperable si se encendiera; (3) `story-HU-307.md` sigue **untracked** en `main`, único artefacto del directorio sin versionar; (4) el self-heal automático se **eliminó a propósito**, y su reemplazo es la salida manual de `runbook-destrabe.md` bajo la regla **"la cadena manda sobre la tabla"**. ⚠️ Reporte escrito **después** del merge (desvío de proceso registrado en el propio reporte). 📄 Sincronizar a `wasiai-ecosystem-docs`. | bugfix/security/billing | QUALITY | DONE (merged `c92330a`) · MIGRACIÓN A PROD PENDIENTE (WKH-307b) | feat/307-solana-durable-idempotency-ledger ([work-item.md](209-wkh-307-solana-durable-idempotency-ledger/work-item.md) · [auto-blindaje.md](209-wkh-307-solana-durable-idempotency-ledger/auto-blindaje.md) · [runbook-destrabe.md](209-wkh-307-solana-durable-idempotency-ledger/runbook-destrabe.md) · [work-item-wkh-307b.md](209-wkh-307-solana-durable-idempotency-ledger/work-item-wkh-307b.md) · [done-report.md](209-wkh-307-solana-durable-idempotency-ledger/done-report.md)) |
| 211 | 2026-07-30 | [WKH-313] Carril de estreno (trial standing) — un agente recién publicado tiene score 0 y cualquier piso de reputación `> 0` lo excluye para siempre, porque nunca es elegido y por eso nunca acumula historial; **importa ahora** porque todo agente nuevo de la incubadora nace sin historial. Agrega un carril **opt-in del consumidor** (`allowTrial` / `constraints.allow_trial`, default OFF, CD-9: cero queries nuevas sin opt-in) que admite bajo el piso a un `newcomer` (0 liquidadas, 0 fallos) durante sus primeros `N=3` trabajos pagos, con cupo `M=2` por publicador, techo `T=10` (sobre ese piso no hay estreno) y anulación permanente al primer fallo confirmado por `F=2` callers distintos. El score **nunca se fabrica**: el admitido conserva su puntaje real (0) y ordena **último** — sólo gana cuando ningún agente pasa por mérito (AC-7/CD-6). El vacío por piso deja de ser mudo: `excluded` ahora cuenta `reputation` (descartados) y `trialAvailable` (admisibles), y `standingUnavailable` distingue "sin historial" de "no pude leer el historial" (fail-closed, AC-9). **Corrección al encargo original** (verificada, cambia el encuadre): el gateway hoy NO pone ningún piso por defecto — lo fija siempre quien consulta — y `verified` también es un filtro opt-in, no una asimetría de forma; la asimetría real es de *alcanzabilidad de la salida* (`verified` tiene salida self-service on-chain, la reputación no: la única entrada es ser elegido, que es justo lo que la exclusión niega). 10/10 ACs PASS con evidencia archivo:línea. AR APROBADO 0 BLQ (tras 4 fix-packs: 1 BLQ-ALTO — el `verified`/`reputation` auto-reportado del card rompía la garantía de orden para agentes federados — + 3 BLQ-MED/BAJO — desempate no-permanente, anulación no-perpetua, fail-closed que explica su motivo). CR APROBADO (tras 1 fix-pack BLQ-MED-1 — el carril admitía a quien ya pasaba por mérito en la ventana `1..N-1`, violando CD-6 — + 2 MENORes de mutación). F4 APROBADO en el reintento. tsc 0, biome 0, **4452 tests / 19 skipped**. **W0.3 (consumo desde `chaski-v3`) explícitamente NO EJECUTADA** — bloqueada por decisión del founder sobre el control compensatorio del leg de desembolso (§3.4 SDD/AC-6); `chaski-v3` queda en 0 bytes tocados. **NO MERGEADO** — orden de merge coordinado por el orquestador junto con WKH-315/316/318 (313 va antes que 316: `types/index.ts` en 316 aún no tiene líneas propias; el diff de 313 sobre `types/index.ts` tiene un único hunk no-aditivo, `excluded?: {scope}` → objeto de 4 campos, que exige resolución manual de cualquier conflicto). Corrección de hecho registrada: la afirmación previa "la demo insignia no se destraba" era más amplia de lo medido — sólo el leg de payout depende de esta HU/W0.3; el leg de cotización responde 200 hoy y lo que mantiene inerte el flujo completo son 2 flags de `chaski-v3` sin relación con WKH-313. | feature/marketplace/security | QUALITY | DONE (código) · REVIEWED · NO MERGEADO — pendiente orden de merge coordinado (WKH-315/316/318) | feat/211-wkh-313-primer-trabajo `86216ce` ([work-item.md](211-wkh-313-primer-trabajo-agentes-sin-historial/work-item.md) · [sdd.md](211-wkh-313-primer-trabajo-agentes-sin-historial/sdd.md) · [story-HU-313.md](211-wkh-313-primer-trabajo-agentes-sin-historial/story-HU-313.md) · [auto-blindaje.md](211-wkh-313-primer-trabajo-agentes-sin-historial/auto-blindaje.md) · [residuales.md](211-wkh-313-primer-trabajo-agentes-sin-historial/residuales.md) · [report.md](211-wkh-313-primer-trabajo-agentes-sin-historial/report.md)) |
| 212 | 2026-07-29 | [WKH-314] La pata de ENTRADA de pagos en Solana (x402 inbound) — planificada originalmente como `WKH-210` / `HU-SOL-F3` ("402 intent", OPCIONAL / fast-follow), **ahora obligatoria**: compromiso con la incubadora WayLearn / Solana LATAM Labs para la semana del 2026-08-03. Hoy el leg de SALIDA (gateway → agente, SPL transfer real) funciona y está verificado en cadena, pero **no existe ninguna forma de que el dinero ENTRE al gateway por Solana**, y el encargo nombraba sólo una de las dos paredes. **Pared A** (la conocida): el middleware x402 corta con 400 antes de tocar nada (`middleware/x402.ts:479-497`) porque `acceptsInboundPayment` es literalmente `bundle.payment.vmFamily === 'evm'` (`adapters/registry.ts:510-512`), y todo el camino inbound pasa por `getPaymentAdapter()`, que **lanza a propósito** sobre un adapter no-EVM (`registry.ts:414-422`); el guard gemelo de compose está en `compose.ts:1430-1462`. **Pared B (HALLAZGO NUEVO de este F1)**: el camino prepago tampoco se puede FONDEAR en Solana — `deposit-verifier.ts:82-86` dice en el propio código que el deposit es *Scope OUT / código muerto* para Solana, y `routes/auth/deposit.ts:57,63-65` exige `tx_hash` `/^0x…{64}$/` y `chain_id: number` (una firma Solana es base58 y su chainId es un sentinel sintético, `registry.ts:263-264`) ⟹ **todo dólar que entra hoy entra por una chain EVM**, y *"no debe intervenir Avalanche"* no es satisfacible por ninguno de los dos caminos. **Tercer hallazgo**: el anti-replay inbound que ya existe (`x402-nonce.ts:31-53`) **falla ABIERTO** con una justificación escrita —*"el nonce EIP-3009 ya es single-use a nivel token on-chain"*— que **es falsa en Solana**, donde una prueba de pago es una firma ya aterrizada que se puede presentar N veces sin que la cadena objete ⟹ reusar ese seam convierte un blip de DB en servicio gratis ilimitado (CD-4 lo prohíbe). **LA DECISIÓN DE NEGOCIO, aislada y NO tomada** (`[DECIDE FOUNDER]`, §3.4 del work-item): la pata de entrada **invierte** la asimetría del bug sistémico del proyecto — cuando no se puede determinar si un pago LLEGÓ, o se regala el servicio (**≤1.9 USD/corrida**, cota MEDIDA por WKH-306, plata nuestra, en devnet no real) o se rechaza a alguien que pagó de verdad (su SPL transfer es irreversible; recuperarlo exige reembolso manual). El encuadre que hace la pregunta decidible: no es cuál error es más barato, es **cuál es más barato cuando un adversario elige cuál ocurre** — el servicio gratis es repetible a voluntad, el rechazo falso no es explotable por un tercero; y un rechazo **sin consumir la prueba** convierte el rechazo falso en demora, salvo cuando lo indeterminado es el store de single-use. Default propuesto hasta que haya respuesta: denegar sin consumir. **El tipo de retorno de 3 valores YA EXISTE y se reusa, no se rediseña**: `SettlementPresence` (`adapters/types.ts:170-187`, 5 estados con exhaustividad forzada) + el probe `probeSettlementPresence` (`solana/payment.ts:572-644`, `searchTransactionHistory: true`) + `checkTerms` (`:1101-1130`, que lee SÓLO `pre/postTokenBalances` ⟹ la validación de `reference` es código nuevo) + la doctrina del ledger durable (`settle-ledger.ts:15-39`). **Sizing honesto contra la fecha**: el leg completo NO entra; **corte mínimo = "el gateway como TESTIGO"**, una HU y un solo repo (`wasiai-a2a`), con `wasiai-facilitator` en CERO cambios (regla de un escritor por repo; su propia doctrina testigo-vs-tesorero está en `solana-payout.ts:1-32` y su `verifyPayoutSignature` de 3 estados en `verify-transfer.ts:88-159` queda para una HU posterior en ese repo). Corte DESCARTADO: "sólo el 402 intent" sin verificación = anunciar públicamente un pago que no podemos verificar = arreglo a medias en el camino del dinero. 9 ACs EARS (feliz · replay · monto insuficiente · destinatario/mint equivocado · el "no pude determinar" · EVM byte-idéntico · honestidad de `/capabilities` · cero claves privadas Solana). 8 CDs (CD-1 = EVM byte-idéntico probado por 7 suites x402 sin modificar, con `x402.non-evm-inbound.test.ts` reescrita a propósito porque sus expectativas se invierten). **BLOQUEANTES abiertos**: MI-1 — ¿el pagador de la demo paga por x402 o con clave prepaga? Si es prepaga, la HU del camino crítico es la **pared B**, no la A (el modo ratificado de los 3 agentes remit-\* es a2a-key prepago, filas 167/169/170) — no verificable desde este repo, requiere `chaski-v3`; MI-2 — la política de `unknown`. **Dependencia con el KYC**: cero acoplamiento de código, pero el entregable *"los 3 agentes se cobran en Solana"* necesita además que el KYC pase por el riel a2a (`WKH-233`, reportada bloqueada hace 5 días, NO verificada acá) ⟹ **esta HU es necesaria pero no suficiente**. **Riesgo de merge**: la fila 189 (`fix/p1-discover-reputation-402-cap`) está ABIERTA y tocó la superficie del challenge 402 en los 5 adapters ⟹ conflicto alto en `middleware/x402.ts`, conviene esperar su merge. **[CORRECCIÓN 2026-07-29, nexus-docs: la fila 189 YA estaba mergeada a `main` cuando se escribió esto (merge `6373dd8`, la rama no tiene commits fuera de `main`). No hay merge que esperar: el conflicto, si aparece, es contra `main` y se resuelve rebaseando. Esta recomendación salió de leer el estado viejo de la fila 189, ver la nota del final de este índice.]** ⚠️ Escalado aparte: `.nexus/project-context.md` (últ. act. 2026-03-31) contradice el código — Kite-only, sin Solana/Base/Tempo/facilitator; un sub-agente que lo tome como fuente de verdad decide mal en esta HU. | feature/billing/security | QUALITY | in progress (F2 en curso: `work-item.md` + `sdd.md` presentes al 2026-07-29, todavía sin story-file) | feat/212-wkh-314-x402-inbound-solana ([work-item.md](212-wkh-314-x402-inbound-solana/work-item.md) · [sdd.md](212-wkh-314-x402-inbound-solana/sdd.md)) |
| 213 | 2026-07-30 | [WKH-315] Depósito prepago en Solana — hasta esta HU la plata sólo podía **entrar** por EVM; ahora `POST /auth/deposit` acredita un depósito SPL de USDC en Solana devnet, verificado on-chain a `finalized`, exactamente una vez, sin que el gateway cargue ni derive ninguna clave privada (`AC-12`), y sólo desde una wallet Solana que probó su control con firma ed25519 real (`AC-7`, primitivo `node:crypto` medido, no supuesto). Camino EVM queda byte-idéntico (`AC-10`). **SEIS RONDAS de AR/CR sobre el MISMO síntoma** (`{"ok":true,"amountUsd":"1001"}` por un depósito de 1 USDC, cuatro causas distintas dando la salida idéntica) hasta encontrar la causa estructural real: el monto se **calculaba** por un camino y se **auditaba** por otro, con reglas de agregación distintas que nunca se comparaban entre sí — fix: una sola **tabla canónica** (índice→monto, fail-closed) de la que se derivan TANTO el crédito como la ecuación que lo audita. **La causa raíz de las cinco vueltas**: cada iteración escribió una propiedad universal ("cualquier/ninguno/siempre") que la fórmula no sostenía, y esa frase apagaba la búsqueda del propio revisor — criterio que lo cortó: toda afirmación necesita, al lado, el input concreto que la rompería. El mismo patrón (regla general enunciada, aplicada a un solo caso) reincidió 3 veces con el mismo autor hasta mecanizarse con un grep + conteo en el commit. El primer fuzz decía cubrir 4 clases de corrupción y cubría 1 (5/6 guards neutralizados quedaban VERDES); arreglado con candado por-guard + barrido, medido 8/8 ROJO al neutralizar cada guard por separado. 15/15 ACs con evidencia archivo:línea y test nombrado (`src/adapters/solana/deposit-verifier.test.ts` 2116 líneas, `src/adapters/solana/deposit-verifier.fuzz.test.ts` 639 líneas, `src/routes/auth.solana-deposit.test.ts` 1105 líneas). AC-9 (mínimo de depósito) pasa **vacuo y declarado como tal**: `[DECIDE FOUNDER] D-2` sigue abierto. 36 archivos, +11484/-35, 22 commits. `tsc` 0 · suite **4517 passed / 19 skipped** · fuzz 8/8. **Migración NO aplicada** (ni `bdwv` ni `caldz`); runbook fija orden migración→env→flag (`doc/INTEGRATION.md` §6.6). Sin roce con WKH-318; roce trivial con WKH-319 (`src/adapters/types.ts`, bloques aditivos). ⚠️ Al mergear habrá DOS archivos `deposit-verifier.ts` (EVM en `src/adapters/`, Solana en `src/adapters/solana/`): comentarios de otras HUs necesitan el path completo. | feature/billing/security | QUALITY | DONE (código, pendiente merge/activación founder-gated) | feat/213-wkh-315-deposito-prepago-solana ([work-item.md](213-wkh-315-deposito-prepago-solana/work-item.md) · [sdd.md](213-wkh-315-deposito-prepago-solana/sdd.md) · [story-HU-315.md](213-wkh-315-deposito-prepago-solana/story-HU-315.md) · [auto-blindaje.md](213-wkh-315-deposito-prepago-solana/auto-blindaje.md) · [report.md](213-wkh-315-deposito-prepago-solana/report.md)) |
| 214 | 2026-07-29 | [WKH-316] El escritor del bloque de pago de un agente (`payment` en POST/PATCH /agents) — **hoy NO EXISTE camino de escritura para `metadata.payment`**: `PublishAgentInput` (`src/types/index.ts:191-218`) no tiene el campo y `buildMetadata` (`src/services/agent.ts:177-189`) mas el merge del PATCH (`:556-571`) solo persisten `inputSchema`, `outputSchema` y `discoverable`. WKH-241 construyo el LECTOR; el escritor nunca existio, y por eso las dos filas de agentes Solana se sembraron FUERA del repo (done-report de WKH-241:40). Consecuencia de producto, no de demo: **nadie puede publicar un agente que declare en que red cobra**, ni nosotros por API ni un tercero de la incubadora. Ademas **desbloquea WKH-317** (pasar el cobro del agente de KYC a Solana): `remit-kyc-validator` tiene `payment: null`, o sea que cobra por la red default (Kite), NO por Avalanche como se creia. Se valida: slug conocido, riel vivo (via `getInitializedChainKeys()`, porque `getSupportedChains()` no esta exportada), formato del payTo por familia de VM, zero-address de ambas familias, que el payTo no sea la wallet del operador, `method === 'x402'`, y `asset` contra el simbolo del adapter (`'USDC'`, `solana/payment.ts:81`). NO se valida la existencia on-chain del token ni sus decimales, y el fundamento esta medido: el settle SIEMPRE los toma de `adapter.supportedTokens[0]` (`downstream-payment.ts:284`, `:777-785`), asi que un token inexistente no puede cambiar lo que se transfiere, solo hace mentir al catalogo. El `toLowerCase()` peligroso queda **encerrado en la rama EVM** (es correcto del hex y DESTRUYE un base58). La actualizacion posterior del bloque se permite sin versionado: repuntar el payTo redirige la plata **del propio dueno**, asi que no es robo; la mitigacion proporcionada es un log de auditoria con `owner_ref` y el par anterior/nuevo. ⚠️ ASUNCION DEL ORQUESTADOR PENDIENTE DE RATIFICACION: se disena **sin** prueba de posesion del payTo (declarar la billetera de un tercero le regala plata a ese tercero, o sea que el dano es de atribucion y no de custodia), de forma aditiva para poder agregarla despues sin rediseno. | feature | QUALITY | in progress (F1 hecho: `work-item.md` con 12 ACs; F2 en curso en su worktree al 2026-07-29) | feat/214-wkh-316-payment-block-writer ([work-item.md](214-wkh-316-escritor-payment-block/work-item.md)) |
| 216 | 2026-07-30 | [WKH-319] Cerrar el fail-open de `checkTerms` en el camino de SALIDA Solana (corte mínimo W0+W1) — `SolanaPaymentAdapter.checkTerms` (`src/adapters/solana/payment.ts:1101-1130` pre-fix) no fallaba abierto por verificar de menos, sino por verificar OTRA COSA: con `preTokenBalances` ausente, el `?? []` fabricaba lista vacía, `balanceFor(pre)` daba `0n`, y `delta` dejaba de ser un delta para pasar a ser el **saldo absoluto** de `payTo` — una tx donde el agente GASTA 100 USDC y no recibe nada se certificaba `{ok:true}` ("nuestro pago llegó"). Fix: `checkTerms` pasa de responder dos valores (*coincide*/*no coincide*) a tres (+ `indeterminate`), con discriminante `verdict` (NO `ok`, deliberado: `ok:true\|false\|'unknown'` sería peor que el bug). Cierra las 5 formas del fail-open (lista ausente/null/vacía/sin la entry/sin `owner`), la familia de `amount` no numérico, el delta negativo, y el `.find()` que sólo miraba la primera cuenta (agregación por `accountIndex`, cierra `TD-INBOUND-MULTI-ATA` de WKH-314 sin trabajo extra). AR APROBADO (2 BLQ resueltos: la 6ta forma del fail-open vía `owner` ausente en `pre` agrandaba el delta; la indeterminación se perdía en el borde del adapter y el leg publicaba `SETTLE_FAILED` disparando reembolso). CR APROBADO (7 MENOR resueltos, incl. adelantar de W2 la lista blanca de la cola de re-transmisión). Re-AR: 3 MENOR de auto-blindaje propio (mutante M25 sobre `declaredOwner`, premisa de frecuencia corregida). F4 APROBADO: 27/27 ACs del corte PASS con evidencia archivo:línea + test nombrado, 3 ACs de W2 (AC-8/16/30, tier de dirección) correctamente deferidos y declarados desde el work-item (DT-11). `tsc` exit 0, `biome` 420 archivos limpio, suite completa **4333 passed / 19 skipped**, Solana **194/1**, **22/22 mutantes** muertos por test nombrado. W2 (tier de dirección, gatillado por la aparición de `terms_unclassifiable_entry` en producción) y W3.2/W3.4 (fila del `_INDEX.md` del repo principal + nota a WKH-314) quedan explícitamente pendientes. **Orden de merge: WKH-319 ANTES que WKH-314** (314 va a mover `checkTerms` con criterio "suite verde sin modificarse", incompatible con este fix); roce trivial con WKH-315 (sólo `adapters/types.ts`, bloques aditivos disjuntos). **NO mergeado ni pusheado desde `wt-319`** (decisión del founder, 4 HUs en vuelo). | feature/security | QUALITY | DONE (código, en worktree — pendiente merge/decisión del founder) | feat/216-wkh-319-checkterms-fail-open ([work-item.md](216-wkh-319-checkterms-fail-open/work-item.md) · [sdd.md](216-wkh-319-checkterms-fail-open/sdd.md) · [validation.md](216-wkh-319-checkterms-fail-open/validation.md) · [report.md](216-wkh-319-checkterms-fail-open/report.md)) |
| 215 | 2026-07-30 | [WKH-318] `limit` colapsa el registro federado en `/discover` (y la respuesta lo negaba) — **corte A (W0+W1+W2), shippeable solo**. Medido en producción: `/discover?limit=N` devolvía 3 de 23 agentes para cualquier `N` (incluso `limit=200`), porque el fanout federado recibe HTTP 400 del upstream (over-fetch siempre ≥200, cap real del registro `WasiAI` = 100), el `.catch(() => [])` se traga el error, y la respuesta seguía afirmando `registries: ["WasiAI","self-published"]`. **Los 3 sobrevivientes son exactamente los 3 agentes de Chaski** (self-published) — Chaski funcionaba por coincidencia, no por diseño. El corte A no arregla el fetch (eso es el corte B, W3+W4, **no empezado**): arregla que la falla deje de reportarse como éxito — `registries` se calcula desde las fuentes que aportaron filas, y la respuesta suma `sources[]` + `catalogStatus` (`complete\|unverified\|truncated\|partial`) como señal explícita. Fix-pack post-AR (BLQ-1, BLQ-2): la primera versión colapsaba dos semánticas de `state: 'ok'` ("trajo todo" vs. "no hay forma de saber") y era exactamente el caso de producción; se agregó el cuarto estado `unverified`, que se gana con evidencia positiva en vez de por descarte. AR y CR **APROBADOS** tras el fix-pack; F4 **APROBADO**: `tsc --noEmit` limpio, suite 4342 passed/0 failed/19 skipped, 16/16 mutantes KILLED con hash+tsc+test citado (`mutation-log.md`), y verificación read-only contra `bdwv` de que la migración `20260730010000_wkh318_registry_next_cursor_path.sql` **no está aplicada** (consistente con el `unverified` observado, no una promesa de código). Tres hallazgos quedan como **precondiciones del corte B** en `backlog.md` (B-5: la fuente local declara `ok` incondicionalmente por comentario, no evidencia; B-1: `?registry=` sin match devuelve `complete` vacío; B-6: la evidencia de completitud la auto-declara el registro que se está midiendo). Roce de merge con WKH-313 en `types/index.ts`/`discovery.ts` (separable) y conflicto textual casi seguro, sólo JSDoc, en `routes/discover.ts` — guía de re-anclaje en `story-HU-318.md` §10. Sin roce con WKH-315. Reporte completo: `215-wkh-318-discover-limit-colapsa-registro-federado/report.md`. | feature | QUALITY | **DONE (corte A)** — W3 (clamp) y W4 (money-path estricto) quedan sin empezar, por decisión de corte declarada desde el work-item; no mergeado/pusheado a `main`, decisión pendiente del founder | feat/215-wkh-318-discover-limit-federado ([work-item.md](215-wkh-318-discover-limit-colapsa-registro-federado/work-item.md) · [sdd.md](215-wkh-318-discover-limit-colapsa-registro-federado/sdd.md) · [story-HU-318.md](215-wkh-318-discover-limit-colapsa-registro-federado/story-HU-318.md) · [report.md](215-wkh-318-discover-limit-colapsa-registro-federado/report.md)) |

---

## ⚠️ Deuda de numeración de este índice (anotada, NO resuelta)

Registrada al cerrar WKH-306 y WKH-307 (2026-07-29). **No se renumeró nada a propósito**:
renumerar directorios con varios árboles de trabajo activos es pedir un choque de merges.
Queda para ordenar con el árbol quieto.

**1. Cuatro directorios comparten el número `190`**, y hoy el índice tiene **dos filas
numeradas `190`** (WKH-303 y WKH-306):

| Directorio | ¿Tiene fila? |
|---|---|
| `190-p1-guards-sin-proteccion` | no |
| `190-wkh-303-quote-freeze` | sí (fila `190`) |
| `190-wkh-305-compose-field-mapping` | no |
| `190-wkh-306-prepago-agentes-propios` | sí (fila `190`) |

Las filas apuntan al directorio por **nombre completo** en los links, así que el destino
es inequívoco aunque el número esté repetido.

**2. Dieciocho directorios no tienen fila propia.** Verificado cruzando los prefijos
numéricos de los directorios contra los números de fila: **16 números sin ninguna fila**
(`023 024 068 089 187 188 191 192 193 194 195 198 201 202 203 208`), con un directorio
cada uno, **más** los dos directorios `190-` de la tabla de arriba que no tienen fila ⟹
16 + 2 = **18**.

*Trazabilidad del número*: antes de los cierres de WKH-303/306/307 eran **21**
directorios (18 números sin fila, de los cuales `190` aportaba 4 ⟹ 17 + 4 = 21). Las tres
filas agregadas desde entonces lo bajaron a 18. Si alguien vuelve a medir y le da otro
número, esta es la aritmética con la que reconciliar.

*Addendum 2026-07-29 (auditoría de estados, ver nota siguiente)*: el conteo de arriba se
midió antes de que existieran los directorios `211-`, `212-`, `213-` y `214-`. Los tres
primeros **ya tienen fila** (agregadas por esa auditoría). Siguen sin fila `210-wkh-308-verify-tres-estados`
(WKH-308, **ya mergeada** según el historial de `main`) y `214-wkh-316-escritor-payment-block`
(F1 en curso al momento de la auditoría, sin fila staged). No se recalculó el número de
arriba a propósito: la aritmética original queda como quedó, y esto es el delta.

---

## ⚠️ Un estado desactualizado acá hace planificar mal (nota 2026-07-29)

**Este archivo ES el backlog del proyecto**: Jira está fuera de uso por decisión del
founder, así que la lista de HUs vive en git y se lee desde acá. Por eso una fila con el
estado viejo no es un detalle cosmético: es el dato con el que se decide qué se hace,
en qué orden y qué hay que esperar.

**El caso concreto, y por eso queda escrito.** La fila `189`
(`fix/p1-discover-reputation-402-cap`) decía *"F3 DONE + fix-pack AR it2 (pendiente
re-AR/CR/F4)"*, o sea abierta. Estaba **mergeada**: merge `6373dd8`, la rama no tiene
ningún commit fuera de `main`, y su trabajo se lee en `main` (`usdToAtomicUnits` en
`src/adapters/base/payment.ts:471` y `src/adapters/solana/payment.ts:260`). Un F1 leyó esa
fila, la creyó, y **recomendó esperar el merge de una rama ya mergeada** (quedó escrito en
el "Riesgo de merge" de la fila `212`, con su corrección al lado). El orquestador estuvo a
punto de retrasar una HU por eso. El costo no fue el estado equivocado en sí: fue una
decisión de planificación tomada sobre él.

**Regla, entonces.** Antes de dejar una fila en estado abierto (`in progress`, `WIP`,
`pendiente X`), verificar dos cosas y escribir la evidencia en la propia celda:

1. si la rama existe (`git branch -a` + `git ls-remote --heads origin`: puede existir local
   y estar ya contenida en `main`, que es exactamente el caso de la `189`);
2. si su trabajo está en `main` (`git merge-base --is-ancestor <rama> main`,
   `git rev-list --count main..<rama>`, y un grep del símbolo que la HU introdujo).

Y al revés: **cerrar una HU incluye volver acá**. Una fila que nadie actualiza al mergear
es la forma normal en que esto se rompe: cinco de las siete filas corregidas en esta
auditoría (`159`, `160`, `161`, `162`, más `189`) estaban mergeadas y nadie tocó su estado.

**Qué corrigió la auditoría del 2026-07-29** (siete filas): `007` (era `WIP`, duplicada por
la fila `007` de abajo), `159`, `160`, `161`, `162` (eran `in progress`, están mergeadas,
con su commit de merge citado), `163` (era `in progress` y no lo está: sin rama en ningún
lado, sólo work-item) y `189` (el caso de arriba).

**Qué NO se pudo verificar desde git, y por eso quedó como estaba**: las filas con
`PENDING-DEPLOY` / `UPGRADE-PENDING` / `ACTIVATION-PENDING` (`096`, `167`, `168`, `169`,
`170`, `171`, `173`, `174`, `175`, `177`, `178`) y los pendientes de ops de `190` (WKH-306,
control diferido con las envs sin setear) y `209` (WKH-307b, migración a prod). En todas,
la mitad de código está en el repo y es verificable; la mitad que falta vive en Railway,
en Vercel y en las bases (`bdwv` / `caldz`), no en git. Verificarlas exige mirar el entorno,
no el árbol. Las filas `136` (BACKLOG) y `141` (DEFERRED) son estados deliberados, no
desactualizados.

*Nota de formato, sin cambiar*: la fila `155` (WKH-115) tiene dos `|` literales dentro de
un bloque de código en su descripción (el lifecycle `settled|rejected|failed`), así que la tabla le
cuenta columnas de más y el Markdown renderizado muestra el tipo donde va el status. El
estado real de esa fila es `DONE`. No se tocó para no reescribir texto ajeno; queda
anotado para quien lo arregle con el árbol quieto.
