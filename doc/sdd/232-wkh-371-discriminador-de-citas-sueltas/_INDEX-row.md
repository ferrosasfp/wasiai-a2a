# Fila para `doc/sdd/_INDEX.md` — WKH-371 (fila 232)

⚠️ **No pude insertarla yo**: este `nexus-analyst` corrió **sin herramienta `Edit`** y el
archivo (~90.653 tokens) excede el límite de `Read`/`Write` en una pasada, así que
reescribirlo entero habría sido destructivo.

## Dónde va, exactamente

**Insertar como línea nueva 224**, inmediatamente **después** de la línea 223 (la fila `231`
de WKH-370) y **antes** de la línea en blanco que precede al `---` de la línea 225.

⛔ **NO se toca NADA por encima de la línea 144** — `src/services/capability-risk.ts` cita esa
región y desplazarla rompe una cita del lado del código (CD-9 del work-item). Una inserción
al final del cuerpo de la tabla no desplaza nada de arriba.

⚠️ Tras insertarla, **re-correr el gate completo con `git add -A` antes**: hay guardianes que
cuentan filas y directorios de `doc/sdd/` (`test/sdd-index-matches-folders.test.ts`,
`test/readme-numbers.test.ts`, que enumera con `git ls-files`, o sea contra el ÍNDICE).

---

## La fila

```
| 232 | 2026-08-28 | [WKH-371] Las citas sueltas (`` `:60` ``, `:208` — el número sin el archivo al lado) se pudren en silencio, y **el instrumento que las cuenta cuenta puertos** (issue #178). **La premisa del issue está medida con un instrumento roto y corregirla es el punto de la HU**: el «8501 tokens sueltos» (666 + 7835) sale de un `:[0-9]+` que también caza `localhost:3001`, `eip155:43113`, `1:50`, `T00:00:00`, `{reputation:100}` y `minLength:1`. El propio `_INDEX.md` ya registraba que el número **depende del patrón** (`:218`, fila 226: *«666/7835 con rangos, **592/4222** con `` `:N` `` a secas»*), y re-medido hoy **depende también del ALCANCE, que nadie declaró**: `src+test+scripts` da ~1470 sueltos y agregando `doc/` da ~14772 — un orden de magnitud. ⇒ **EL PRIMER ENTREGABLE NO ES EL ARREGLO NI EL CONTEO: ES EL DISCRIMINADOR.** Hasta que exista una forma de separar una cita de un puerto, todo número sobre esta deuda —incluido el del issue— es una afirmación sin instrumento, y un plan dimensionado sobre un número falso se rompe al primer contacto. **El defecto SÍ es real y está caracterizado**: `test/cited-lines-guard.test.ts` deja la forma suelta fuera de su universo **por construcción** —sin el nombre del archivo `citePathOf` devuelve `null` y el cruce mecánico `citeMatchesTarget` es vacuo—, y su propio docblock lo declara dos veces (punto 2, *«no hay cota superior conocida»*, `:118-123`; punto 14, **102 de sus 261 tokens son `:N` sueltos**, `:182-183`) ⇒ **el guardián de citas es el mayor productor de citas sueltas del repo**. **Severidad medida, no supuesta** (HU 231, `auto-blindaje.md`): con dos citas apuntando a `:99999`/`:99998` en un archivo de 950 líneas, **`npm test` completo sale VERDE** — no hay rojo que confirmar porque no hay guardián. Costo de la semana: **1 BLOQUEANTE y 5 MENORes** en las HUs 230/231, **7 citas rotas** en los README (cinco **corridas**, no mal escritas), y un `BLQ-2` nacido de **sumar un delta (`+64`) en vez de abrir la línea (`+78`)**. 🎯 **EL DISCRIMINADOR QUE PROPONE EL F1, con su calibración medida a mano**: cascada de 4 reglas sintácticas + 1 de contexto, con residuo `INDECIDIBLE` de primera clase — **D1** el carácter previo al `:` no puede ser `[A-Za-z0-9_]` (mata puertos, chain IDs, ratios, timestamps y propiedades); **D2** no puede ser comilla (mata `{"k":1}` y los valores del campo `cite:`); **D3** el párrafo tiene que nombrar un archivo **que esté en el ÍNDICE DE GIT**, reusando `citeTargetIfTracked` que **ya existe** (`scanner.ts:251-267`); **D4** excluye los tokens que son DATO de un registro (ya tienen testigo `G-C5`/`G-C7`). Calibrado a mano sobre `test/cited-lines-guard.exceptions.ts` (279 líneas, **30 tokens P3/P4 etiquetados uno por uno**: 11 CITA · 12 RUIDO · 6 DATO · 1 ILUSTRATIVO): **hoy 11/30 = 37 % de precisión; con la cascada completa 11/11**. ⚠️ **Ese 100 % es la señal de que la medición NO vale**, no de que la regla sea buena: las reglas salieron de ese mismo archivo ⇒ es **CALIBRACIÓN, no precisión**, y **AC-2 lo vuelve un requisito bloqueante** (muestra reservada, etiquetada antes de correr el clasificador). **Los dos falsos positivos que valen, medidos**: (a) 🔴 **los backticks NO discriminan nada** — `exceptions.ts:47` escribe *«un `:336` que cita una línea de un `:8443` que es un puerto»* y `:226` tiene cuatro puertos más backtickeados, o sea con la forma P3 más «de cita» que existe; (b) sin la condición de git, los 3 tokens de ruido de `:237` sobreviven porque el párrafo nombra `https://x.io:8443/y`. **Falso negativo declarado y NO medido (MI-5)**: un párrafo que nombra el archivo **sin** `:N` — y la sospecha viene de un dato medido, `src/adapters/chain-resolver.ts` tiene **CERO tokens `:N`** y aun así cita otros nueve archivos, **todos a nivel de ARCHIVO** ⇒ existe una **QUINTA forma que no puede pudrirse** y que abre un tercer arreglo (DT-4 opción C: **borrarle el número**, el único con costo de mantenimiento cero). **PERÍMETRO RECOMENDADO, con su razón**: `src` + `test` + `scripts` entran al arreglo; **`doc/` se MIDE (AC-3, obligatorio) y NO se toca**, porque ahí una cita suele ser **REGISTRO HISTÓRICO y re-anclarla la vuelve FALSA** — con dos precedentes escritos en este mismo índice: la fila 226 declara que sus citas numeran el árbol previo y que el `throw` de `compose.ts:1757` **ya no existe**, y la fila 224 registra **9 tokens `.gitignore:172`** cuyo sentido entero es estar históricamente mal. ⚠️ Y `doc/` **sí es alcanzable** (el `.gitignore` excluye archivos de `doc/sdd/**` de a uno, `:183-193`) ⇒ **no barrerlo es una DECISIÓN y va escrita**, no un accidente. **¿CONVERTIR O ACOTAR? Recomendación: ACOTAR (B) para las inequívocas · BORRAR EL NÚMERO (C) donde la prosa no lo necesita · CONVERTIR (A) SÓLO las ambiguas**, que son las peligrosas y las pocas — porque convertir todo *«reproduce la enfermedad»*: alarga líneas, Biome las reparte, y eso desplaza lo de abajo (la 231 re-derivó **cuatro veces**, la última por un párrafo de prosa). **DT-6**: un párrafo que nombra **más de un** archivo trackeado ⇒ `INDECIDIBLE`, nunca se resuelve solo — **es literalmente el caso del issue** (se aplicó el desplazamiento `+29` del archivo equivocado en vez de `+7`). **PARTIDA en dos cortes**: A = discriminador + medición sobre muestra reservada + censo del perímetro (**cierra solo y ya vale**: convierte «8501» en un número con instrumento); B = el guardián + la corrección, **en commits separados** (CD-2: una parte grande ya estaba podrida ANTES, y atribuirle a este trabajo un daño preexistente apaga la búsqueda de la causa real). **Modo QUALITY CONFIRMADO con razones propias, no por deferencia a `CLAUDE.md`**: (1) el entregable es un INSTRUMENTO y el anterior ya mintió — un clasificador mal calibrado es **peor que ninguno**, porque publica una lista que se lee como cerrada; (2) el entregable es un GUARD y este repo tiene historial medido de guards vacuos (fila 224, con **uno defectuoso con nombre todavía vacuo en `main`**); (3) el arreglo toca comentarios adentro de archivos del camino del dinero y la prosa desplaza igual que el código — criterio de la fila 230: *«lo que decide el modo es la ubicación del arreglo, no la severidad del defecto»*. **11 CDs**, entre ellos: ⛔ ningún número sin su perímetro Y su patrón; ⛔ deuda vieja y nueva nunca en el mismo commit; ⛔ ningún control que se lea a sí mismo; ⛔ nunca descartar por RANGO del número; ⛔ nunca sumar un delta, **abrir la línea**; ⛔ ninguna línea de código ejecutable en el diff de `src/` (100 % comentario); ⛔ ninguna lista de excepciones volcada desde la salida del clasificador; ⛔ **no tocar `_INDEX.md` por encima de la línea 144** (`capability-risk.ts` la cita). ⚠️ **Este F1 corrió SIN SHELL** (sólo `Read`/`Write`/`Glob`): **no se pudo leer el issue #178** (MI-1, primera tarea de F2), **no se corrió ni un solo barrido** ⇒ **ningún conteo es exhaustivo** y la única medición propia es el etiquetado a mano de 30 tokens en 1 archivo (MI-2); cada cita va marcada `[MEDIDO-F1]`, `[HEREDADO]` o `[NO MEDIDO]`; y **el nombre de la rama es una propuesta sin verificar** (MI-6). ⚠️ Y el work-item **se declara a sí mismo registro histórico** (CD-5): numera el árbol de F1 y **no se re-ancla**. | tooling/test | QUALITY | in progress (F1 · sólo `work-item.md`) | feat/232-wkh-371-discriminador-de-citas-sueltas ([work-item.md](232-wkh-371-discriminador-de-citas-sueltas/work-item.md)) |
```
