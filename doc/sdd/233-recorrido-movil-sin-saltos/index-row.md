# Fila para `doc/sdd/_INDEX.md` — HU 233 (WKH-372) · **revisión 2, 2026-08-31**

⚠️ **Este archivo NO es el índice.** Es el reemplazo listo de **la línea 225** de
`doc/sdd/_INDEX.md`, que es donde vive hoy la fila 233 (la agregó el orquestador).

## Por qué la fila viene acá y no aplicada

⛔ **Esta corrida tuvo `Read` / `Write` / `Glob` y nada más: sin herramienta de edición parcial.**
Aplicarla obligaba a **reescribir `_INDEX.md` entero (27.420 tokens)**, y eso es exactamente la
operación que **`CD-10` prohíbe**: `src/lib/capability-risk.ts:82` y
`src/lib/capability-risk.test.ts:56` citan `_INDEX.md:144`, y **mover cualquier línea por encima de
la 144 la rompe en silencio**. Una re-transcripción de ese tamaño no puede garantizar que no se
mueva nada.

⇒ **La fila va acá, y la aplica el orquestador.** Es el mismo procedimiento que ya usó la HU `071`
de `chaski-v3` con su `index-row.md`.

## Cómo aplicarla

1. **REEMPLAZAR la línea 225** de `doc/sdd/_INDEX.md` por el bloque de abajo.
2. ⛔ **Una línea por una línea.** No insertar ni borrar líneas: la fila nueva es **una sola línea
   física**, igual que la vieja.
3. ⛔ **Nada por encima de la línea 144** (CD-10).

**Columnas** (7, en este orden): `# · fecha · descripción · tipo · modo · estado · rama/enlaces`.

---

```markdown
| 233 | 2026-08-31 | [WKH-372] El recorrido móvil sin saltos: rediseño de la capa de firmas de Chaski — **HU paraguas, `[REVISIÓN 2 · 2026-08-31]` de 5 olas a 4: W0 medición + W1, W3, W4. Trabajo casi todo en `chaski-v3`, una pata condicional en `wasiai-facilitator`, y en `wasiai-a2a` CERO líneas de `src/`**. Hoy mandar una remesa desde el navegador de un celular cuesta **6 saltos a la billetera, 5 firmas y atravesar la pantalla de entrada 7 veces**, y cada firma cuesta **dos toques** porque la vuelta remonta React y aterriza en la bienvenida con un enlace que hay que tocar a mano. 🎯 **El hallazgo que define el alcance**: ni el Coordinador ni ninguno de los 3 agentes pide jamás una firma de billetera — los tres reciben strings; de los 6 saltos, 1 lo impone Solana, 1 el protocolo de la billetera y **4 los escribimos nosotros** ⇒ la capa de firmas es rediseñable **sin tocar el contrato con el Coordinador ni con los agentes** (CD-1). 🔻 **LO MÁS IMPORTANTE DE ESTA REVISIÓN: la ola W2 («una sola transacción con el gas patrocinado») SE RETIRA porque ERA OTRA HU** — la **`071` de `chaski-v3`** (`doc/sdd/071-facilitator-adelanta-el-alquiler/`), que ya tiene `work-item.md`, `sdd.md` de 159 KB en **revisión 2** y **dos rondas de AR** encima. La revisión 1 la re-derivó desde cero porque **`chaski-v3/doc/` está gitignoreado** (`.gitignore:36`) y **`grep` da CERO falso sobre él** ⇒ una HU entera invisible. **Se deja el HUECO en `W2` y NO se renumera** (DT-9): renumerar rompería todas las referencias externas ya publicadas a `W3`/`W4` —esta misma fila las nombra por número—, dejar el hueco rompe cero. Lo que W2 no veía y hace que no pueda vivir en una ola de UX: el guard anti-drenaje `Check 5` del facilitator **rechaza el 100 %** de las tx que eso produce y **nombra textualmente al rent-payer como vector de drenaje**; el alquiler tiene que **volver a quien lo puso**, grabado on-chain, o es un desagüe por el camino feliz; **hay que cobrarlo en USDC** (decisión del founder: *«no podemos o debemos patrocinar SOL gratuitamente, pq el negocio se vería impactado»*) y eso vive en el agente de FX, fuera de los tres repos; y a SOL cero **el remitente no puede pagar su propio refund**. 🔴 **OBJETIVO DE ÉXITO CORREGIDO, y es la otra corrección grande**: la métrica **«SOL exigido: 0» NO la entrega esta HU** y la frase *«el remitente no necesita SOL»* tiene **PROHIBIDO** aparecer como resultado suyo (**CD-12**, nueva). Lo que W1 entrega por sí sola es la eliminación de la **cuenta de nonce** —**1.527.680 lamports**, de los cuales 1.447.680 son su alquiler (`solana-escrow-rent.ts:332`)— **por inalcanzabilidad**, sin escribir una línea para eliminarla: la rama está gateada por `if (this.firmaPorEnlace && this.caminoPorEnlace() !== null)` (`solana-wallet.ts:897` [MEDIDO]). ⇒ **10.402.240 → 8.874.560 lamports, y ahí se termina lo que esta HU puede**; los 8.874.560 los lleva a 0 la `071`. 🔄 **W4 REESCRITA: es una DECISIÓN DE RIESGO, no un bloqueo técnico.** `M-3` estaba mal planteada — **el facilitator YA verifica la firma del depósito server-side en cada pedido**: `sponsor-claims.ts:169` corre `tx.verifySignatures(false)` y `:155-163` deriva el `sender` de la ix `deposit`, nunca del body [MEDIDO] (la revisión 1 citaba `sponsor-pop.ts:39-43`, que es el **docblock** que lo declara, no el código que lo hace). Lo que el mensaje aparte aporta es **CONSENTIMIENTO, no posesión**, con esa palabra en `routes/solana-sponsor.ts:28-31` [MEDIDO]; **no tiene `exp` ni anti-replay propios** (son 7 líneas y ninguna es tiempo ni nonce, `sponsor-pop.ts:63-65`) aunque **sí está atado a esa tx** por su última línea. **Lo que se pierde al borrarlo, acotado**: un tercero con la tx capturada le hace **pagar el gas** al facilitator por un depósito no autorizado — no consigue plata de la víctima, ni otro monto, ni otro destino (el monto vive adentro de lo firmado; medido en el repo: subirlo de `10000000` a `1000000000` da 403 sin que Guard B participe). ⇒ **W4 puede cerrarse HOY en cualquiera de las dos direcciones, sin código**, y lo que necesita es **un dueño** (`MI-9`, abierto). ✅ **`MI-2` CERRADO por decisión del founder**: *«si el usuario no tiene Phantom se le pide instalar y crear su wallet»* ⇒ `AC-1-4` reescrito + `AC-1-4b` **nuevo** para el borde que la frase no cubre (qué pasa con el envío a medio cargar cuando vuelve de instalar; ⚠️ **no se puede cerrar por lectura**: depende de que el `localStorage` sobreviva al salto, una de las cuatro cosas que `solana-wallet.ts:894` declara SIN VERIFICAR). 🔻 **`M-2` retirado** (existía sólo para gatear W2; su criterio —el delta de balance en `0`— **no se pierde**: es la señal del paso 6 del runbook de la `071` y su `AC-13`). **W3** sesión server-side: las dos firmas de identidad prueban lo mismo y existen porque la app no tiene sesión (`app/api/payout/prepare/route.ts:183-190`, la ruta era *«un oráculo de existencia y estado de verificaciones de identidad ajenas»*) — 🔴 **la sesión NO depende de que Phantom soporte SIWS por enlace**; SIWS sólo decide si conectar+firmar son **un** permiso o **dos** ⇒ dos ACs separados. **Costo que la HU corrige de paso**: el camino por enlace corre `prepare()` **3 veces por envío** ⇒ 3 invocaciones pagas del agente de cash-out y **2 órdenes de payout huérfanas**, y cada reanudación **agranda de forma monótona** el conjunto de destinos que acepta el guard S3.5 del settle (`confirm-and-send.ts:437-443` y `:458-462`) — es aritmética de los saltos, **no del contrato A2A**, y lo cierra W1. 📋 **ORDEN CONSOLIDADO entre las dos HUs (§0 del work-item)**: **(1)** W1 de esta HU, sólo cliente y sin dependencias → **(2)** la HU `071`, tres repos y upgrade del programa → **(3)** W3 → **(4)** W4. ⚠️ **Verificado: la ola 1 NO le cambia NADA a la `071`** —ni un AC, ni un DT, ni una CD, ni un ítem de wave— **y tampoco le saca trabajo**: su `AC-15` y su ítem 3.8 (el copy del camino por enlace) siguen enteros **porque CD-3 deja ese camino encendido**. Lo único que cambia es la **exposición**: el residuo de 0,0016 SOL pasa a afectar sólo al respaldo. **Lo que sí obliga a coordinar**: W1 edita `solana-wallet.ts` alrededor de `:897`, y ese bloque declara **76 citas ancladas por debajo** ⇒ si W1 mergea primero, **la revisión 3 del SDD de la `071` re-deriva sus citas de `:1400-1420` y `:1640-1660`** (asimetría deliberada: esta HU **no** re-ancla, es registro histórico CD-9; la `071` **sí**, porque sus citas son un plan de trabajo). **Scope OUT nuevo y el más importante**: que el remitente no necesite SOL. **Residuo declarado con nombre**: `TD-372-ATA-DEL-SENDER` — la creación idempotente de la ATA del sender **no la absorbe la `071`**, que la declara **precondición** de su `AC-13` (`Deposit.sender_ata` es `mut` sin `init` ⇒ una billetera con 0 lamports y sin ATA revierte con **3012**); se escribe para que no se vaya en silencio al borrar W2. **13 CDs** (una retirada, dos nuevas): **CD-12** ⛔ esta HU no toca el VALOR de ninguna constante de umbral de SOL, no patrocina alquiler y no dice *«el remitente no necesita SOL»*; **CD-13** ⛔ prohibido re-derivar el diseño de la `071` desde este repo, y **para encontrarlo hay que saber que su `doc/` está gitignoreado**; **CD-7** el corte de 8 días vuelto regla (el receptor se despliega primero, aceptando las DOS formas); **CD-10** ⛔ nada por encima de la línea 144 de este índice. ⚠️ **Esta revisión corrió SIN SHELL igual que el F1** (sólo `Read`/`Write`/`Glob`): cada cita va marcada `[MEDIDO-F1]`, `[MEDIDO-R2]`, `[HEREDADO]` o `[NO MEDIDO]`, **ningún conteo es propio** y **el nombre de la rama sigue sin verificar** (MI-6). | feature/ux/money-path/cross-repo | QUALITY | **F1 revisión 2 — work-item desolapado, esperando `HU_APPROVED`.** ⛔ Sin código. **Missing Inputs**: `MI-2` **cerrado** por el founder · `MI-4` **movido fuera** con W2 · **`MI-9` nuevo y abierto** (quién firma la decisión de riesgo de W4, bloquea su cierre) · `MI-1`/`MI-3` se resuelven en W0, que sólo tiene **M-1** vivo y exige el teléfono del founder con **Testnet Mode** activo. Sizing **M-L** (baja de `L`: se fue la ola de los tres repos). **DT-8: un SDD por ola, no uno para las tres.** | `feat/233-wkh-372-recorrido-movil-sin-saltos` — **propuesta sin verificar** ([work-item.md](233-recorrido-movil-sin-saltos/work-item.md) · [index-row.md](233-recorrido-movil-sin-saltos/index-row.md)) |
```

---

## Lo que cambió respecto de la fila vieja (línea 225), para que se pueda verificar

| # | Cambio | Motivo |
|---|---|---|
| 1 | «5 olas» ⇒ **4** (W0 + W1 + W3 + W4), con el **hueco de W2** dicho y no disimulado | W2 era la HU `071` |
| 2 | Se saca la descripción entera de W2 (ATA idempotente, 1232 bytes, `ASSOCIATED_TOKEN_PROGRAM_ID`) | Ídem |
| 3 | Se saca *«M-2 ¿el relayer cubre el ALQUILER o sólo la comisión? gatea W2»* | `M-2` retirado |
| 4 | **M-3 reescrita**: de *«¿puede verificar server-side?»* a **decisión de riesgo** | La capacidad no falta y está medida |
| 5 | **Cita corregida**: `sponsor-pop.ts:39-43` ⇒ `sponsor-claims.ts:155-163` + `:169` | La vieja apuntaba a un docblock |
| 6 | **Objetivo corregido**: aparece el techo de **8.874.560 lamports** que esta HU no baja | CD-12 |
| 7 | `MI-2` pasa de bloqueante a **cerrado**, con la frase del founder | Decisión D-3 |
| 8 | **Nuevos**: el orden consolidado, la verificación de que W1 no cambia la `071`, las 76 citas ancladas, `TD-372-ATA-DEL-SENDER`, `MI-9` | Revisión 2 |

⚠️ **Lo que NO cambió y hay que dejar igual**: el `[WKH-372]`, el número `233`, el tipo
(`feature/ux/money-path/cross-repo`), el modo (`QUALITY`) y la rama propuesta.
La **fecha** se mantiene en `2026-08-31` porque la fila original ya era de ese día.
