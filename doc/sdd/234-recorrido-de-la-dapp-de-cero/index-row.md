# Fila para `doc/sdd/_INDEX.md` — HU 234

## ⛔ Por qué no la escribió el Analyst

Este F1 corrió **sin herramienta de edición parcial** (sólo `Read` / `Write` / `Glob`). Reescribir
`_INDEX.md` entero para agregar una fila viola **CD-10**: prohibido cambiar nada por encima de la
línea 144, porque `src/lib/capability-risk.ts:82` cita `_INDEX.md:144` **por número**.

## Dónde va, exactamente

- La tabla termina en la fila **233**, que ocupa la **línea 225** `[MEDIDO]`.
- La **línea 226 está vacía** `[MEDIDO]`.
- La **línea 227 es el `---`** que abre la prosa del pie `[MEDIDO]`.

⇒ **Insertar la fila de abajo como línea 226**, empujando el resto hacia abajo. **Cero líneas
anteriores tocadas.**

## ⚠️ Se pone rojo solo si no se hace

`test/sdd-index-matches-folders.test.ts` deriva las carpetas de `git ls-files doc/sdd` y exige que
**cada carpeta de HU tenga exactamente una fila** (`_INDEX.md:240-242`). En cuanto
`doc/sdd/234-recorrido-de-la-dapp-de-cero/` entre al índice de git sin esta fila, `npm test` de
`wasiai-a2a` se pone **ROJO**.

## La fila (una sola línea, pegar tal cual)

```
| 234 | 2026-09-01 | [WKH-374] El recorrido de la DApp, rediseñado: de 8 pantallas / 6 salidas a la billetera a **5 pantallas / 2 salidas**. Pedido del founder, textual: *«rediseñes el viaje del DApp esta medio confuso … aun me pide firmar varias veces, hay varios botones que confunden … si es necesario has una DApp de cero, evalua»*, y después **«acepto la propuesta»**. El diagnóstico aceptado: **la interfaz es un museo de nuestros errores** — cada defecto de la semana agregó un botón o un aviso, y ninguno existe porque la persona lo necesite. **Trabajo todo en `chaski-v3`; en `wasiai-a2a/src/` CERO líneas** (CD-3/AC-15). Tres cambios: (1) **conectar es lo PRIMERO**, no el paso 3 (hoy `connect` va después de monto+beneficiario+CCI, derivado de `flow.tsx:88`); (2) **el envío vive en el SERVIDOR atado a la dirección** y el enlace de vuelta trae **sólo un identificador opaco**; (3) **salir a la billetera es un PASO, no un error**: se anuncia antes, se muestra mientras pasa, y al volver **se aterriza donde se estaba, un paso más adelante** — ⛔ nunca en la pantalla de entrada, tampoco en el camino de error (CD-5). 🔴 **LA DECISIÓN QUE MÁS CONDICIONA TODO, planteada acá y resuelta en F2 (DT-1)**: `flow.tsx` tiene **4453 líneas**, **Δ0 estricto** y **165 citas ancladas entrantes a 96 destinos** —**165, no 163**: el marcador `[[CENSO src/presentation/flow.tsx entrantes=165]]` de `flow.tsx:44` lo dice y el encargo traía el número viejo—, así que rediseñar adentro es pelear con el andamio, y hay evidencia mecánica de lo que eso produce: con Δ0 todo código nuevo se **encaja en líneas existentes** (`flow.tsx:162` mete un `useState` + un handler completo + ~1.500 caracteres de comentario en un renglón; `:163` y `:175` meten tres declaraciones cada uno) ⇒ **el Δ0 es un mecanismo que FUERZA prosa de producción**, que es exactamente donde el CR de la ola anterior midió el desborde (**61 % de prosa contra ~50 % de la casa, y los tres bloqueantes del CR vivían ahí**). Las tres opciones van con costo escrito; el Analyst recomienda **B = árbol propio detrás de bandera opt-in estricto** (patrón `mwaEnabled`/`deeplinkEnabled`, `wallet-availability.ts:98`/`:156`), única con **repliegue de un interruptor** sobre un camino que mueve plata y con **0 citas rotas** — ⚠️ **y con su costo dicho, no escondido: DOS CAMINOS QUE MANTENER A LA VEZ (R-1)**, la misma clase de deuda que ya mordió a este repo cuando el arreglo del reloj se hizo en `sesion-store.ts` y `pop-proof-store.ts` quedó abierto ⇒ **DT-1 debe venir con condición de retiro del camino viejo, no sólo con la bandera**. 🎯 **HALLAZGO DEL F1 QUE CORRIGE EL ENCARGO Y CAMBIA DÓNDE SE TRABAJA (DT-5)**: el encargo dice *«la sesión del servidor vive en memoria»* y **medido es al revés** — el token del servidor es **HMAC APÁTRIDA** atado a `address`+`networkId`+`exp` (`sesion-de-posesion.ts:95-106`), y lo que vive en memoria es **el almacén DEL CLIENTE**, por **tres razones deliberadas** una de las cuales es *«es una credencial al portador»* (`sesion-store.ts:16-25`) ⇒ **«persistirla» NO es mover el servidor a una base: es decidir si una credencial al portador puede tocar disco**, que es una decisión de seguridad y un **foco obligatorio de AR**. La salida propuesta es **DT-3: el vale de vuelta NO es la sesión** — credencial opaca, un solo uso, TTL corto, con exemplar ya en producción en ESTE repo (`a2a_agent_links`/`claim_agent_link`, fila 225). 🔴 **Y LA TENSIÓN QUE F2 TIENE QUE DESATAR (DT-4)**: leer un borrador *atado a una dirección* exige autenticar esa dirección, y la credencial que lo haría es justo la que **no sobrevive al salto** — es circular, y DT-3 es lo que la corta. 🔴 **LA LÍNEA DE BASE NO SE COPIA, SE DERIVA EJECUTANDO, y hoy DOS TERCIOS de la métrica NO son derivables**: **8 pantallas** sí está medido (`STEP_INDEX`, `flow.tsx:90-99`), pero **«6 saltos», «5 firmas» y «7 travesías de la pantalla de entrada» son `[HEREDADO]`** — el instrumento que cuenta travesías **ya existe y tiene su definición escrita** (*«travesías = 1 + asignaciones de `window.location.href`»*, `recorrido-en-el-navegador-de-la-billetera.test.tsx:676-685`) pero **sólo se corrió en el cuadrante `injected`**, donde da recurrente **1** / primera vez **2** (`it("T-372-W1-13: …")`, `:707`); **nadie lo apuntó nunca al camino por enlace**. ⇒ **W0 lo apunta ahí, y sin W0 todo AC que compare contra 6/5/7 es NO VERIFICABLE** — que es exactamente el defecto que dejó un AC muerto en la HU anterior. **Wave 0 de premisa falsable, 0 líneas de producción, 6 mediciones, y si una sale roja la ola SE DETIENE** (CD-4): las dos líneas de base, **la premisa L-5 de que el salto por enlace remonta el árbol de React** (hoy **doctrina heredada, NO una corrida** — si sale falsa, el diseño del identificador opaco no hace falta y el §2 entero cambia), que un archivo nuevo recibe **0 citas ancladas**, que la bandera apagada deja las pantallas **byte-idénticas** (mecanismo de `T-065-21`), y la de teléfono que **no se puede cerrar en `jsdom`**. **18 ACs en EARS**, con **AC-9b** que es la parte incómoda: **si la firma de patrocinio (W4 de WKH-372) no se elimina —decisión de riesgo del founder, NO TOMADA, sin dueño (`MI-9`)— el objetivo publicable son 3 salidas, no 2**, y se escribe así en la métrica. **AC-17/AC-18**: todo número va con etiqueta de confianza y ⛔ un número que no se pudo derivar se declara **NO VERIFICABLE** con esas palabras, nunca se redondea ni se copia. **Scope OUT con razón escrita**: la arquitectura A2A (⛔ **ni el Coordinador ni ninguno de los 3 agentes pide jamás una firma: reciben strings**), el escrow on-chain, el proveedor de KYC y el camino de pago, **el recorrido dentro del navegador de Phantom** —que cierra bien hoy y ⚠️ **no puede empeorar: por eso tiene CONTROL (AC-14 exige `T-372-W1-13` verde), no una promesa**— y **billeteras custodiales o embebidas** (el producto es no custodial y la pantalla lo dice: *«tu plata no pasa por Chaski»*). **14 CDs**, entre ellas **CD-7 = el corte de 8 días de agosto vuelto regla: el receptor se despliega PRIMERO, aceptando las DOS formas**; **CD-1** Phantom no empeora **con control**; **CD-6** el enlace de vuelta no transporta monto, beneficiario, CCI ni credencial al portador; **CD-12** ⛔ prohibido decir *«el remitente no necesita SOL»* (eso lo entrega la **`071` de `chaski-v3`**, que **espera el `HU_APPROVED` del founder** ⇒ esta HU **no puede prometer que «crear la cuenta» desaparece**, sólo que cuando esa HU entre desaparece sin tocar nada más); **CD-13** ⛔ prohibido re-derivar el diseño de la `071` desde acá, y ⚠️ para encontrarlo hay que saber que `chaski-v3/doc/` está **gitignoreado** y `grep` da **CERO falso** sobre él; **CD-14** el presupuesto de prosa. 📏 **PRESUPUESTO DE ESCALA CON LA LECCIÓN ADENTRO**: las dos olas anteriores se pasaron **2,21x** y **1,68x** y el CR midió que el exceso estaba **en los docblocks de PRODUCCIÓN, no en los tests** (ratio test-código **4,7:1**, *por encima* del piso de 4:1 que el SDD pedía) ⇒ **se presupuesta prosa APARTE de código**, con techo propio **≤45 %** (por debajo de la media de la casa a propósito, porque de esa media salió el 61 %), total **≤1.020 ejecutables / ≤760 comentario / ≤4.500 test / ≤32 archivos** repartidos en 4 olas, **un SDD por ola** (DT-8), y la pregunta escrita para el CR: *¿qué parte de esto seguiría existiendo si lo escribiera alguien que ya conoce esta librería?* **7 escenarios de despliegue a medias** analizados, con **M-4 marcado BLOQUEANTE de AR por adelantado** (persistir el token de sesión en disco sería abrir superficie nueva sobre el camino que autoriza el desembolso) y **M-7** = el arreglo que se hace en un solo camino de los dos. ⚔️ **Paralelismo**: con DT-1 = opción B esta HU **no toca `solana-wallet.ts`** y el conflicto con las **76 citas ancladas** que la `071` declara ahí **desaparece** — un argumento más a favor de B. ⚠️ **Precondición de arranque de W1**: `main` de `chaski-v3` estaba **pendiente de push** (`origin/main` en `3178360`, con el receptor de W3 y sin el cliente). ⚠️ **Este F1 corrió SIN SHELL** (sólo `Read`/`Write`/`Glob`): cada cita va marcada `[MEDIDO]`, `[HEREDADO]` o `[NO MEDIDO]`, **ningún conteo exhaustivo es propio**, **el nombre de la rama es una propuesta sin verificar** (MI-6) y **el ID `WKH-374` no se pudo verificar como libre** (MI-7). | feature/ux/money-path/cross-repo | QUALITY | F1 — esperando `HU_APPROVED` | feat/234-recorrido-de-la-dapp-de-cero *(propuesta sin verificar)* ([work-item.md](234-recorrido-de-la-dapp-de-cero/work-item.md) · [index-row.md](234-recorrido-de-la-dapp-de-cero/index-row.md)) |
```
