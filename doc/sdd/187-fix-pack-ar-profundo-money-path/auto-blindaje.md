# Auto-Blindaje — 187 / fix-pack AR-profundo P0 money-path (FIX 1-4)

## Iteración 4 (AR APROBÓ y F4 APROBÓ; el **CR RECHAZÓ**: 2 MAYOR + 10 MENOR — cierre de 2 MAYOR + 6 MENOR + 1 residuo)

### [2026-07-26 05:20] it4 CR-MAYOR-1 — el runbook mandaba setear 2 envs que a2a no lee, y una de ellas apaga el pre-check de fondos del leg mainnet
- **Error**: `doc/operations/mainnet-activation-runbook.md`, bloque `#### In wasiai-a2a-production (Gateway)` — el MISMO bloque que la it3 reescribió — seguía instruyendo `BASE_ENABLED=true` (**0 lectores en a2a Y en el facilitator**; el facilitator usa `BASE_SEPOLIA_ENABLED`/`BASE_MAINNET_ENABLED`, `wasiai-facilitator/src/chains/base.ts:114-123`) y `AVALANCHE_MAINNET_RPC_URL` (**0 lectores en el `src/` de a2a**; el rail `avalanche-mainnet` del gateway lee `AVALANCHE_RPC_URL` — `src/lib/downstream-payment.ts:72`, `src/adapters/avalanche/payment.ts:153`). La tabla de Prerequisites §3 tenía el mismo error pre-existente, con UN nombre por chain como si los dos servicios compartieran la env.
- **Por qué es de dinero, no de docs**: el nombre equivocado NO falla ruidoso. `RPC_ENV_BY_CHAIN['avalanche-mainnet']` queda vacío ⇒ el pre-check de balance del operador se saltea con `BALANCE_PRECHECK_SKIPPED` (`downstream-payment.ts`, paso 9 de `signAndSettleDownstream`, guard `if (!rpc)`) ⇒ **el leg de MAINNET firma sin verificar fondos**. Un operador que siguiera el runbook al pie de la letra creía tener el control puesto.
- **Causa raíz**: la it3 verificó las envs que ELLA escribió y no las que ya estaban en el mismo bloque; y el modelo mental "una env por chain" es falso — a2a y el facilitator son dos servicios con dos vocabularios (el `_MAINNET_` de Avalanche existe, pero es del facilitator: `wasiai-facilitator/src/chains/avalanche.ts:63`, y `scripts/activate-mainnet-downstream.sh:113` lo setea BIEN ahí y `AVALANCHE_RPC_URL` en a2a en `:106`).
- **Fix**: tabla §3 reescrita a **un nombre por SERVICIO** con archivo:línea del lector de cada lado (Kite y Base sí comparten nombre; sólo Avalanche mainnet diverge); `BASE_ENABLED` retirada y reemplazada por la verdad (en a2a Base se habilita con el SLUG en `WASIAI_A2A_CHAINS`; en el facilitator con `BASE_MAINNET_ENABLED`); `AVALANCHE_MAINNET_RPC_URL` → `AVALANCHE_RPC_URL` en el bloque del gateway (intacta en el del facilitator); catálogo de skip-codes de `downstream-payment.ts` ampliado para que `BALANCE_PRECHECK_SKIPPED` diga explícitamente que en mainnet significa "va a firmar sin verificar fondos" y nombre la env correcta.
- **Aplicar en**: cuando dos servicios comparten un runbook, la unidad de verificación es **(env, servicio)**, no (env). Y al corregir un bloque de envs, grepear TODAS las envs del bloque, no sólo las que uno agrega.

### [2026-07-26 05:22] it4 CR-MAYOR-2 — los dos rollbacks de emergencia producían el bundle Kite ROTO y cerraban afirmando que todo estaba bien
- **Error**: `doc/migration/MAINNET-ACTIVATION-RUNBOOK.md:206-207` (editado en la it3) y `doc/operations/mainnet-activation-runbook.md:490-491`: el rollback seteaba `KITE_NETWORK=testnet` y vaciaba el gate, pero **NO revertía `WASIAI_A2A_CHAINS`**, que el paso de activación había puesto en `kite-mainnet`. Resultado: `WASIAI_A2A_CHAINS=kite-mainnet` + `KITE_NETWORK=testnet` ⇒ `chainConfig.chainId=2366` / `getPaymentAdapter('kite-mainnet').chainId=2368` / `getToken()` = PYUSD **testnet** ⇒ `ADAPTER_CHAIN_ID_DRIFT`, el rail arranca roto. Es EXACTAMENTE el bundle que la it3 documentó como roto en la ACTIVACIÓN y que `src/adapters/__tests__/registry.test.ts` (T-it2-MNR-3-reg) pinnea. Y el doc cerraba con *"System reverts to testnet-only behavior"*: **el procedimiento de emergencia dejaba el sistema peor y decía que estaba bien.**
- **Causa raíz**: la it3 trató el acoplamiento `WASIAI_A2A_CHAINS` ↔ `KITE_NETWORK` como una propiedad de la ACTIVACIÓN. Es una propiedad del PAR de envs, así que aplica en las dos direcciones. Lo mismo pasó con el alcance del barrido: se arregló el bloque de activación y no el bloque de rollback, 130 líneas más abajo del mismo archivo.
- **Fix**: los dos rollbacks revierten ahora `WASIAI_A2A_CHAINS` al CSV testnet **junto con** `KITE_NETWORK`, con las dos direcciones de la falla parcial explicadas (sólo `KITE_NETWORK` ⇒ rail roto; sólo el CSV ⇒ `initAdapters` LANZA) y un paso 4 de **verificación** obligatorio (`Adapters initialized` sólo con slugs testnet **y** cero `ADAPTER_CHAIN_ID_DRIFT`). La afirmación de resultado pasó a ser condicional a esa verificación. `MULTI-CHAIN.md` §8 — que documentaba la activación y **no** el rollback — tiene ahora su punto 4 "Rollback de Kite mainnet" como fuente única, y `HACKATHON-FINAL.md:263` (que ya lo decía bien) apunta ahí y agrega la verificación. Bonus del mismo bloque: la instrucción "(unset `MAINNET_ENABLED` flags if set)" se retiró — `MAINNET_ENABLED` a secas tiene 0 lectores en los dos repos; los reales son los tres `*_MAINNET_ENABLED` del facilitator.
- **Aplicar en**: (1) si dos envs están acopladas para activar, están acopladas para desactivar — documentar el par, no el paso. (2) Un rollback SIN paso de verificación no es un rollback: la afirmación "el sistema volvió a X" tiene que estar condicionada a una señal observable (una línea de log), nunca al hecho de haber corrido los comandos. (3) Barrer el archivo COMPLETO: el CR encontró esto porque la it3 arregló un bloque y no el contiguo.

### [2026-07-26 05:25] it4 — barrido COMPLETO de los 2 runbooks: 5 hallazgos más que ningún AR/CR había pedido
Al grepear TODA env/símbolo citado en los dos archivos (no sólo las líneas del CR) aparecieron 5 afirmaciones falsas más, todas del mismo tipo (instrucción que un operador ejecuta y que no hace lo que dice):
1. **`INBOUND_WEBHOOK_SOURCES`** (Step 1, operations): un JSON con 0 lectores en TODO el repo. La config real son envs por-campo-y-por-fuente (`INBOUND_SOURCE_SECRET_<S>`, `_A2A_KEY_`, `_MAX_BUDGET_`, `_DEFAULT_BUDGET_`, `_CHAIN_`; `src/services/inbound-task.ts:153-197`). Con el JSON seteado, `loadSourceConfig` devuelve `null` y la fuente entera queda rechazada. **Gotcha del sufijo**: `sanitizeSource` (`:101-103`) BORRA todo lo que no sea `[A-Z0-9_]` — no convierte `-` en `_` ⇒ `generic-bounties` → `GENERICBOUNTIES`.
2. **Headers del webhook**: decía `X-Webhook-Signature: hmac_sha256=...`; los reales son `x-wasiai-timestamp` + `x-wasiai-signature` con `HMAC-SHA256(secret, "<ts>.<rawBody>")` hex (`src/routes/inbound.ts:7-8`, `:69-70`).
3. **Step 4 en el servicio equivocado**: `HEALTH_MONITOR_TARGETS` / `ONCALL_MENTION` / `CRON_SECRET` / `MCP_ALERT_WEBHOOK_URL` decían "Set env vars in Railway `wasiai-a2a`" y tienen **0 lectores en el `src/` de a2a**: las lee `mcp-servers/wasiai-x402/` en **Vercel** (`api/cron/health-check.mjs:85`, `src/alerts.mjs`). Setearlas en el gateway = alerting P0 que nunca dispara, en silencio (el propio comando de verificación del Step ya apuntaba a `wasiai-x402-mcp.vercel.app`).
4. **Step 2 vendido como opcional siendo un gate de arranque**: `assertGasOverheadConfigured` (`src/lib/gas-overhead.ts:384-399`, llamada en `src/index.ts:55`) hace que en producción un chainId mainnet en `WASIAI_A2A_CHAINS` SIN `STEP_GAS_OVERHEAD_USD*` **no arranque**. ⇒ se documentó el ORDEN obligatorio (Step 2 antes del Step 5). Y la "Verification" nombraba una función inexistente (`composeFeeWithOverhead()`): el camino real es `getStepGasOverheadUsd` (`gas-overhead.ts:362`) desde `compose.ts:171`, con clamp `[0,$1]`.
5. **Hybrid mode escrito para un a2a mono-chain** (migration): decía "only ONE network active at a time per request" y ponía el override por header como "TD: future enhancement", y de ahí recomendaba **dos servicios Railway**. Falso desde WKH-MULTICHAIN: el CSV inicializa N bundles (`registry.ts:291-325`) y `x-payment-chain` existe. La excepción REAL es Kite (los dos rails no coexisten, `TD-NEW-KITE-PARAMS`) — se dejó explícita para no invertir el error.
- **Aplicar en**: el barrido de un runbook se hace por **extracción mecánica** de todos los identificadores tipo env (`grep -oE '\b[A-Z][A-Z0-9_]{4,}\b'`) y grep de cada uno contra el `src/` del servicio al que el doc dice que pertenece. Leer el archivo "con atención" no alcanza: 4 de estos 5 estaban en secciones que nadie había marcado como sospechosas.

### [2026-07-26 05:28] it4 CR-MNR-1 — dos opcionales independientes hacían REPRESENTABLE otra vez el bug que FIX 3 arregló
- **Error**: `DownstreamResult.settleCaip2?` y `.settledAmountUsd?` eran dos opcionales SUELTOS. La rama Solana los poblaba siempre juntos y la EVM nunca, pero el tipo admitía el tercer estado, y `compose.ts` decidía por el PRIMERO (`if (ds?.settleCaip2)`) y tomaba el monto del SEGUNDO con `?? stepDebitedUsd`. Un rail futuro que poblara sólo el CAIP-2 escribía `amount_usd = 0` al lado de una `settle_signature` on-chain REAL, en silencio: el bug exacto de FIX 3, reintroducible por tipos.
- **Fix**: un solo campo anidado `nonEvmSettle?: { caip2: string; amountUsd: number }` (`downstream-payment.ts`). El estado inválido no compila y **el `??` desapareció** del call-site. Tests: `T-FIX3a` ahora assertea el objeto COMPLETO con `toEqual` (antes dos asserts independientes, que era el mismo error en el test) y se agregó `T-CR-MNR-1` (leg EVM ⇒ `nonEvmSettle` undefined). Los 4 fixtures de `compose.test.ts` ya no pueden representar "CAIP-2 sin monto".
- **Aplicar en**: si dos campos opcionales SIEMPRE van juntos, el tipo tiene que decirlo (objeto anidado o unión discriminada). Regla operativa: **un `??` sobre un campo opcional cuya presencia decide otro campo opcional es un olor a bug de dinero** — el fallback nunca se ejercita en los tests y espera el primer productor que se desalinee.

### [2026-07-26 05:30] it4 CR-MNR-4 — elegí (a) para el cast y (b) para el docstring; el zero-address queda como TD y NO se toca
- **Hallazgo del CR**: `validatePayTo` se exportó con un docstring que declaraba un consumidor **inexistente** ("la comparten este gate y el sign INBOUND de `compose.invokeAgent`"). `compose.ts` importa `isValidWallet`, no `validatePayTo`; 0 importadores externos.
- **Decisión (mixta, con motivo)**:
  - El objetivo REAL de la opción (a) —**eliminar el cast crudo del sign inbound de `compose.ts`**— se logró sin pasar por `validatePayTo`: `isValidWallet` ahora narrowea a `` `0x${string}` `` en vez de a `string` (`wallet-format.ts`, cambio SOLO de tipos: el template literal es asignable a `string`, así que los otros consumidores compilan byte-idénticos) y `compose.ts` conserva el resultado tipado en `payToEvm`. El `payTo as \`0x${string}\`` se borró.
  - El resto de (a) —**usar `validatePayTo` en el guard inbound**— se DESCARTÓ y se reportó, porque cambia comportamiento observable del gate: `validatePayTo` rechaza la zero-address y el guard inbound no, así que un agente con `payTo=0x0…0` pasaría de "firma" a "skip". Ese gate lo aprobaron AR+F4 en ESTE diff y la instrucción era parar antes que cambiarlo.
  - Se aplicó (b) para el docstring: `validatePayTo` **des-exportada** (queda interna, único call-site en el paso 5 del path EVM) y la frase falsa reemplazada por la asimetría REAL documentada — downstream rechaza zero-address (fondos del operador), inbound no.
- **La asimetría quedó registrada como `TD-INBOUND-ZERO-PAYTO`** (`MULTI-CHAIN.md` §10) con su trigger: antes de cualquier activación mainnet del rail inbound, o en cuanto se toque el guard por otro motivo. Es un fix de una línea + tests.
- **Aplicar en**: un `export` cuya justificación es un consumidor que hay que ir a verificar es deuda: si el consumidor no existe, la frase envejece a mentira y el símbolo queda como API pública sin dueño. Y: el objetivo "sacar el cast" casi nunca exige el refactor grande — muchas veces alcanza con que el VALIDADOR devuelva el tipo preciso.

### [2026-07-26 05:32] it4 CR-MNR-5 — el drift de config se logueaba con el código del gate de opt-in: dos incidentes distintos en el mismo contador
- **Error**: el corte por incoherencia slug↔destino emitía `code:'MAINNET_NOT_ALLOWED'` + `reason:'CHAIN_ENVIRONMENT_DRIFT'`. Eso mezcla "un agente pidió una mainnet sin opt-in" (esperable, sano, agent-driven) con "NUESTRA config apunta a un destino que contradice su slug" (bug de operación) — y el segundo incluye el caso `declared=mainnet/actual=testnet`, donde permitir mainnet no tiene nada que ver. Un dashboard que cuenta `MAINNET_NOT_ALLOWED` sumaba los dos. Además `DownstreamSkipCode` NO incluía `BALANCE_LOW_ON_IDEMPOTENT_REPLAY` ni `BALANCE_PRECHECK_SKIPPED`, que SÍ se emiten en el campo `code`, y el catálogo del docstring omitía el corte por drift.
- **Fix**: `CHAIN_ENVIRONMENT_DRIFT` es ahora código propio; los dos códigos de observabilidad entraron al tipo en una sección aparte ("NO cortan el leg") con su condición de emisión; el catálogo del docstring lista los 3 y advierte que `BALANCE_PRECHECK_SKIPPED` en mainnet = firma sin verificar fondos; y el `reason` del guard `inboundVmUnsupported` de `compose.ts` unificó `reason:'PAY_TO_FORMAT'` → `'INVALID_PAY_TO_FORMAT'`, el mismo nombre que el skip-code del downstream para la MISMA condición. Test nuevo `T-CR-MNR-5`: chainId desconocido bajo slug **mainnet** ⇒ `MAINNET_NOT_ALLOWED`/`NO_OPT_IN` y **NO** `CHAIN_ENVIRONMENT_DRIFT` — el par que prueba que los dos códigos no son sinónimos (`T-it2-ALTO-1d` cubre el mismo chainId bajo slug testnet ⇒ drift).
- **Aplicar en**: un skip-code se comparte sólo si el INCIDENTE es el mismo. Regla: si dos caminos difieren en "quién tiene la culpa" (el caller/agente vs nuestra config) o en "admite opt-in vs no lo admite", son códigos distintos. Y el tipo de skip-codes se cierra grepeando `code:` en el módulo, no de memoria.

### [2026-07-26 05:34] it4 CR-MNR-3 — elegí PARTIR la función (opción a, con nombres honestos) Y documentar la simetría
- **Hallazgo del CR**: `assertChainEnvironmentCoherent` prometía "throw o nada" pero el camino REAL de Kite sólo logueaba; y el chequeo 1 lanzaba en AMBAS direcciones mientras el chequeo 2 lanzaba sólo en la peligrosa, con el párrafo "severidad POR DIRECCIÓN" scopeado únicamente al segundo. Leíble como inconsistencia ⇒ candidato a que alguien lo "arregle".
- **Decisión**: hice las DOS cosas que el CR ofrecía como alternativas, porque son complementarias. Partí en `assertNoSlugDestinationDrift()` (throw siempre) + `checkAdapterChainIdDrift()` (throw sólo en la dirección peligrosa, `log.error` en la otra) + un `checkChainEnvironmentCoherence()` de 2 líneas con la **tabla de severidades** que llama a los dos y es el único call-site de `initAdapters`. **No usé el nombre `reportAdapterChainIdDrift` que sugería el CR**: ese chequeo SÍ lanza en una dirección, así que "report" habría reintroducido el mismo defecto de naming en el otro extremo — `check` es honesto para "hace algo distinto según el caso".
- **Y documenté por qué el chequeo 1 es simétrico**, con dos razones independientes: (1) **el slug ES la llave del opt-in de dinero** (`WASIAI_DOWNSTREAM_MAINNET_ALLOW` es un CSV de slugs), así que un slug que miente rompe el significado del control del operador en las DOS direcciones — no sólo la que gasta: `mainnet→testnet` hace que el operador crea que autorizó dinero real y el rail settlee en testnet, o sea una activación mainnet que "funciona" sin haber tocado mainnet; el `chainConfig.chainId` del chequeo 2 no es llave de ningún opt-in. (2) **costo de disponibilidad cero**: ninguna config alcanzable produce el mismatch del chequeo 1 en la dirección segura (el registry le pasa `{network}` explícito a cada factory), mientras el chequeo 2 SÍ tiene un caso alcanzable ahí (`kite-mainnet` sin `KITE_NETWORK`) donde un throw sería un outage autoinfligido.
- **Aplicar en**: cuando dos guards vecinos tienen severidades distintas, la asimetría hay que **justificarla en el código** o alguien la va a normalizar. Y el prefijo del nombre es un contrato: `assert*` = throw-o-nada, `check*` = decide, `report*` = nunca lanza. Elegir el prefijo por lo que la función HACE, no por lo que uno querría que hiciera.

### [2026-07-26 05:36] it4 CR-MNR-7 — el mock afirmaba fidelidad al adapter real justo en los dos campos donde no la tenía
- **Error**: el mock del factory Kite en `registry.test.ts` modelaba `attestation.chainId` y `gasless.chainId` como network-dependientes (2366 en mainnet), pero los reales están **hardcodeados en testnet**: `kite-ozone/attestation.ts:8` → `readonly chainId = 2368`; `kite-ozone/gasless.ts:258` → `2368`, más `:60` (walletClient sobre `kiteTestnet`), `:70` (dominio EIP-712 con `kiteTestnet.id`) y `:275` (`network:'kite-testnet'`). No había falso verde ACTIVO (nadie assertea esos campos hoy), pero el comentario `:36-47` afirmaba fidelidad — o sea el próximo test que los asserteara habría "probado" que el rail mainnet los mueve.
- **Fix**: mock alineado al real (2368 fijo) + el comentario dice qué está hardcodeado y dónde. Y se agregó a **`TD-NEW-KITE-PARAMS`** (`MULTI-CHAIN.md` §10) la consecuencia que faltaba: **activar Kite mainnet — lo que el runbook ahora recomienda — NO mueve attestation ni gasless**, siguen en 2368.
- **Aplicar en**: un mock que dice "espeja el real" es una afirmación verificable: hay que grepear el real campo por campo antes de escribir esa frase. Un mock más "capaz" que el real da verde a configuraciones imposibles.

### [2026-07-26 05:38] it4 CR-MNR-8/9/10 + residuo — los baratos que evitan un falso verde
- **MNR-8** (`compose.test.ts`): 33 copias CRUDAS del payTo EVM (`'0xBBB…'`) mientras el equivalente Solana sí tenía constante. Con el guard de familia del FIX 4 eso es una trampa: `isValidWallet` exige EXACTAMENTE 40 hex, así que una copia con 39 o 41 `B` sigue siendo un string plausible pero cambia EN SILENCIO la semántica del test de "firma inbound" a "skip por `INVALID_PAY_TO_FORMAT`", y el test queda verde por el motivo equivocado. Extraído `const EVM_PAYTO = \`0x${'B'.repeat(40)}\`` (la longitud la calcula el runtime) y aplicado a las 33.
- **MNR-9** (`chain-resolver.ts`): el fail-open de `classifySolanaCaip2` estaba explicado pero no citaba `TD-SOLANA-CAIP2-DENYLIST` ni su condición de reactivación, a diferencia de `TD-NEW-KITE-PARAMS` (citada 5 veces). Agregadas las 3 condiciones que lo hacen aceptable hoy + el trigger exacto (existir `solana-mainnet` **o** salir de flag OFF ⇒ pasa a allowlist obligatoria).
- **MNR-10** (`docs/networks.md`): la viñeta del pre-check afirmaba que `INSUFFICIENT_BALANCE` hace que "**the request fails** before any signing happens". Doblemente falso: se saltea sólo ESE leg (`return null`) y **el request sigue OK** (el agente se invoca y no se le paga); y el pre-check sólo corre si hay RPC env **y** `OPERATOR_PRIVATE_KEY`, si no se saltea y el leg firma sin verificar nada. Reescrita con las tres verdades + el gotcha de `AVALANCHE_RPC_URL`. **La viñeta se había reescrito en este mismo diff y la frase falsa sobrevivió** — misma lección que los MAYOR: editar un párrafo no es verificarlo.
- **Residuo del CR** (`settle-verifier.ts`): `rpcUnavailableResult` (gate de dinero de WKH-144) clasifica por SLUG, justo lo que el ⚠️ de `chain-resolver.ts:153` desaconseja. Es sano HOY sólo porque el assert de arranque garantiza "slug testnet ⇒ destino testnet". Documentado como **dependencia de seguridad explícita**: si alguien ablanda ese assert (lo baja a warning, lo scopea, o agrega un factory env-driven sin cubrirlo), WKH-144 vuelve a fail-OPEN sobre mainnet real, y ahí hay que migrar a `classifyDestinationEnvironment(destination)`.
- **Aplicar en**: si un guard es correcto por una precondición que vive en OTRO archivo, esa precondición va escrita en el docstring del guard. "Es seguro porque X" sin decir dónde vive X es una bomba de tiempo para el que ablande X.

### [2026-07-26 05:40] it4 — follow-ups NO hechos (decisión explícita del orquestador)
- **CR-MNR-2 — `describeBundleDestination(bundle)` sin extraer**: la derivación de `LegDestination` está DUPLICADA en `registry.ts:212-217` (dentro de `assertNoSlugDestinationDrift`) y `downstream-payment.ts:568-571`. Las dos ramas son idénticas (`vmFamily==='solana' ? {caip2ChainId} : {chainId: chainConfig.chainId}`) y si divergen, el gate del leg y el assert de arranque dejan de mirar lo mismo. **No se extrajo**: es refactor de estructura sobre el money-path que AR+F4 ya aprobaron en este diff. **Trigger**: cuando entre un tercer consumidor de `LegDestination`, o cuando aparezca una familia de VM nueva (ahí la duplicación se convierte en dos lugares que hay que acordarse de tocar).
- **CR-MNR-6 — `signAndSettleDownstream` en 354 líneas con la numeración de cortes ROTA** (`1,2,3,4,4a,4b,4b-i,<sin número>,4c,5`): el bloque sin número es justamente el gate de opt-in mainnet. **No se extrajo `checkLegEnvironment`** por el mismo motivo (estructura sobre money-path aprobado). **Trigger**: el próximo cambio funcional en el gate, o al agregar un rail no-EVM nuevo. Al hacerlo, renumerar la secuencia completa.
- **Ambos son de estructura, sin riesgo de dinero.** Anotados acá para que no se pierdan al cerrar la HU.

### [2026-07-26 05:42] it4 — TDs abiertas al cierre de esta iteración
| ID | Dónde | Trigger de reactivación |
|----|-------|-------------------------|
| `TD-NEW-KITE-PARAMS` | `MULTI-CHAIN.md` §10 | HU que requiera los dos rails Kite a la vez. **Ampliada en it4**: attestation/gasless siguen en 2368 incluso con Kite mainnet activo. |
| `TD-MAINNET-GATE-USA-CHAINCONFIG` | `MULTI-CHAIN.md` §10 | Se cierra con TD-NEW-KITE-PARAMS; revisar si entra otro adapter env-driven. |
| `TD-SOLANA-CAIP2-DENYLIST` | `MULTI-CHAIN.md` §10 + `chain-resolver.ts` (citada en it4) | Existir `solana-mainnet` **o** el rail Solana salir de flag OFF ⇒ allowlist fail-CLOSED obligatoria. |
| `TD-INBOUND-ZERO-PAYTO` | `MULTI-CHAIN.md` §10 (**nueva en it4**) | Antes de activar mainnet en el rail INBOUND, o al tocar el guard de `invokeAgent`. |
| `TD-AVALANCHE-DEPOSIT-AUTOMATION` | `MULTI-CHAIN.md` §10 | Post-MVP / volumen de smokes Avalanche. |
| `legIntentId` sin scope de tenant (it2 MNR-2) | este archivo, it2 | Un call-site que llame a `signAndSettleDownstream` SIN `intentId`. |
| Drift residual del rail Solana (`SOLANA_RPC_URL` mainnet + CAIP-2 devnet) | este archivo, it2 | Requeriría `getGenesisHash()` en el init (I/O al arrancar). |

## Iteración 3 (RE-AR APROBÓ la it2 con 0 bloqueantes — cierre de 4 MENORes)

### [2026-07-26 04:40] it3 MNR-1 — el runbook del HACKATHON-FINAL tenía las DOS envs falsas juntas (una brickea, la otra no existe)
- **Error**: `HACKATHON-FINAL.md` "Mainnet readiness → To activate mainnet" paso 3 instruía `KITE_NETWORK=mainnet` **y** `WASIAI_DOWNSTREAM_NETWORK=avalanche-mainnet`. La primera hoy rompe el arranque (el assert de it2 lanza si coexiste con el slug `kite-ozone-testnet`); la segunda no la lee ningún archivo de `src/` desde WKH-112. Un operador que siguiera ese runbook tiraba el gateway abajo y creía haber activado el downstream mainnet sin activar nada.
- **Causa raíz**: la it2 corrigió los runbooks de `doc/` pero no el reporte de cierre del hackathon, que también es un runbook ejecutable (y el más visible). El grep de la it2 se limitó a los archivos que "parecían" runbooks.
- **Fix**: sección reescrita y partida en los dos rails independientes: (A) downstream Avalanche = `WASIAI_DOWNSTREAM_MAINNET_ALLOW` + `AVALANCHE_RPC_URL` + slug en `WASIAI_A2A_CHAINS` + `WASIAI_DOWNSTREAM_X402=true` + fondos (los mismos pasos que ya declaran `scripts/activate-mainnet-downstream.sh` y MULTI-CHAIN §8, con el rollback por script); (B) inbound Kite mainnet = las 3 envs acopladas. **Cada env citada fue grepeada en `src/` para confirmar que existe y que alguien la lee** (`WASIAI_A2A_CHAINS` registry.ts:182, `KITE_NETWORK` kite-ozone/chain.ts:46, `KITE_MAINNET_RPC_URL` payment.ts:204 + deposit-verifier.ts:137 + gas-overhead.ts:147, `WASIAI_DOWNSTREAM_MAINNET_ALLOW` downstream-payment.ts:155, `WASIAI_DOWNSTREAM_X402` downstream-payment.ts:42, `AVALANCHE_RPC_URL` avalanche/payment.ts:153; las del facilitator contra `wasiai-facilitator/src/infra/env.ts`). Los párrafos históricos del 2026-04-29 se marcaron como histórico-no-ejecutable en vez de borrarse: la env SÍ se leía esa fecha (la introdujo `068` el 2026-04-28 y WKH-112 la dejó sin lectores el 2026-05-27), así que borrarla habría falseado la evidencia del hito.
- **Aplicar en**: todo archivo con un bloque de env vars copiables es un runbook, aunque se llame "reporte final". Y al retirar un control muerto: marcar el registro histórico como histórico, nunca reescribirlo como si nunca hubiera funcionado.

### [2026-07-26 04:45] it3 MNR-3 — el hint del assert mentía: "activá por slug" produce un bundle Kite mainnet ROTO
- **Error**: la remediación que it2 escribió en el throw de `assertChainEnvironmentCoherent` (`registry.ts`) decía *"Do NOT set KITE_NETWORK=mainnet: add the 'kite-mainnet' slug instead"*, y esa misma frase se replicó en 5 docs. El AR probó que seguirla al pie de la letra deja un rail roto: con `WASIAI_A2A_CHAINS=kite-mainnet` y `KITE_NETWORK` ausente el proceso arranca con `chainConfig.chainId=2366` (sin drift de slug detectable) pero `getPaymentAdapter('kite-mainnet').chainId === 2368` y `getToken()` devuelve el PYUSD de TESTNET.
- **Verificación propia (probe de factories REALES, `tsx`, no el mock)**: `[A]` slug solo → cfg 2366 / adapter 2368 / token `0x8E04…ec9` (PYUSD testnet). `[B]` slug + `KITE_NETWORK=mainnet` → 2366 / 2366 / `0x7aB6…149e` (USDC.e). `[C]` los dos slugs + env mainnet → LANZA por el slug testnet. `[D]` los dos slugs sin env → arranca con el rail mainnet firmando en 2368. `[E]/[F]/[G]` configs de prod (sin `kite-mainnet`) → intactas, cfg == adapter en los 6 rails EVM.
- **Causa raíz**: yo asumí que `{ network: 'mainnet' }` configuraba el bundle COMPLETO. Sólo configura el `chainConfig` (se calcula dentro del `try`, con la env mutada); el ADAPTER resuelve chainId/token/dominio EIP-712 en **call-time**, o sea DESPUÉS del `finally` que restaura la env (TD-NEW-KITE-PARAMS). Dos temporalidades distintas dentro del mismo factory, y yo documenté una como si fuera las dos.
- **Fix**: (1) hint reescrito con la verdad medida — `KITE_MAINNET_REMEDIATION` exige el slug `kite-mainnet` **Y** `KITE_NETWORK=mainnet` **Y** ningún slug Kite testnet en el CSV, y declara que los dos rails Kite no pueden convivir. (2) el mismo texto alineado en `MULTI-CHAIN.md` §4/§7/§8/§10, `HACKATHON-FINAL.md`, los 2 runbooks, `docs/networks.md`, `.env.example` y `README.md`. (3) el mock del factory Kite en `registry.test.ts` pasa a ser FIEL a las dos temporalidades (antes derivaba ambas de `opts`, lo que daba verde a configs que en real quedan rotas).
- **Aplicar en**: cuando un factory muta env en `try/finally`, preguntarse SIEMPRE qué partes del objeto devuelto se congelaron dentro del `try` y cuáles se resuelven en call-time. Y: un hint de remediación en un throw es DOCUMENTACIÓN EJECUTABLE — hay que probarla como se prueba un fix, no escribirla de memoria.

### [2026-07-26 04:50] it3 MNR-3 punto 2 — extender el assert al drift inverso: throw sólo en la dirección que pierde dinero
- **Decisión**: `assertChainEnvironmentCoherent` ahora compara además `chainConfig.chainId` contra el `chainId` del ADAPTER ya construido (sólo EVM: el `chainConfig.chainId` de Solana es un sentinel sintético y su adapter no expone `chainId`; ese rail ya se cubre por CAIP-2). La severidad se decide POR DIRECCIÓN:
  - adapter **mainnet** + `chainConfig` **testnet** ⇒ **THROW**: el gate del leg clasifica `chainConfig`, así que pasaría sin opt-in mientras el adapter firma dinero real. Hoy NINGÚN factory produce esa combinación (el registry nunca pasa `{network:'testnet'}`) — se documenta como defensa para el próximo adapter env-driven, NO como un control que hoy proteja algo alcanzable (lección BLQ-MED-1: no vender un control inejercitable como protección).
  - cualquier otro mismatch (el caso REAL de Kite) ⇒ `log.error` con `code=ADAPTER_CHAIN_ID_DRIFT` + remediación, y el arranque **sigue**.
- **Por qué NO un throw para todo** (lo intenté primero y lo bajé): volvía NO-ARRANCABLE una configuración que hoy arranca (cualquier CSV con los dos slugs Kite, p.ej. el "6-rail CSV" que pinnea `registry.test.ts` en la AC-2 de WKH-090), y no puedo verificar el `WASIAI_A2A_CHAINS` real del Railway de prod desde acá. La dirección alcanzable es money-SAFE (el gate lo trata como mainnet y exige opt-in; el dinero sería testnet), así que un rail roto en silencio se arregla haciéndolo ruidoso, no tirando el gateway abajo. Fix de fondo: TD-NEW-KITE-PARAMS.
- **Tests**: `T-it2-MNR-3-reg` (log + arranque + el bundle exacto que reportó el AR), `-reg-b` (la remediación del log dice la verdad y el hint viejo no puede volver), `-reg-c` (las dos combinaciones de la coexistencia), `-reg-d` (dirección peligrosa ⇒ throw). `T-it2-ALTO-1-reg` dejó de usar `mockResolvedValueOnce`: con el mock fiel, el input lo produce el MISMO mecanismo que en real (la env).
- **Aplicar en**: al endurecer un guard de arranque, separar "incoherencia que gasta dinero" (fail-loud/brick) de "incoherencia que rompe un rail" (fail-noisy). Un brick sobre una config que no podés verificar en prod es un outage autoinfligido.

### [2026-07-26 04:55] it3 MNR-5 — el inventario del invariante `ChainKey` omitía justamente al consumidor que sale por HTTP
- **Error**: el inventario de `types.ts` (escrito en it2) listaba 6 consumidores del invariante `-mainnet` y no incluía `payment-spec-reader.ts` → `resolveAvalancheOutputChain` (`isMainnetChainKey`), que es el ÚNICO cuyo resultado se publica por HTTP (`/discover` → `payment.chain`) y el que produjo BLQ-MED-1. Además `scripts/smoke-prod-via-app-wasiai.mjs:18` afirmaba que la chain real la decide `WASIAI_DOWNSTREAM_NETWORK` (control muerto) y la tabla de envs de `README.md` seguía ofreciendo `KITE_NETWORK=mainnet` a secas, sin remitir al warning que el propio README ya tenía 87 líneas arriba.
- **Causa raíz**: armé el inventario grepeando `isMainnetChainKey` en los módulos que había tocado en la it2, no en todo `src/`. Y una tabla de envs se lee como referencia rápida: un warning que vive en otra sección no la protege.
- **Fix**: 7º ítem agregado al inventario (marcado como el único que sale por HTTP + su rol en BLQ-MED-1); comentario del smoke corregido (la chain la declara el agente, el único control del operador es el gate); fila `KITE_NETWORK` del README con el acoplamiento y el link a MULTI-CHAIN §8.
- **Aplicar en**: un "inventario de consecuencias" se cierra con un grep del símbolo sobre TODO `src/`, y se ordena poniendo primero al consumidor que cruza el borde del sistema (HTTP/DB/chain).

### [2026-07-26 05:00] it3 MNR-4 — `classifySolanaCaip2` queda como denylist (TD, NO implementado)
- **Evaluado y NO cambiado**: convertirlo a allowlist de refs conocidas (fail-closed en desconocido) NO es trivial. (a) Rompería cualquier cluster local: `solana-test-validator` genera su propio genesis hash ⇒ CAIP-2 desconocido ⇒ `mainnet` ⇒ el gate fail-CLOSED mata el rail devnet en dev/CI. (b) Obliga a tocar 3 archivos de test que usan refs sintéticas (`solana:test` en `registry.test.ts`, `solana:testcluster` en `downstream-payment.test.ts` — el del money-path aprobado — y `chain-resolver.test.ts`), y con el fixture actual el bundle `solana-devnet` pasaría a tener drift de slug ⇒ `initAdapters` lanzaría en esos tests.
- **Por qué es aceptable hoy**: no es alcanzable por un agente (sale de `SOLANA_CAIP2_CHAIN_ID`, env-only), el rail está flag-gated OFF y NO existe ChainKey `solana-mainnet` (CD-4), así que ningún slug puede declarar mainnet.
- **Condición de reactivación** (documentada como `TD-SOLANA-CAIP2-DENYLIST` en `MULTI-CHAIN.md` §10): cuando exista `solana-mainnet` o el rail salga de flag OFF. Ahí la allowlist es obligatoria y el cluster local pasa a ser una excepción explícita por env.

## Iteración 2 (AR RECHAZÓ la it1: 4 bloqueantes)

### [2026-07-26 03:30] it2 W0 — clasificar mainnet por el STRING del slug era una premisa falsa (BLQ-ALTO-1)
- **Error**: el gate de opt-in del leg downstream (it1) decidía con `isMainnetChainKey(chainKey)`, es decir por el sufijo del slug. El AR probó con un probe ejecutable que se saltea: con `KITE_NETWORK=mainnet` (lo que instruían LOS DOS runbooks de activación) el bundle del ChainKey `kite-ozone-testnet` apunta a chainId 2366 / USDC.e de Kite MAINNET, mientras `isMainnetChainKey('kite-ozone-testnet') === false`. Un agent card con `payment.chain: 'kite-ozone-testnet'` (o `2368` / `kite-testnet`) settleaba dinero real del operator wallet con el gate vacío. El MISMO clasificador alimenta el fail-CLOSED de WKH-144 ⇒ ese settle mainnet real también fallaba OPEN.
- **Causa raíz**: el bundle de ese slug se construye SIN `opts` (`registry.ts` → `createKiteOzoneAdapters()`) y `getKiteChain()` / `getKiteNetworkTag()` leen `KITE_NETWORK` en **call-time** (deuda `TD-NEW-KITE-PARAMS`). El slug es una ETIQUETA de configuración, no el destino. Yo tomé la etiqueta como fuente de verdad porque `types.ts` la documenta como invariante — pero el invariante sólo dice cómo se DEBEN nombrar los slugs, no que la config no pueda contradecirlos.
- **Fix**: (1) la clasificación de dinero pasa al DESTINO REAL: `classifyDestinationEnvironment()` en `chain-resolver.ts` — EVM por `bundle.chainConfig.chainId` contra `EVM_CHAIN_ENVIRONMENT` (exhaustivo sobre una unión de literales; chainId desconocido ⇒ `mainnet`, fail-closed), Solana por el CAIP-2 del adapter (el `chainId` de Solana es un sentinel sintético env-driven, NO autoritativo). (2) `CANONICAL_CHAIN_ID: Record<ChainKey, KnownEvmChainId | 'non-evm'>` fuerza en COMPILACIÓN que una chain nueva declare su chainId y que ese chainId esté clasificado. (3) defensa en profundidad: `registry.initAdapters` → `assertChainEnvironmentCoherent()` LANZA si el destino real contradice el mainnet-ness del slug (verificado con probe real: la combinación `KITE_NETWORK=mainnet` + default CSV ya no arranca). (4) el gate bloquea el drift SIEMPRE, sin opt-in posible.
- **Aplicar en**: cualquier gate que decida sobre dinero mirando un STRING de configuración. Preguntar siempre "¿este string lo puede contradecir una env var leída en call-time?". Los otros 3 adapters (avalanche/base/tempo) reciben `network` por parámetro del registry y NO tienen el drift (probado); el único vector residual es Solana vía `SOLANA_RPC_URL` (ver abajo).

### [2026-07-26 03:34] it2 W1 — el "fix" de it1 mató un control muerto y creó otro inejercitable (BLQ-MED-1)
- **Error**: FIX 1(a) de it1 colapsaba TODO alias mainnet del namespace avalanche a `'avalanche'` (destino Fuji). Como `readPaymentSpec` es el ÚNICO productor de `Agent.payment`, NINGÚN valor declarable por un agente podía producir `chainKey='avalanche-mainnet'` en el leg ⇒ el opt-in `WASIAI_DOWNSTREAM_MAINNET_ALLOW=avalanche-mainnet` que instruyen 5 documentos era **inejercitable**. Y el test que lo "cubría" (`T-FIX1B-4`) INYECTABA `payment.chain='avalanche-mainnet'` en el fixture: un estado inalcanzable por los productores reales ⇒ falso verde.
- **Causa raíz**: dos controles superpuestos para el mismo riesgo. Cerré el bypass en el LECTOR (colapsando el destino) además de en el GATE, y al hacerlo el gate quedó sin entradas posibles. Nunca verifiqué la cadena completa card → productor → gate; testeé el gate con un fixture hecho a mano.
- **Fix**: el lector vuelve a reportar con fidelidad (los alias mainnet salen como su ChainKey `avalanche-mainnet`; el colapso legacy de `avalanche-testnet` y el pass-through de los alias testnet quedan intactos) y el GATE es el único choke-point que decide. `T-FIX1B-4` reescrito para recorrer card → `readPaymentSpec` → `signAndSettleDownstream` (+ `T-FIX1B-4b` para el card testnet). Probe propio confirma: `43114` y `avalanche-mainnet` → `payment.chain='avalanche-mainnet'` → `chainKey='avalanche-mainnet'`.
- **CAMBIO INTENCIONAL DE API OBSERVABLE**: `/discover` deja de mostrar `'avalanche'` para un agente que declara `avalanche-mainnet`/`43114` (lo consumen wasiai-v2, Chaski, remit-*). Es más honesto: reporta lo declarado, y el dinero lo bloquea el gate fail-closed.
- **Aplicar en**: un test de gate SIEMPRE debe construir su input con el productor real del dato. Si el fixture se escribe a mano, el test puede estar validando un estado que el sistema no puede alcanzar. Y: dos controles para el mismo riesgo no son "defensa en profundidad" si uno le corta la entrada al otro.

### [2026-07-26 03:36] it2 W2 — un script ejecutable con la env muerta miente más fuerte que un doc (BLQ-MED-2)
- **Error**: FIX 1(c) de it1 corrigió `.env.example`, README, `docs/networks.md` y 2 runbooks, pero dejó VIVO `scripts/activate-mainnet-downstream.sh:57`, que hacía `variableUpsert` de `WASIAI_DOWNSTREAM_NETWORK` y luego imprimía "✅ Hybrid mode activated / Outbound: Avalanche C-Chain MAINNET USDC (NEW)". Correrlo = creer que activaste mainnet sin activar nada. Faltaban además 3 hits en `doc/architecture/MULTI-CHAIN.md` (§8 paso 1, tabla §9 "la Variable de control", smoke post-merge).
- **Causa raíz**: grepié la env muerta sólo en `src/` y en los docs que ya conocía; no corrí el grep sobre `--include='*.sh'`. Un control muerto no vive sólo en prosa.
- **Fix**: script reescrito — setea `WASIAI_DOWNSTREAM_MAINNET_ALLOW=avalanche-mainnet`, agrega `--rollback` (lo vacía ⇒ fail-CLOSED) y su output dice la verdad, incluidos los pre-requisitos que el script NO hace (`WASIAI_A2A_CHAINS`, `WASIAI_DOWNSTREAM_X402`, fondos del operator). ~~MULTI-CHAIN.md actualizado en los 3 puntos.~~
- **CORRECCIÓN (it3 MNR-2, 2026-07-26)**: la frase "MULTI-CHAIN.md actualizado en los 3 puntos" era **falsa por incompleta**. Sobrevivía un 4º punto (§7 "Para Kite mainnet (post-merge)", que seguía instruyendo `KITE_NETWORK=mainnet` + RPC nueve líneas ANTES de que §8 dijera "⛔ NO setear `KITE_NETWORK=mainnet`") y, fuera de MULTI-CHAIN, seguían vivos `HACKATHON-FINAL.md` (runbook completo con la env muerta), `scripts/smoke-prod-via-app-wasiai.mjs:18` y la tabla de envs de `README.md`. Contar "los N puntos" a partir del grep que uno YA hizo es exactamente el error de la it2: el cierre correcto es re-grepear la variable/afirmación en TODO el repo y listar los hits, no confiar en la cuenta previa.
- **Aplicar en**: al retirar una env var, `grep -rn VAR --include='*.sh' --include='*.md' --include='*.yml' .` — y priorizar lo EJECUTABLE sobre la prosa.

### [2026-07-26 03:37] it2 W2 — guardear la chain declarada no es guardear el payTo (BLQ-BAJO-1)
- **Error**: FIX 4 de it1 decidía el skip del sign inbound con `agent.payment?.chain`, pero el valor que se castea a `0x${string}` sale de OTRA fuente (`agent.metadata.payTo` / `metadata.payment.contract`). El AR probó 2 caminos donde el base58 crudo seguía llegando a `viem.signTypedData`: (a) `metadata.payTo` base58 SIN `payment` declarado; (b) `payment.chain='avalanche-fuji'` (pasa el guard) + `metadata.payTo` base58.
- **Causa raíz**: guardeé un PROXY del valor en vez del valor. `discovery.mapAgent` setea `metadata: raw` (el card completo del registry, y cualquier caller autenticado puede registrar un registry), así que chain declarada y payTo son fuentes independientes y ninguna implica la otra.
- **Fix**: `payTo` se resuelve ANTES del guard y se valida con `isValidWallet` (`wallet-format.ts`, la fuente única del criterio EVM — `validatePayTo` de `downstream-payment.ts` ahora también la usa en vez de repetir el regex). El skip conserva `INBOUND_VM_UNSUPPORTED` y agrega `reason: DECLARED_CHAIN | PAY_TO_FORMAT`. 3 tests nuevos con los repros del AR.
- **Aplicar en**: si un guard protege un cast, tiene que mirar el MISMO dato que se castea. Cualquier otra señal (chain declarada, familia del adapter, flag) es un proxy que se puede desalinear.

### [2026-07-26 03:33] it2 — MNR-1: omitir el pre-check de fondos perdía la distinguibilidad en el camino self-heal
- **Error**: en el replay idempotente, it1 omitía el pre-check de balance por completo. Pero `SolanaPaymentAdapter.settle()` puede tomar el camino SELF-HEAL (si la firma previa no verifica on-chain, la borra y re-broadcastea FRESCO) ⇒ justo ahí se perdía `INSUFFICIENT_BALANCE` vs `SETTLE_FAILED`.
- **Fix**: en el replay el pre-check pasa de GATE a SONDA: se lee el balance y un balance bajo se reporta en INFO (`BALANCE_LOW_ON_IDEMPOTENT_REPLAY`) sin cortar nunca. Mantiene el fix de it1 (un leg ya pagado JAMÁS se reporta como no pagado) y recupera la observabilidad. Costo: una lectura de balance extra por replay.
- **Aplicar en**: antes de "saltear un chequeo porque el camino no mueve fondos", verificar TODAS las ramas del callee. Un método idempotente puede tener una rama que sí mueve fondos.

### [2026-07-26 03:40] it2 — deudas documentadas (NO implementadas)
- **MNR-2 — `legIntentId` sin scope de tenant**: el fallback `intentId ?? \`${agent.slug}:${payTo}\`` (`downstream-payment.ts`, `settleSolanaLeg`) no incluye tenant/owner ni ejecución. **HOY ES INALCANZABLE**: los 2 call-sites de `invokeAgent` (`compose.ts:316` happy-path y `compose.ts:486` retry-ok) siempre pasan `${composeRunId}:${i}`, con `composeRunId = randomUUID()` por ejecución (`compose.ts:120`). **Condición de reactivación**: si alguien agrega un call-site que llame a `signAndSettleDownstream` SIN `intentId`, dos ejecuciones distintas del mismo agente colisionarían en el seam de idempotencia y la segunda devolvería la firma de la primera sin pagar. **NO se hizo `intentId` requerido** porque obligaba a tocar ~35 call-sites de test (churn de riesgo alto para un fix-pack de dinero); queda como TD con esta condición explícita.
- **Drift residual del rail Solana**: no existe ChainKey `solana-mainnet` (CD-4), así que el slug no puede declarar mainnet, pero `SOLANA_RPC_URL` sí puede apuntar a mainnet-beta. La clasificación nueva cubre la mitad del vector (el CAIP-2: `SOLANA_CAIP2_CHAIN_ID` con el genesis de mainnet-beta ⇒ `mainnet` ⇒ gate fail-closed). Un `SOLANA_RPC_URL` mainnet con el CAIP-2 devnet NO se detecta — requeriría un `getGenesisHash()` al arrancar (I/O en el init). TD abierta.

### [2026-07-26 02:52] Wave 1 — el colapso "namespace completo" rompió una expectativa pinneada Y habría cambiado la API pública
- **Error**: primera implementación de FIX 1(a) colapsó TODO el namespace avalanche a `'avalanche'` (`getChainNamespace(chainKey) === 'avalanche' ? 'avalanche' : chainRaw`). El test `payment-spec-reader.test.ts:38` ("chain EVM reconocida (avalanche-fuji) pass-through") pasó a fallar: `'avalanche-fuji'` → `'avalanche'`.
- **Causa raíz**: tomar la regla más simple sin evaluar QUIÉN lee el valor de salida. `Agent.payment.chain` no es interno: se expone en las respuestas de `/discover` (lo consumen wasiai-v2 / Chaski / los remit-*). Cambiar `'avalanche-fuji'` → `'avalanche'` era un cambio de API observable, fuera del objetivo del fix (el bypass de dinero real).
- **Fix**: regla mínima y namespace-aware sobre el `ChainKey` normalizado (`collapsesToLegacyAvalanche`): colapsan (1) TODO alias que resuelve a `avalanche-mainnet` (cierra el bypass de `43114`) y (2) el literal legacy `'avalanche-testnet'` (byte-identidad). El resto de los alias testnet quedan pass-through — todos normalizan al MISMO `ChainKey`, así que no existe divergencia de destino posible. Cero expectativas existentes modificadas.
- **Aplicar en**: antes de "normalizar mejor" un campo, grepear si ese campo sale por HTTP. Un fix de seguridad no necesita cambiar strings que ya tienen destino único.

### [2026-07-26 02:54] Wave 2 — test del gate mainnet escrito contra una chain sin bundle en el mock
- **Error**: `T-FIX1B-2` incluía `kite-mainnet` / `2366` en el loop y falló: el log real fue `CHAIN_NOT_SUPPORTED`, no `MAINNET_NOT_ALLOWED`.
- **Causa raíz**: el mock del registry (`downstream-payment.test.ts`) sólo inicializa bundles para las chains que declaro en `CHAIN_IDS`; agregué `avalanche-mainnet`/`base-mainnet` pero no kite. El guard preexistente de bundle corre ANTES del gate nuevo, así que la chain nunca llegaba a evaluarse.
- **Fix**: el loop ejercita las mainnets que SÍ tienen bundle (el caso peligroso real: chain soportada + inicializada). Comentario en el test explicando que una mainnet no inicializada ya corta antes.
- **Aplicar en**: cuando se agrega un guard nuevo en una cadena de guards, el test tiene que dejar pasar TODOS los guards anteriores; si no, se está testeando el guard viejo y el nuevo queda sin cobertura (falso verde).

### [2026-07-26 02:50] Wave 0 — riesgo evitado: duplicar el clasificador de mainnet
- **Error potencial (no cometido)**: escribir `chainKey.endsWith('-mainnet')` inline en `downstream-payment.ts` porque `isMainnetChainKey` vivía en `settle-verifier.ts`, que arrastra viem + `deposit-verifier` + los 4 chain-factories (import pesado en un módulo del hot-path, y riesgo de romper el mock de `viem` del test).
- **Causa raíz**: el clasificador estaba en el módulo equivocado. Es una función PURA sobre el `ChainKey` (invariante WKH-144 documentado en `types.ts`), no algo del verificador on-chain.
- **Fix**: el cuerpo se movió a `chain-resolver.ts` (módulo puro, sólo importa tipos) y `settle-verifier.ts` lo re-exporta sin cambios → un solo clasificador, cero imports nuevos pesados, y el test de invariante WKH-150 (que lo importa desde `settle-verifier`) sigue verde. Un test nuevo cross-checkea que ambas rutas de import son la MISMA función.
- **Aplicar en**: si un helper puro hace falta en dos capas, moverlo a la capa pura y re-exportar. Nunca copiar la regla — un clasificador de mainnet duplicado que divergen es un incidente de dinero real.
