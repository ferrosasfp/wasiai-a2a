# Auto-Blindaje — 187 / fix-pack AR-profundo P0 money-path (FIX 1-4)

## Iteración 2 (AR RECHAZÓ la it1: 4 bloqueantes)

### [2026-07-26 03:30] it2 W0 — clasificar mainnet por el STRING del slug era una premisa falsa (BLQ-ALTO-1)
- **Error**: el gate de opt-in del leg downstream (it1) decidía con `isMainnetChainKey(chainKey)`, es decir por el sufijo del slug. El AR probó con un probe ejecutable que se saltea: con `KITE_NETWORK=mainnet` (lo que instruían LOS DOS runbooks de activación) el bundle del ChainKey `kite-ozone-testnet` apunta a chainId 2366 / USDC.e de Kite MAINNET, mientras `isMainnetChainKey('kite-ozone-testnet') === false`. Un agent card con `payment.chain: 'kite-ozone-testnet'` (o `2368` / `kite-testnet`) settleaba dinero real del operator wallet con el gate vacío. El MISMO clasificador alimenta el fail-CLOSED de WKH-144 ⇒ ese settle mainnet real también fallaba OPEN.
- **Causa raíz**: el bundle de ese slug se construye SIN `opts` (`registry.ts` → `createKiteOzoneAdapters()`) y `getKiteChain()` / `getKiteNetworkTag()` leen `KITE_NETWORK` en **call-time** (deuda `TD-NEW-KITE-PARAMS`). El slug es una ETIQUETA de configuración, no el destino. Yo tomé la etiqueta como fuente de verdad porque `types.ts` la documenta como invariante — pero el invariante sólo dice cómo se DEBEN nombrar los slugs, no que la config no pueda contradecirlos.
- **Fix**: (1) la clasificación de dinero pasa al DESTINO REAL: `classifyDestinationEnvironment()` en `chain-resolver.ts` — EVM por `bundle.chainConfig.chainId` contra `EVM_CHAIN_ENVIRONMENT` (exhaustivo sobre una unión de literales; chainId desconocido ⇒ `mainnet`, fail-closed), Solana por el CAIP-2 del adapter (el `chainId` de Solana es un sentinel sintético env-driven, NO autoritativo). (2) `CANONICAL_CHAIN_ID: Record<ChainKey, KnownEvmChainId | 'non-evm'>` fuerza en COMPILACIÓN que una chain nueva declare su chainId y que ese chainId esté clasificado. (3) defensa en profundidad: `registry.initAdapters` → `assertChainEnvironmentCoherent()` LANZA si el destino real contradice el mainnet-ness del slug (verificado con probe real: la combinación `KITE_NETWORK=mainnet` + default CSV ya no arranca). (4) el gate bloquea el drift SIEMPRE, sin opt-in posible.
- **Aplicar en**: cualquier gate que decida sobre dinero mirando un STRING de configuración. Preguntar siempre "¿este string lo puede contradecir una env var leída en call-time?". Los otros 3 adapters (avalanche/base/tempo) reciben `network` por parámetro del registry y NO tienen el drift (probado); el único vector residual es Solana vía `SOLANA_RPC_URL` (ver abajo).

### [2026-07-26 03:34] it2 W1 — el "fix" de it1 mató un control muerto y creó otro inejercitable (BLQ-MED-1)
- **Error**: FIX 1(a) de it1 colapsaba TODO alias mainnet del namespace avalanche a `'avalanche'` (destino Fuji). Como `readPaymentSpec` es el ÚNICO productor de `Agent.payment`, NINGÚN valor declarable por un agente podía producir `chainKey='avalanche-mainnet'` en el leg ⇒ el opt-in `WASIAI_DOWNSTREAM_MAINNET_ALLOW=avalanche-mainnet` que instruyen 5 documentos era **inejercitable**. Y el test que lo "cubría" (`T-FIX1B-4`) INYECTABA `payment.chain='avalanche-mainnet'` en el fixture: un estado inalcanzable por los productores reales ⇒ falso verde.
- **Causa raíz**: dos controles superpuestos para el mismo riesgo. Cerré el bypass en el LECTOR (colapsando el destino) además de en el GATE, y al hacerlo el gate quedó sin entradas posibles. Nunca verifiqué la cadena completa card → productor → gate; testeé el gate con un fixture hecho a mano.
- **Fix**: el lector vuelve a reportar con fidelidad (los alias mainnet salen como su ChainKey `avalanche-mainnet`; el colapso legacy de `avalanche-testnet` y el pass-through de los alias testnet quedan intactos) y el GATE es el único choke-point que decide. `T-FIX1B-4` reescrito para recorrer card → `readPaymentSpec` → `signAndSettleDownstream` (+ `T-FIX1B-4b` para el card testnet). Probe propio confirma: `43114` y `avalanche-mainnet` → `payment.chain='avalanche-mainnet'` → `chainKey='avalanche-mainnet'`.
- **CAMBIO INTENCIONAL DE API OBSERVABLE**: `/discover` deja de mostrar `'avalanche'` para un agente que declara `avalanche-mainnet`/`43114` (lo consumen wasiai-v2, Chaski, remit-*). Es más honesto: reporta lo declarado, y el dinero lo bloquea el gate fail-closed.
- **Aplicar en**: un test de gate SIEMPRE debe construir su input con el productor real del dato. Si el fixture se escribe a mano, el test puede estar validando un estado que el sistema no puede alcanzar. Y: dos controles para el mismo riesgo no son "defensa en profundidad" si uno le corta la entrada al otro.

### [2026-07-26 03:36] it2 W2 — un script ejecutable con la env muerta miente más fuerte que un doc (BLQ-MED-2)
- **Error**: FIX 1(c) de it1 corrigió `.env.example`, README, `docs/networks.md` y 2 runbooks, pero dejó VIVO `scripts/activate-mainnet-downstream.sh:57`, que hacía `variableUpsert` de `WASIAI_DOWNSTREAM_NETWORK` y luego imprimía "✅ Hybrid mode activated / Outbound: Avalanche C-Chain MAINNET USDC (NEW)". Correrlo = creer que activaste mainnet sin activar nada. Faltaban además 3 hits en `doc/architecture/MULTI-CHAIN.md` (§8 paso 1, tabla §9 "la Variable de control", smoke post-merge).
- **Causa raíz**: grepié la env muerta sólo en `src/` y en los docs que ya conocía; no corrí el grep sobre `--include='*.sh'`. Un control muerto no vive sólo en prosa.
- **Fix**: script reescrito — setea `WASIAI_DOWNSTREAM_MAINNET_ALLOW=avalanche-mainnet`, agrega `--rollback` (lo vacía ⇒ fail-CLOSED) y su output dice la verdad, incluidos los pre-requisitos que el script NO hace (`WASIAI_A2A_CHAINS`, `WASIAI_DOWNSTREAM_X402`, fondos del operator). MULTI-CHAIN.md actualizado en los 3 puntos.
- **Aplicar en**: al retirar una env var, `grep -rn VAR --include='*.sh' --include='*.md' --include='*.yml' .` — y priorizar lo EJECUTABLE sobre la prosa.

### [2026-07-26 03:37] it2 W2 — guardear la chain declarada no es guardear el payTo (BLQ-BAJO-1)
- **Error**: FIX 4 de it1 decidía el skip del sign inbound con `agent.payment?.chain`, pero el valor que se castea a `0x${string}` sale de OTRA fuente (`agent.metadata.payTo` / `metadata.payment.contract`). El AR probó 2 caminos donde el base58 crudo seguía llegando a `viem.signTypedData`: (a) `metadata.payTo` base58 SIN `payment` declarado; (b) `payment.chain='avalanche-fuji'` (pasa el guard) + `metadata.payTo` base58.
- **Causa raíz**: guardeé un PROXY del valor en vez del valor. `discovery.mapAgent` setea `metadata: raw` (el card completo del registry, y cualquier caller autenticado puede registrar un registry), así que chain declarada y payTo son fuentes independientes y ninguna implica la otra.
- **Fix**: `payTo` se resuelve ANTES del guard y se valida con `isValidWallet` (`wallet-format.ts`, la fuente única del criterio EVM — `validatePayTo` de `downstream-payment.ts` ahora también la usa en vez de repetir el regex). El skip conserva `INBOUND_VM_UNSUPPORTED` y agrega `reason: DECLARED_CHAIN | PAY_TO_FORMAT`. 3 tests nuevos con los repros del AR.
- **Aplicar en**: si un guard protege un cast, tiene que mirar el MISMO dato que se castea. Cualquier otra señal (chain declarada, familia del adapter, flag) es un proxy que se puede desalinear.

### [2026-07-26 03:33] it2 — MNR-1: omitir el pre-check de fondos perdía la distinguibilidad en el camino self-heal
- **Error**: en el replay idempotente, it1 omitía el pre-check de balance por completo. Pero `SolanaPaymentAdapter.settle()` puede tomar el camino SELF-HEAL (si la firma previa no verifica on-chain, la borra y re-broadcastea FRESCO) ⇒ justo ahí se perdía `INSUFFICIENT_BALANCE` vs `SETTLE_FAILED`.
- **Fix**: en el replay el pre-check pasa de GATE a SONDA: se lee el balance y un balance bajo se reporta en INFO (`BALANCE_LOW_ON_IDEMPOTENT_REPLAY`) sin cortar nunca. Mantiene el fix de it1 (un leg ya pagado JAMÁS se reporta como no pagado) y recupera la observabilidad. Costo: una lectura de balance extra por replay.
- **Aplicar en**: antes de "saltear un chequeo porque el camino no mueve fondos", verificar TODAS las ramas del callee. Un método idempotente puede tener una rama que sí mueve fondos.

### [2026-07-26 03:40] it2 — deudas documentadas (NO implementadas)
- **MNR-2 — `legIntentId` sin scope de tenant**: el fallback `intentId ?? \`${agent.slug}:${payTo}\`` (`downstream-payment.ts`, `settleSolanaLeg`) no incluye tenant/owner ni ejecución. **HOY ES INALCANZABLE**: los 2 call-sites de `invokeAgent` (`compose.ts:316` happy-path y `compose.ts:486` retry-ok) siempre pasan `${composeRunId}:${i}`, con `composeRunId = randomUUID()` por ejecución (`compose.ts:120`). **Condición de reactivación**: si alguien agrega un call-site que llame a `signAndSettleDownstream` SIN `intentId`, dos ejecuciones distintas del mismo agente colisionarían en el seam de idempotencia y la segunda devolvería la firma de la primera sin pagar. **NO se hizo `intentId` requerido** porque obligaba a tocar ~35 call-sites de test (churn de riesgo alto para un fix-pack de dinero); queda como TD con esta condición explícita.
- **Drift residual del rail Solana**: no existe ChainKey `solana-mainnet` (CD-4), así que el slug no puede declarar mainnet, pero `SOLANA_RPC_URL` sí puede apuntar a mainnet-beta. La clasificación nueva cubre la mitad del vector (el CAIP-2: `SOLANA_CAIP2_CHAIN_ID` con el genesis de mainnet-beta ⇒ `mainnet` ⇒ gate fail-closed). Un `SOLANA_RPC_URL` mainnet con el CAIP-2 devnet NO se detecta — requeriría un `getGenesisHash()` al arrancar (I/O en el init). TD abierta.

### [2026-07-26 02:52] Wave 1 — el colapso "namespace completo" rompió una expectativa pinneada Y habría cambiado la API pública
- **Error**: primera implementación de FIX 1(a) colapsó TODO el namespace avalanche a `'avalanche'` (`getChainNamespace(chainKey) === 'avalanche' ? 'avalanche' : chainRaw`). El test `payment-spec-reader.test.ts:38` ("chain EVM reconocida (avalanche-fuji) pass-through") pasó a fallar: `'avalanche-fuji'` → `'avalanche'`.
- **Causa raíz**: tomar la regla más simple sin evaluar QUIÉN lee el valor de salida. `Agent.payment.chain` no es interno: se expone en las respuestas de `/discover` (lo consumen wasiai-v2 / Chaski / los remit-*). Cambiar `'avalanche-fuji'` → `'avalanche'` era un cambio de API observable, fuera del objetivo del fix (el bypass de dinero real).
- **Fix**: regla mínima y namespace-aware sobre el `ChainKey` normalizado (`collapsesToLegacyAvalanche`): colapsan (1) TODO alias que resuelve a `avalanche-mainnet` (cierra el bypass de `43114`) y (2) el literal legacy `'avalanche-testnet'` (byte-identidad). El resto de los alias testnet quedan pass-through — todos normalizan al MISMO `ChainKey`, así que no existe divergencia de destino posible. Cero expectativas existentes modificadas.
- **Aplicar en**: antes de "normalizar mejor" un campo, grepear si ese campo sale por HTTP. Un fix de seguridad no necesita cambiar strings que ya tienen destino único.

### [2026-07-26 02:54] Wave 2 — test del gate mainnet escrito contra una chain sin bundle en el mock
- **Error**: `T-FIX1B-2` incluía `kite-mainnet` / `2366` en el loop y falló: el log real fue `CHAIN_NOT_SUPPORTED`, no `MAINNET_NOT_ALLOWED`.
- **Causa raíz**: el mock del registry (`downstream-payment.test.ts`) sólo inicializa bundles para las chains que declaro en `CHAIN_IDS`; agregué `avalanche-mainnet`/`base-mainnet` pero no kite. El guard preexistente de bundle corre ANTES del gate nuevo, así que la chain nunca llegaba a evaluarse.
- **Fix**: el loop ejercita las mainnets que SÍ tienen bundle (el caso peligroso real: chain soportada + inicializada). Comentario en el test explicando que una mainnet no inicializada ya corta antes.
- **Aplicar en**: cuando se agrega un guard nuevo en una cadena de guards, el test tiene que dejar pasar TODOS los guards anteriores; si no, se está testeando el guard viejo y el nuevo queda sin cobertura (falso verde).

### [2026-07-26 02:50] Wave 0 — riesgo evitado: duplicar el clasificador de mainnet
- **Error potencial (no cometido)**: escribir `chainKey.endsWith('-mainnet')` inline en `downstream-payment.ts` porque `isMainnetChainKey` vivía en `settle-verifier.ts`, que arrastra viem + `deposit-verifier` + los 4 chain-factories (import pesado en un módulo del hot-path, y riesgo de romper el mock de `viem` del test).
- **Causa raíz**: el clasificador estaba en el módulo equivocado. Es una función PURA sobre el `ChainKey` (invariante WKH-144 documentado en `types.ts`), no algo del verificador on-chain.
- **Fix**: el cuerpo se movió a `chain-resolver.ts` (módulo puro, sólo importa tipos) y `settle-verifier.ts` lo re-exporta sin cambios → un solo clasificador, cero imports nuevos pesados, y el test de invariante WKH-150 (que lo importa desde `settle-verifier`) sigue verde. Un test nuevo cross-checkea que ambas rutas de import son la MISMA función.
- **Aplicar en**: si un helper puro hace falta en dos capas, moverlo a la capa pura y re-exportar. Nunca copiar la regla — un clasificador de mainnet duplicado que divergen es un incidente de dinero real.
