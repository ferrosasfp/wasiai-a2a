# D-2 y D-3 — NO EJECUTADOS. Esto NO es evidencia: es el registro de por qué falta.
# fecha: 2026-08-25

MOTIVO: la credencial `A2A_PROBE_KEY` NO EXISTE todavía. Es el bloqueante conocido y
heredado de §16 del Story File, y su creación es del founder, no de un agente.

Medido, no supuesto:
  $ echo "${A2A_PROBE_KEY:-<AUSENTE>}"
  <AUSENTE>
  $ gh secret list --repo ferrosasfp/wasiai-a2a
  (salida VACÍA, exit 0 — el repo no tiene ningún repo secret configurado)

Qué queda pendiente, y qué NO:
  · D-1  EJECUTADO y en verde-sobre-rojo → `D-1-credencial-invalida.log` (exit 3, 0 USDC)
  · D-2  PENDIENTE — necesita la credencial real (≤ 0,0303 USDC)
  · D-3  PENDIENTE — necesita la credencial real Y el workflow en una rama del repo

⛔ Ninguno de los dos se simuló. Un log fabricado sería peor que la ausencia de log:
convertiría "no lo pude probar" en "lo probé", que es el defecto de origen de esta HU
aplicado a su propia evidencia.

CONSECUENCIA OPERATIVA que hay que mirar ANTES de mergear a main: sin el secret, la
corrida por reloj cae en la fila 0 → CONFIG → exit 3 → job rojo → issue abierto cada
30 min. La sonda lo diría bien (CONFIG, "producción no está implicada"), pero el ruido
es real. O se crea el secret antes del merge, o se mergea sabiendo esto.

Lo que el founder necesita para crearla (§16):
  · nombre: A2A_PROBE_KEY, como REPO SECRET (no environment secret)
  · presupuesto ≥ 44 USDC / 30 días
  · DAILY_LIMIT > 1,46 USD/día — si queda corta, la sonda se apaga sola cada tarde
    y lo reporta como CONFIG, no como caída
