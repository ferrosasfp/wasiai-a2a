# Auto-Blindaje — WKH-AUDIT-A2A-CLEANUP (098)

### [2026-05-29 23:32] Bloque A — work-item mislabeled x402.chain-aware.test.ts error
- **Error**: El work-item (sección A) categorizó `src/middleware/x402.chain-aware.test.ts` como un fallo de `funding_wallet` en `makeKeyRow()`. Ese archivo NO tiene `makeKeyRow` ni usa `A2AAgentKeyRow`. Su único error de tsc es `TS6059` (rootDir): importa `../../test/fixtures/passport-shape.js`, fichero ubicado fuera de `rootDir: ./src`.
- **Causa raíz**: El inventario del work-item se construyó asumiendo que los 6 errores de `tsc --noEmit` eran todos del mismo origen (`funding_wallet`). En realidad eran 5 de `funding_wallet` (TS2322) + 1 de rootDir (TS6059) pre-existente desde WKH-111/WKH-69. El fixture `test/fixtures/passport-shape.ts` lo importan DOS test files: `x402.chain-aware.test.ts` (in-scope) y `x402.passport-shape.test.ts` (OUT-of-scope).
- **Fix**: Los 5 errores reales de `funding_wallet` se resolvieron agregando `funding_wallet: null` al `makeKeyRow()` de los 5 archivos correctos (authz, compose, gasless, a2a-key, e2e/setup). El TS6059 de x402.chain-aware NO se puede resolver sin: (a) tocar tsconfig (CD-5 PROHIBIDO), (b) mover el fixture a `src/` afectando un import out-of-scope (x402.passport-shape.test.ts), o (c) inlinear el fixture solo en x402.chain-aware (no resuelve el error porque el otro importer sigue arrastrando el fichero al programa). ESCALADO al orquestador — ver reporte final.
- **Aplicar en**: Cualquier WKH futuro que prometa "tsc exit 0" debe verificar el código de error de CADA diagnostic individualmente (TS2322 vs TS6059), no agruparlos por archivo. Los fixtures compartidos en `test/` fuera de `rootDir` son una deuda estructural transversal.

### [2026-05-29 23:55] Bloque A — Opción 1 autorizada: mover fixture bajo rootDir (AC-1)
- **Error**: N/A (no fue un fallo de implementación; fue un bloqueo de scope). El TS6059 (rootDir) quedaba sin resolver porque el fix requería tocar un import OUT-of-scope (`x402.passport-shape.test.ts`).
- **Causa raíz**: El Story File no incluía `x402.passport-shape.test.ts` ni `test/fixtures/passport-shape.ts` en Scope IN, pero esos archivos eran los que arrastraban el fichero fuera de `rootDir` al programa de `tsc`. Sin ampliar scope era imposible cerrar AC-1.
- **Fix**: El orquestador autorizó la **Opción 1** (escalation rule #5 — ampliación de scope). Movido `test/fixtures/passport-shape.ts` → `src/__tests__/fixtures/passport-shape.ts` con `git mv`. Actualizado el import interno del fixture (`../../src/middleware/x402.js` → `../../middleware/x402.js`) y los 2 importers verificados por grep (`x402.chain-aware.test.ts` y `x402.passport-shape.test.ts`): `../../test/fixtures/passport-shape.js` → `../__tests__/fixtures/passport-shape.js`. Cambio mecánico, behavior-preserving. Resultado: `tsc --noEmit` exit 0 (AC-1 PASS), `tsc -p tsconfig.build.json --noEmit` exit 0, lint exit 0, build exit 0, vitest 1109 passed / 0 failed. `test/fixtures/` quedó vacío (untracked, no se borró — fuera de scope per regla #4); `test/` conserva archivos legítimos (`migrate-preflight.test.ts`, `smoke-passport-autonomous.test.mjs`, `types/`).
- **Aplicar en**: Fixtures de test deben vivir bajo `src/__tests__/fixtures/` (dentro de `rootDir`), NUNCA en `test/` raíz, para evitar TS6059. Si un futuro WKH crea un fixture compartido, ubicarlo bajo `src/`.
