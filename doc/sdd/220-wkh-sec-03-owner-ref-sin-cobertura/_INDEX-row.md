# Fila para `doc/sdd/_INDEX.md` — WKH-SEC-03

No se insertó directamente en `_INDEX.md` para no chocar con las otras HUs en vuelo
(convención ya usada por las HUs 212, 214 y 217). Copiar tal cual al final de la tabla.

⚠️ El número `220` es el siguiente libre **en este árbol** (máximo visible: `219-hu-323-…`).
Hay ramas sin mergear (WKH-325, WKH-326) que pueden haber tomado 220-222 en sus worktrees:
confirmar antes de crear la rama.

---

| 220 | 2026-08-05 | [WKH-SEC-03] Los 23 filtros por dueño que se pueden borrar con la suite entera en verde — la auditoría de mutación (`doc/audit/deuda-tecnica-2026-08-06/A1-guards-que-no-discriminan.md`, hallazgo 0) midió que **87 de los `.eq(...)` de `src/` se borran sin que ninguno de los 5308 tests se ponga rojo, y 23 de ellos son el filtro por dueño**, en 12 archivos. **Verificación caso por caso de este F1: los 23 filtros ESTÁN en el árbol (caso 1: falta el test), 0 son IDOR vivo (caso 2), 0 son improcedentes (caso 3)** — cada sitio verificado leyendo su archivo:línea, que es una pregunta distinta de la que respondió el barrido. **La corrección metodológica que cambia la lectura del hallazgo**: el barrido borra líneas que existen, así que **por construcción no puede encontrar un filtro ausente**; que los 23 sean caso 1 es una propiedad del instrumento, no del código, y el censo de caso 2 es una medición que **no se corrió**. Candidato concreto encontrado fuera de los 23: `reconciliation.ts:1128-1133` actualiza `a2a_payment_intent_debit_signatures` (tabla **con** `owner_ref`, `database.types.ts:807`) sin filtrar por dueño; no se declara IDOR porque no se verificó la procedencia del `keyId`. **Las tres sub-clases del caso 1, porque deciden qué test se escribe**: 18 sitios de aislamiento entre inquilinos alcanzables desde ruta autenticada; 2 de defensa en profundidad detrás de un pre-chequeo en JS (`agent.ts:715`, `fee-split.ts:697`) que **sólo** se matan con un test de entrelazado (receta ya existente en `task.ownership.test.ts:285-317`); y 3 (`fee-split.ts:365/538/618`) donde el `ownerRef` **no viene del caller** sino de `resolveRecipients` (`:215,224,238`), o sea que es ligadura de fila y no IDOR — decirlo al revés sería afirmar de más. **Causa medida de que la suite no los vea**: los fixtures tienen un solo actor, y un fixture de un solo dueño no puede refutar ningún filtro por dueño. **Mecanismo propuesto contra la reincidencia**: el `CLAUDE.md` ya declara esta omisión BLOQUEANTE desde WKH-53 y se violó 23 veces, así que se agrega un guardián estructural en la suite (`npm test` ya es paso obligatorio de CI, `ci.yml:43`) que **deriva** de `database.types.ts` las 18 tablas con `owner_ref` (la lista escrita a mano de `CLAUDE.md:198-205` dice 4) y exige el filtro o una excepción con motivo; se declara explícitamente lo que ese guardián NO mide (el valor pasado, las cadenas de una línea) para no repetir el hallazgo 17 de A1. Mutación en CI descartada por costo (~87 corridas de la suite completa). **Partida en dos HUs** por tamaño de revisión y no por urgencia (no hay IDOR vivo): SEC-03 = mecanismo + 12 sitios de superficie de API; **WKH-SEC-04** = los 11 de dinero y disputas (`fee-split`, `arbiter`, `arbiter/evidence`, `reconciliation`, `debit-capture`). Los otros 64 filtros no-`owner_ref` quedan fuera con inventario: 3 bloques importan (el compare-and-set de `reconciliation.ts:1131-1133`, los UPDATE de `a2a_fee_splits`, los 3 lookups de auth por hash) y `fee-charge.ts:595,695` es el único caso 3 real (`a2a_protocol_fees` no tiene columna de dueño). **CD-1: esta HU no toca una línea de producción.** | security/test-hardening | QUALITY | in progress (F1 hecho: `work-item.md` con 7 ACs EARS, 5 DTs, 6 CDs) | `feat/220-wkh-sec-03-owner-ref-sin-cobertura` ([work-item.md](220-wkh-sec-03-owner-ref-sin-cobertura/work-item.md)) |
