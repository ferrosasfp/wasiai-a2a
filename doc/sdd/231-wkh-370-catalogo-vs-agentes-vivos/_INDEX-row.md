# Fila para `doc/sdd/_INDEX.md` — WKH-370

⚠️ **Este archivo NO es el entregable: es un pendiente.** El F1 corrió sin herramienta `Edit`,
así que la fila quedó acá en vez de en el índice.

## Instrucciones de inserción

1. Pegar el bloque de abajo (**una sola línea**) en `doc/sdd/_INDEX.md`,
   **inmediatamente después de la línea 222** (la fila `230`, última de la tabla).
   La línea 223 es la línea en blanco que cierra la tabla: la fila nueva va **antes** de ella.
2. ⛔ **NO tocar NADA por encima de la línea 144** de `_INDEX.md` — `capability-risk.ts` cita
   ese tramo. Insertar al final no lo desplaza.
3. Borrar este archivo cuando la fila esté puesta, o el directorio queda con un artefacto
   que no es parte de la HU.

🔴 **Es obligatorio**: `test/sdd-index-matches-folders.test.ts` exige **una fila por carpeta**
y deriva las carpetas de `git ls-files doc/sdd`. Con la carpeta `231-…` en el índice de git y
sin su fila, `npm test` se pone **rojo**.

⚠️ **Orden**: `git add -A` va **antes** del gate final. El guardián lee el índice de git, no el
disco; con el entregable untracked da **verde en falso** (le pasó a WKH-369 ayer).

## La fila

| 231 | 2026-08-27 | [WKH-370] Nada compara el catálogo con los agentes vivos, y **son dos defectos de familias distintas** (issue #186). **(1) DERIVA**: la ficha de `a2a_agents` es una copia a mano del manifiesto del agente y nada las compara nunca; hoy da 0/5 tras la corrección manual de esta semana, pero nada lo vigila y se reabre en el próximo cambio de schema. **(2) FILA MAL NACIDA**, que es la tesis de la HU: `remit-kyc-session` y `remit-kyc-decision` se registraron con `metadata` **vacío** y **sin `payout_wallet`** ⇒ no *diferían* del manifiesto, **no tenían nada que comparar**, y un chequeo de deriva las habría dado por conformes. **Una fila mal nacida se ve igual que una sana desde el catálogo** — por eso AC-3 exige que una fila incompleta se reporte INCOMPLETA aunque su deriva dé cero. **La consecuencia de (2) es de dinero y está verificada en código**: con `payout_wallet` nulo, `src/services/agent-split-context.ts:50-52` devuelve `creator: null` ⇒ la pata de creador del split no existe y el 1 % se re-rutea a plataforma (ya se habían perdido 0,24 y 0,06 USDC). El modo de falla es **mudo por diseño**: no es un error que caiga en el best-effort de CD-10, es la rama normal del ternario, y **no loguea nada**. **Nada en el camino de escritura exige completitud y nada en el de lectura la extraña** (`agent.ts:713` escribe la columna sólo si viene; `:735-736` mergea `metadata.inputSchema` sólo si viene). **Las tres trampas medidas van como CD**: CD-1 el `inputSchema` **NO está en la raíz** de `/discover` sino en `metadata.inputSchema` —verificado: hay **dos mappers para la misma fila**, `mapRowToAgent` emite `metadata` entero (`agent.ts:165`) y sólo `mapRowToRecord` lo iza a la raíz (`:190`)— así que un barrido a la raíz devuelve *«0 de 29 publican schema»*, **falso**, y **un cero uniforme acusa a la consulta, no al catálogo** (AC-7 lo vuelve ejecutable); CD-2 el `pathSlug` **no es el slug en 2 de 5** (`remit-corridor-fx-solana` se sirve en `remit-corridor-fx`) ⇒ la clave de unión sale del `invokeUrl` y **se verifica contra la autodeclaración del manifiesto**, no se confía; CD-3 los **24 federados** de `wasiai-v2` pueden no servir manifiesto ⇒ el universo real son los **5 self-published**, y tratarlos igual da un chequeo crónicamente rojo, que **entrena a ignorarlo**. **HALLAZGO DE ARQUITECTURA DEL F1 QUE CONDICIONA EL DISEÑO**: la mitad de completitud **no se puede construir sobre ningún endpoint existente**, porque `payout_wallet` está **deliberadamente fuera de todo shape público** — no está en el `AgentRow` interno (`agent.ts:54-65`), no está en `mapRowToRecord` (`:174-198`), y es regla escrita: *«CD-5: se lee SOLO vía `getSplitContextRow`, nunca por un shape público»* (`agent-split-context.ts:13-14`) ⇒ hacen falta credenciales nuevas, y DT-3 deja las tres opciones abiertas **sin cerrarlas** (3a service key BYPASSRLS en un repo PÚBLICO 🔴, 3b un **booleano** de completitud en ruta autenticada 🟡 —recomendada, no viola CD-5 porque presencia ≠ valor—, 3c una llave de sólo lectura `[NO MEDIDO]`). **DÓNDE CORRE — la premisa del encargo se cae al medirla**: `probe-money-path.yml:100` pasa **un solo** secreto, `A2A_PROBE_KEY`, y **ninguna credencial de Supabase**; «ya tiene credenciales» es cierto para *gastar* y falso para *leer la base*. Y su aviso dedupea y cierra **por título exacto** (`:116,165`) ⇒ colgarse ahí reporta una deriva de catálogo bajo un título que acusa al camino del dinero y la cierra sola cuando el *pago* vuelve al verde. `ci.yml:4-6` queda descartado **no por flakiness sino porque es secret-free y sin base viva por decisión declarada**. ⇒ **recomendado workflow propio partido en dos jobs** (deriva sin secretos / completitud con credencial), con el exemplar estructural de `smoke-downstream.yml:52-94`; si la credencial falta, el job de deriva **sigue corriendo** y el otro sale CONFIG en vez de fingir verde. **DT-2 hereda de WKH-369 (fila 230, cerrada HOY) que la vista autoritativa es la LISTA**, y agrega la razón operativa nueva: `GET /discover/:slug` pasó a costar hasta **201 `supabase.from()`** por request y **perdió su exención de rate limit** ⇒ CD-8 prohíbe iterarlo agente por agente. **El entregable no es que dé verde hoy: es que se ponga rojo cuando corresponda** — AC-10 exige romperlo a propósito en las dos clases, **confirmar el rojo por su MOTIVO** y acompañarlo de un **control positivo**, y AC-4 cierra la vacuidad (cero comparaciones ⇒ CONFIG, nunca conforme). **Sizing subido de FAST+AR a QUALITY con razón escrita** y no por deferencia a `CLAUDE.md`: la mitad de completitud pide meter una credencial **BYPASSRLS** en las Actions de un **repo público** contra `bdwv`, que **es la que sirve producción**; el entregable es un guard y este repo tiene historial medido de guards que se leen a sí mismos (fila 224, con 1 defectuoso con nombre **todavía vacuo en `main`**); y el precedente inmediato es la fila 230, que hizo la misma corrección con el criterio de que **lo que decide el modo es la ubicación del arreglo, no la severidad del defecto**. **Scope OUT explícito**: corregir filas (ya se hizo a mano; esto es el vigilante), el camino del dinero, el pin del KYC, `src/services/discovery.ts`, y los 24 federados —que se **declaran y excluyen con razón escrita**, nunca en silencio (CD-10)—. ⚠️ **Este F1 corrió SIN SHELL** (sólo Read/Write/Glob): cada cita está marcada `[MEDIDO-F1]`, `[HEREDADO]` o `[NO MEDIDO]`; **no se pudo leer el issue #186 ni sus dos comentarios** (MI-1, primera tarea de F2), **no se pudo medir la ruta ni la forma del manifiesto** —`Glob **/*manifest*` da **cero** archivos en el repo, el manifiesto es remoto— (MI-2, bloqueante), **ningún conteo de agentes es propio**: el 29/24/5 es heredado y las dos filas del índice que lo corroboran dan **25 y 29** en fechas distintas ⇒ **el número se mueve y el chequeo debe derivarlo, no hardcodearlo** (MI-5); y **el nombre de la rama es una propuesta sin verificar**. | tooling/observabilidad | QUALITY | in progress (F1 hecho; pendiente HU_APPROVED → F2). ⚠️ La fila se insertó desde `_INDEX-row.md` porque el F1 no tenía herramienta `Edit`. | `feat/231-wkh-370-catalogo-vs-agentes-vivos` `[NO MEDIDO]` ([work-item.md](231-wkh-370-catalogo-vs-agentes-vivos/work-item.md)) |
