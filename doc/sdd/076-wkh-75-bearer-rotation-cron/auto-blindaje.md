
### [2026-05-02 01:49] Wave 2 — Workspace branch instability between Bash invocations
- **Error**: Repeated edits to `src/alerts.mjs`, `scripts/rotate-bearer.mjs` and `tests/alerts.test.mjs` reverted to baseline between Bash tool calls. Branch HEAD silently flipped from `feat/076-wkh-75-bearer-rotation-cron` to `feat/075-wkh-78-migration-preflight` and `feat/077-wkh-82-public-docs-onboarding` between consecutive Bash invocations. `bearer-rotation.mjs` written via Write tool then disappeared on next Bash check.
- **Causa raíz**: OpenClaw workspace did not preserve git HEAD across Bash sessions — different branches share the same working directory but the branch state (and hence file content) flips between commands. Edit/Write tools may target a different branch than the one most-recently checked out via Bash.
- **Fix**: Combine `git stash + git checkout + cat > file <<EOF + npm test + git commit` into ONE bash invocation per W2 deliverable batch. Do not rely on cross-call branch persistence. Use `set -e` plus an explicit `[ "$(git branch --show-current)" = "<expected>" ] || exit 1` guard.
- **Aplicar en**: future waves W3..W6, F4 QA pass, any nexus-dev session that touches `mcp-servers/wasiai-x402/` from a multi-branch workspace. Always do file-write + test + commit in a single atomic Bash command. Verify branch BEFORE and AFTER every batch of writes.

### [2026-05-02 02:18] Wave 4 — Edit-tool writes land on wrong branch when used between Bash calls
- **Error**: After running `git checkout feat/076-wkh-75-bearer-rotation-cron` in one Bash call and then issuing 3 `Edit`/`Write` tool calls (no Bash in between), a subsequent Bash call reported `git branch --show-current` = `feat/075-wkh-78-migration-preflight`, with the W4 file modifications staged as `M` against THAT branch (not 076). The edits did not vanish — they got "stuck" on whatever branch the workspace decided to expose at the moment the Edit tool ran. The W2 fix (atomic Bash) is necessary but NOT sufficient when intermediate Edit/Write tool calls exist.
- **Causa raíz**: The Edit/Write tools also target the workspace at "their own moment in time" — there is no transactional binding between a Bash `git checkout` and the next Edit. So Edit reads/writes against whichever branch is currently checked out at the millisecond it runs, which may be a sibling branch's HEAD.
- **Fix**: When file writes must happen between Bash calls (because we use Edit for surgical changes), recover with the `stash → checkout target → stash pop → run tests → commit` pattern, all inside a single subsequent Bash call. The W4 commit (32dbdbe) used exactly this pattern and succeeded: 225 tests passing, branch verified pre/post.
- **Aplicar en**: any future wave that mixes Edit/Write with Bash on this workspace. Two safe strategies: (a) write everything via heredocs inside a single Bash call (W2 pattern), or (b) accept the drift, then `git stash push -- <files>` followed by `git checkout <target>` + `git stash pop` inside ONE Bash call before testing/committing (W4 pattern). Always verify `git branch --show-current` BEFORE and AFTER the test run, since long-running tests can also straddle a branch flip.

